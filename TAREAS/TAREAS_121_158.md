# TAREAS — Lote 7 (121–158, 38 tareas)

> Instrucciones para el agente local (Claude Code / VS Code):
> Ejecutá las tareas en orden. Después de cada tarea, agregá una entrada al final de
> `TAREAS/REPORTE_DE_CAMBIOS.md` describiendo qué se hizo, qué archivos se tocaron y si quedó
> algo pendiente o algo que ya estaba implementado de otra forma a la esperada. Este lote es más
> grande que los anteriores (38 tareas en vez de 20) — no hace falta terminarlo en una sola
> sesión, pero cada tarea debe reportarse individualmente apenas se completa, no al final.
>
> Regla de oro: no modifiques lógica de negocio, BD ni archivos fuera de los indicados. Ante la duda, menos es más.
>
> Antes de tocar código, correr `npx tsc --noEmit` para confirmar que partís de un estado limpio,
> y volver a correrlo antes de reportar cada tarea como completada.

---

## SECCIÓN: USUARIOS

### TAREA 121 · 🟢

**Descripción:** Usuario activo siempre visible en el header con nombre y rol.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: esto ya está parcialmente hecho. En `src/app/ui/app-shell.tsx`, el componente `SidebarUser`
(barra lateral, visible en desktop) ya muestra `businessName` y `userName` obtenidos de
`GET /api/me`. Falta el rol: sumá el rol debajo del nombre, con una etiqueta corta en español
(OWNER → "Dueño", CASHIER → "Cajero", INVENTORY → "Inventario", READONLY → "Solo lectura").
Revisá qué devuelve hoy `/api/me` (`src/app/api/me/route.ts`) — si ya incluye `role` en el
payload (se usa en `AppShell` para filtrar `hiddenForRoles`), reusalo directamente en
`SidebarUser`, no dupliques el fetch.
```

**RESTRICCIONES ESTRICTAS:**
- No cambies el fetch a `/api/me` si ya devuelve `role` (evitá pedirlo dos veces).
- No toques el layout del resto del `AppShell` ni la navegación.

**Archivos afectados:** `src/app/ui/app-shell.tsx`.

---

### TAREA 122 · 🟡

**Descripción:** Confirmación de dos pasos al eliminar usuario con ventas asociadas.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

`DELETE /api/users/[id]` hoy borra el usuario directamente (solo protege contra que se borre
a sí mismo). El modelo `Sale` tiene `sellerId String?` sin relación FK a `User` (de solo
lectura/histórico, confirmá en `prisma/schema.prisma`), así que borrar un `User` no rompe
ventas existentes a nivel de BD — pero puede ser confuso para el dueño si borra a alguien que
generó muchas ventas.

En `src/app/api/users/[id]/route.ts` (`DELETE`), antes de borrar contá cuántas
`prisma.sale.count({ where: { tenantId, sellerCode: user.userCode } })` tiene asociadas ese
usuario (usá `sellerCode`, que es el campo real que vincula ventas a vendedores, no un ID). Si
el conteo es mayor a 0, devolvé una respuesta que incluya ese número en vez de borrar
directamente. En `src/app/ui/users-list.tsx`, el modal de confirmación ya existente
("¿Eliminar usuario?", línea ~303) debe mostrar ese número si es mayor a 0
("Este usuario tiene N ventas asociadas") y requerir un segundo click de confirmación explícito
(agregá un segundo botón "Sí, eliminar de todas formas" que solo aparece después de ver la
advertencia, o un checkbox "Entiendo que esto no afecta las ventas ya registradas").
```

**RESTRICCIONES ESTRICTAS:**
- No agregues una FK real `Sale.sellerId → User.id` — es un cambio de modelo mucho más grande
  y fuera de alcance; el conteo por `sellerCode` alcanza para esta advertencia.
- El borrado sigue siendo permitido (no lo bloquees), solo agregá el paso de confirmación extra.

**Archivos afectados:** `src/app/api/users/[id]/route.ts`, `src/app/ui/users-list.tsx`.

---

### TAREA 124 · 🟡

**Descripción:** Indicador de último acceso y sesión activa por usuario.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El modelo `User` no tiene ningún campo de último acceso. Agregá `lastLoginAt DateTime?`
(nullable, aditivo) vía `npx prisma migrate dev --name add_user_last_login_at`. Actualizalo en
`POST /api/auth/login` (`src/app/api/auth/login/route.ts`), justo después de verificar la
contraseña y antes de crear la sesión: `prisma.user.update({ where: { id: user.id }, data: { lastLoginAt: new Date() } })`.

En `src/app/ui/users-list.tsx`, agregá una columna "Último acceso" que muestre la fecha
formateada (o "Nunca" si es null). "Sesión activa" es más difícil de determinar con precisión
(las sesiones son JWT sin tabla de sesiones activas en BD — revisá `src/lib/tenant.ts`/
`src/lib/auth.ts` antes de asumir que existe tracking de sesiones); si no hay una forma
confiable de saber si una sesión sigue activa ahora mismo, no inventes ese dato — mostrá
únicamente "Último acceso" y documentá en el reporte por qué no se agregó un indicador de
sesión activa en tiempo real.
```

**RESTRICCIONES ESTRICTAS:**
- No implementes un sistema de sesiones activas en tiempo real (WebSockets, tabla de sesiones,
  etc.) si no existe ya — es un cambio de arquitectura mucho mayor a esta tarea.
- Migración nullable, sin backfill.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/app/api/auth/login/route.ts`,
`src/app/ui/users-list.tsx`.

---

### TAREA 139 · 🟠

**Descripción:** Rol SUPERVISOR: acceso a reportes y clientes sin configuración ni caja.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El enum `UserRole` en `prisma/schema.prisma` hoy es `OWNER | CASHIER | INVENTORY | READONLY`.
Agregá `SUPERVISOR` vía `npx prisma migrate dev --name add_user_role_supervisor` (agregar un
valor a un enum de Postgres es aditivo, no rompe usuarios existentes).

Definí los permisos de SUPERVISOR: acceso de lectura/escritura a Reportes y Clientes (incluida
la gestión de deudas de clientes), acceso de solo lectura a Ventas/POS si ya lo tiene READONLY
(revisá qué puede ver READONLY hoy como referencia), SIN acceso a Configuración ni a Caja
(cierre/apertura). Revisá `requireRole` en `src/lib/tenant.ts` y todos los `requireRole([...])`
existentes en los endpoints de settings y cash-register para agregar o no `SUPERVISOR` según
corresponda, y `hiddenForRoles` en `src/app/ui/app-shell.tsx`/`SettingsNav.tsx` para ocultar
los links de Ajustes y Caja a este rol.

Sumá `SUPERVISOR` como opción en el selector de rol al crear/editar usuarios
(`src/app/ui/users-list.tsx`, buscá dónde se listan los roles hoy, probablemente un `<select>`
con las 4 opciones actuales).
```

**RESTRICCIONES ESTRICTAS:**
- No cambies el comportamiento de los roles existentes (OWNER/CASHIER/INVENTORY/READONLY) —
  SUPERVISOR es un rol nuevo, aditivo.
- Repasá TODOS los `requireRole` del proyecto antes de dar la tarea por terminada (usá grep de
  `requireRole(\[` para no dejar ningún endpoint con permisos inconsistentes para este rol nuevo).

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/lib/tenant.ts`,
`src/app/ui/app-shell.tsx`, `src/app/settings/components/SettingsNav.tsx`,
`src/app/ui/users-list.tsx`, y los endpoints de settings/cash-register que usen `requireRole`.

---

### TAREA 147 · 🟢

**Descripción:** Foto de perfil de usuario para identificación visual en el POS.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

No hay integración con almacenamiento de archivos en la nube en este proyecto (confirmá
revisando si hay algún cliente de S3/Cloudinary/etc. antes de asumir lo contrario). El único
precedente de "subir una imagen" es el logo del negocio en `settings.tsx`
(`handleFileChange`/`logo-upload`), que hoy solo genera un preview local con
`URL.createObjectURL` sin persistirlo — no lo uses como está, es un preview roto sin guardado real.

Para esta tarea, agregá `avatarUrl String?` al modelo `User` (nullable, aditivo) vía
`npx prisma migrate dev --name add_user_avatar_url`. En `src/app/ui/users-list.tsx` (en el
formulario de edición de usuario), agregá un input de archivo que convierta la imagen elegida a
un data URL en base64 con `FileReader.readAsDataURL()` (igual límite de tamaño que el logo,
2 MB, con validación de tamaño antes de convertir) y lo guarde en `avatarUrl` vía
`PATCH /api/users/[id]`. Mostrá el avatar (o las iniciales del nombre si no tiene foto, mismo
patrón que `SidebarUser` en `app-shell.tsx`) en la lista de usuarios y en el selector de
vendedor del POS (revisá cómo se elige/identifica al vendedor actual en `pos.tsx`).
```

**RESTRICCIONES ESTRICTAS:**
- No implementes subida a un servicio externo de almacenamiento — usá data URL en base64
  persistido directamente en la columna `avatarUrl`, igual límite de 2MB que el logo.
- Migración nullable, sin backfill.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/app/api/users/[id]/route.ts`,
`src/app/ui/users-list.tsx`, `src/app/ui/pos.tsx`.

---

### TAREA 148 · 🔴

**Descripción:** Permisos granulares: habilitar o bloquear acceso a secciones por rol.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Hoy los permisos por rol están hardcodeados en el código (arrays `hiddenForRoles` en
`app-shell.tsx`/`SettingsNav.tsx`, y `requireRole([...])` en cada endpoint) — no hay ninguna
tabla ni configuración editable por el OWNER. Implementar permisos verdaderamente granulares y
configurables es un cambio grande; acotá el alcance así:

1. Agregá un modelo nuevo `RolePermission` (tenantId, role: UserRole, section: String,
   canAccess: Boolean @default(true)) vía migración, con un índice único
   `@@unique([tenantId, role, section])`.
2. Definí una lista fija de "secciones" configurables: las mismas que ya existen en
   `navItems` de `app-shell.tsx` (dashboard, pos, returns, products, customers, cashMovements,
   quotes, reports, promotions, settings).
3. En Configuración → Usuarios (revisá `UsuariosPanel.tsx` o donde se gestionan roles hoy),
   agregá una tabla de checkboxes (rol × sección) que el OWNER pueda editar, con un endpoint
   nuevo `PATCH /api/role-permissions` que haga upsert de las filas cambiadas.
4. En `AppShell`, antes de filtrar por `hiddenForRoles` hardcodeado, consultá primero si hay
   una fila en `RolePermission` para `(tenantId, role, section)`; si existe y `canAccess` es
   `false`, ocultá el link. Si no hay ninguna fila configurada para esa combinación, mantené el
   comportamiento actual (`hiddenForRoles` hardcodeado) como fallback — no rompas el
   comportamiento por defecto para tenants que nunca configuraron nada.
```

**RESTRICCIONES ESTRICTAS:**
- El rol OWNER nunca debe poder ser bloqueado de Configuración/Usuarios (evitá que el dueño se
  encierre fuera de su propia cuenta) — validalo tanto en el endpoint como en la UI.
- No elimines los `requireRole` server-side existentes — los permisos granulares son una capa
  adicional de UI/navegación, no un reemplazo de la autorización real en los endpoints.

**Archivos afectados:** `prisma/schema.prisma` (migración, modelo nuevo), `src/app/api/role-permissions/route.ts`
(nuevo), `src/app/ui/app-shell.tsx`, `src/app/settings/components/UsuariosPanel.tsx`.

---

### TAREA 149 · 🟡

**Descripción:** Audit log: registro de actividad por usuario (quién hizo qué y cuándo).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: el backend de esto ya existe casi completo — `src/modules/audit/audit-data-access.ts`
(`logAudit`, tipo `AuditAction` con SALE_CREATED/CASH_REGISTER_OPENED/CASH_REGISTER_CLOSED/
PRODUCT_CREATED/PRODUCT_UPDATED/PRODUCT_DELETED/PRODUCT_PRICE_CHANGE/INVENTORY_ADJUSTED/
USER_CREATED/USER_ROLE_CHANGED/USER_DELETED) y un endpoint `src/app/api/audit-logs/route.ts`
ya existen y ya se usan desde varios endpoints (revisá `src/app/api/users/route.ts` como
ejemplo). Lo que falta es la UI para verlo — no existe ninguna pantalla hoy.

Creá una nueva pantalla en Configuración (agregala como una categoría más en `settings.tsx`,
siguiendo el patrón de las demás secciones tipo `DocumentosSection`) que liste el audit log:
tabla con fecha, usuario (userCode), acción (traducida a español legible, ej.
"USER_ROLE_CHANGED" → "Cambio de rol"), entidad afectada, con paginación (revisá qué parámetros
acepta ya `GET /api/audit-logs` antes de asumir su forma) y un filtro simple por usuario y por
tipo de acción.
```

**RESTRICCIONES ESTRICTAS:**
- No modifiques `logAudit` ni los call sites existentes que ya registran eventos — es
  puramente una pantalla de lectura sobre datos que ya se están grabando.
- Solo visible para OWNER (y SUPERVISOR si la Tarea 139 ya se implementó) — restringí el acceso
  igual que el resto de Configuración.

**Archivos afectados:** `src/app/ui/settings.tsx` (nueva sección), posible ajuste en
`src/app/api/audit-logs/route.ts` solo si falta paginación/filtros (revisar antes de tocar).

---

### TAREA 150 · 🟠

**Descripción:** PIN rápido de 4 dígitos para cambio de cajero sin cerrar sesión.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Agregá `pin String?` (nullable, hasheado igual que `password` — revisá `src/lib/auth.ts` para
reusar la misma función de hash) al modelo `User` vía
`npx prisma migrate dev --name add_user_pin`. En Configuración → Usuarios, cada usuario puede
configurar su propio PIN de 4 dígitos (input numérico, validado a exactamente 4 dígitos).

En `pos.tsx`, agregá una opción "Cambiar de cajero" (ícono de usuario, visible en el header del
POS) que abra un modal pidiendo: 1) seleccionar el usuario de una lista (mismo tenant,
`GET /api/users`), 2) ingresar su PIN. Creá un endpoint nuevo `POST /api/auth/switch-cashier`
que valide el PIN contra el usuario seleccionado (sin pedir la contraseña completa) y actualice
la sesión actual para reflejar el nuevo "cajero activo" — revisá `src/lib/auth.ts`/
`createSession` para ver qué forma tiene el payload de sesión y si conviene agregar un campo
`activeCashierId` separado del `userId` de la sesión original, en vez de reemplazar la sesión
completa (así no se pierde el rol/permisos de quien inició sesión originalmente si el cambio de
cajero es solo para identificar quién está operando la caja en ese momento).
```

**RESTRICCIONES ESTRICTAS:**
- El PIN es un mecanismo adicional de identificación rápida, NO reemplaza el login completo con
  contraseña — no lo uses para iniciar sesión desde cero, solo para cambiar de cajero dentro de
  una sesión ya autenticada.
- Hasheá el PIN igual que la contraseña — nunca lo guardes en texto plano.
- Migración nullable — el PIN es opcional, no obligatorio.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/lib/auth.ts`,
`src/app/api/auth/switch-cashier/route.ts` (nuevo), `src/app/ui/users-list.tsx`,
`src/app/ui/pos.tsx`.

---

## SECCIÓN: CLIENTES

### TAREA 125 · 🟢

**Descripción:** Deuda pendiente en rojo y en grande en el encabezado del perfil de cliente.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/customer-detail.tsx`, revisá el encabezado del perfil de cliente (nombre,
datos de contacto). Si el cliente tiene deuda pendiente (`Debt.remainingAmount > 0` para ese
`customerId` — revisá cómo se obtiene hoy esa deuda, probablemente ya viene en el fetch del
detalle del cliente o hay que sumarlo desde `/api/debts?customerId=...`), agregá un bloque
destacado en el encabezado (texto grande, color rojo/rose, ej. `text-3xl font-bold text-rose-700`)
mostrando "Deuda pendiente: $X" justo debajo del nombre del cliente, antes que el resto de la
información de contacto. Si no tiene deuda, no muestres nada (no un "$0" en rojo).
```

**RESTRICCIONES ESTRICTAS:**
- No dupliques el cálculo de deuda si ya está disponible en el fetch existente del detalle del
  cliente — solo agregá un fetch nuevo si realmente no está disponible.
- No modifiques la lógica de negocio de deudas, solo la presentación en el encabezado.

**Archivos afectados:** `src/app/ui/customer-detail.tsx`.

---

### TAREA 126 · 🟡

**Descripción:** Dirección de entrega o facturación del cliente.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El modelo `Customer` no tiene ningún campo de dirección hoy. Agregá `address String?`
(nullable, aditivo) vía `npx prisma migrate dev --name add_customer_address`. Sumá el campo al
formulario de creación/edición de cliente (`src/app/ui/customer-new-form.tsx` y la edición en
`customer-detail.tsx`) como un input de texto simple (no hace falta separar calle/ciudad/CP en
esta tarea), y mostralo en el detalle del cliente y en el PDF de cotización si el cliente
asociado tiene dirección cargada (revisá `quote-pdf.tsx`, agregalo como línea opcional debajo
de los datos del cliente si `customer.address` existe).
```

**RESTRICCIONES ESTRICTAS:**
- No migres a un modelo de dirección estructurado (calle, ciudad, provincia, CP por separado) —
  un campo de texto libre alcanza para esta tarea.
- Migración nullable, sin backfill.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/modules/customers/customer-validation.ts`,
`src/app/ui/customer-new-form.tsx`, `src/app/ui/customer-detail.tsx`, `src/app/ui/quote-pdf.tsx`.

---

### TAREA 127 · 🟢

**Descripción:** Campo de notas / observaciones libres por cliente (memo interno).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Agregá `internalNotes String?` (nullable, aditivo) al modelo `Customer` vía
`npx prisma migrate dev --name add_customer_internal_notes`. Sumá un `<textarea>` "Notas
internas" en el formulario de edición de `customer-detail.tsx`, con una aclaración visual de que
es un memo interno del negocio (no se le muestra al cliente en ningún ticket/PDF/email — no lo
incluyas en `quote-pdf.tsx` ni en ningún comprobante).
```

**RESTRICCIONES ESTRICTAS:**
- Este campo NUNCA debe aparecer en ningún documento que vea el cliente (PDF, email, ticket,
  WhatsApp) — es explícitamente interno.
- Migración nullable, sin backfill.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/modules/customers/customer-validation.ts`,
`src/app/ui/customer-detail.tsx`.

---

### TAREA 135 · 🟢

**Descripción:** Fecha de cumpleaños para acciones de fidelización.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Agregá `birthDate DateTime?` (nullable, aditivo) al modelo `Customer` vía
`npx prisma migrate dev --name add_customer_birth_date`. Sumá un input de tipo `date` en el
formulario de edición de cliente. En `src/app/ui/customers-list.tsx`, agregá un pequeño
indicador (ej. ícono de torta/regalo) junto al nombre de los clientes cuyo cumpleaños cae
dentro de los próximos 7 días (compará mes/día de `birthDate` contra la fecha actual, sin
considerar el año). No implementes envío automático de saludos por esta tarea — es solo el
campo y el indicador visual; el envío de mensajes de cumpleaños queda fuera de alcance.
```

**RESTRICCIONES ESTRICTAS:**
- No implementes envío automático de emails/WhatsApp de cumpleaños en esta tarea — solo el
  campo y el indicador visual en la lista.
- Migración nullable, sin backfill.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/modules/customers/customer-validation.ts`,
`src/app/ui/customer-detail.tsx`, `src/app/ui/customers-list.tsx`.

---

### TAREA 136 · 🟢

**Descripción:** Exportar lista de clientes a CSV / Excel.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Seguí el mismo patrón de exportación client-side ya usado en `sales-list.tsx`,
`cash-movements-list.tsx` y `expenses-list.tsx` (`escapeCsvValue` + construcción manual del CSV
+ `Blob` + `URL.createObjectURL` + `<a>` con click programático — sin librerías nuevas).
Agregá un botón "Exportar" en `src/app/ui/customers-list.tsx` (cerca de los filtros/acciones
existentes) que descargue un CSV con: nombre, teléfono, email, código de cliente, segmento,
y (si ya se agregaron en tareas anteriores de este mismo lote) dirección y fecha de cumpleaños.
Respetá los filtros actualmente aplicados en la lista (búsqueda, segmento, deuda) — exportá el
array ya filtrado que se renderiza en la tabla, no todos los clientes sin filtrar. Nombrá el
archivo `clientes-YYYYMMDD.csv`.
```

**RESTRICCIONES ESTRICTAS:**
- No agregues librerías nuevas de generación de Excel — CSV plano (abre bien en Excel) alcanza,
  igual que en las demás pantallas.
- No dupliques `escapeCsvValue` si ya está definida en este archivo o es fácil de importar
  compartida; si no, replicala localmente (es una función chica, no vale la pena refactorizar
  un módulo compartido solo por esto).

**Archivos afectados:** `src/app/ui/customers-list.tsx`.

---

### TAREA 137 · 🟢

**Descripción:** Búsqueda por CUIT / DNI además de por nombre completo.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El modelo `Customer` no tiene ningún campo de CUIT/DNI hoy (revisá `prisma/schema.prisma` para
confirmar). Antes de agregar el campo, revisá si esta tarea depende de que exista — si no
existe, agregá `taxId String?` (nullable, aditivo) vía
`npx prisma migrate dev --name add_customer_tax_id`, sumalo al formulario de creación/edición
de cliente, y extendé la búsqueda de `customers-list.tsx` (buscá dónde se filtra por
`c.name.toLowerCase().includes(q)`, cerca de la lógica ya usada para buscar por teléfono) para
que también matchee contra `taxId` si está cargado.
```

**RESTRICCIONES ESTRICTAS:**
- Migración nullable, sin backfill ni formato/validación estricta de CUIT (no valides dígito
  verificador ni formato AR específico — es un campo de texto libre para buscar).

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/modules/customers/customer-validation.ts`,
`src/app/ui/customer-new-form.tsx`, `src/app/ui/customer-detail.tsx`, `src/app/ui/customers-list.tsx`.

---

### TAREA 138 · 🟠

**Descripción:** Límite de crédito configurable por cliente con alerta si lo supera.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Agregá `creditLimit Decimal? @db.Decimal(12,2)` (nullable, aditivo — sin límite si es null) al
modelo `Customer` vía `npx prisma migrate dev --name add_customer_credit_limit`. Sumá el campo
al formulario de edición de cliente. En el flujo de venta a crédito (revisá `pos.tsx`, donde se
selecciona `paymentType: "CREDIT"` y se elige el cliente), antes de confirmar la venta, si el
cliente tiene `creditLimit` configurado, calculá su deuda actual
(`Debt.remainingAmount` sumado para ese cliente) + el monto de la nueva venta, y si supera el
límite mostrá una advertencia (no bloqueante, un modal de confirmación tipo "Este cliente
superaría su límite de crédito de $X — ¿confirmar de todas formas?") antes de completar la
venta.
```

**RESTRICCIONES ESTRICTAS:**
- La advertencia NO debe bloquear la venta — es una confirmación que el cajero puede aceptar,
  no una restricción dura (el dueño puede querer vender igual en casos puntuales).
- Migración nullable — sin límite configurado, ningún cliente existente se ve afectado.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/modules/customers/customer-validation.ts`,
`src/app/ui/customer-detail.tsx`, `src/app/ui/pos.tsx`.

---

### TAREA 142 · 🟢

**Descripción:** Click en teléfono abre WhatsApp directamente (link wa.me automático).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/customers-list.tsx` y `src/app/ui/customer-detail.tsx`, donde se muestra el
teléfono del cliente como texto plano, convertilo en un link a
`https://wa.me/<solo-dígitos-del-teléfono>` (limpiá el número con un regex que quite espacios,
guiones y paréntesis, igual criterio que ya usa `shareSaleWhatsApp` en `sales-list.tsx` para
armar el link de WhatsApp de una venta — reusá esa misma lógica de limpieza de número si es
sencillo extraerla, o replicala si no). Abrí el link en una pestaña nueva
(`target="_blank" rel="noopener noreferrer"`) y agregá un ícono pequeño de WhatsApp o de
teléfono junto al número para indicar que es clickeable.
```

**RESTRICCIONES ESTRICTAS:**
- Si el cliente no tiene teléfono cargado, no muestres ningún link (mismo comportamiento actual
  de mostrar "Sin teléfono" o similar).
- No agregues librerías de iconos nuevas — usá lucide-react, ya instalado.

**Archivos afectados:** `src/app/ui/customers-list.tsx`, `src/app/ui/customer-detail.tsx`.

---

### TAREA 143 · 🟢

**Descripción:** Acción rápida "Registrar pago" desde la lista sin entrar al detalle — ya
implementado; falta que sea un botón directo, no un ítem dentro de un menú "...".

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: esta tarea ya está mayormente resuelta. En `src/app/ui/customers-list.tsx`, cada fila ya
tiene un menú "..." (kebab) con la opción "Registrar pago" (línea ~959-963) que abre el modal
de pago sin necesidad de entrar al detalle del cliente — técnicamente ya cumple "sin entrar al
detalle".

Lo que falta para que sea una acción verdaderamente rápida: agregá un botón visible directamente
en la fila (ícono `DollarSign` de lucide-react, ya usado en otras pantallas como
`debts-list.tsx`), fuera del menú "...", que llame al mismo `onPayment` que ya usa la opción del
menú — así el pago se registra con un solo click en vez de dos (abrir menú + click en la
opción). Dejá la opción "Registrar pago" también disponible en el menú "..." tal como está hoy,
no la borres.
```

**RESTRICCIONES ESTRICTAS:**
- No dupliques la lógica del modal de pago — reusá el mismo `onPayment`/estado ya existente.
- No quites la opción del menú "...", solo agregá el botón directo como atajo adicional.

**Archivos afectados:** `src/app/ui/customers-list.tsx`.

---

### TAREA 151 · 🟡

**Descripción:** Segmento (VIP / Regular / Nuevo / Inactivo) como badge de color en la lista —
ya implementado; hay una inconsistencia entre dos sistemas de segmentación paralelos que vale
la pena documentar y unificar visualmente.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: esta tarea ya está implementada en `src/app/ui/customers-list.tsx` —`SegmentBadge`,
`computeSegment` y un filtro por segmento ya existen y muestran exactamente
VIP/Regular/Nuevo/Inactivo con colores, calculado en el cliente a partir de `totalPurchases`.

PERO hay una inconsistencia real que no se resolvió antes: el modelo `Customer` tiene un campo
`segment` en la base de datos (`NINGUNO | NUEVO | RECURRENTE | VIP`, editable manualmente desde
`customer-detail.tsx`) que es un sistema DIFERENTE y en paralelo al `computeSegment` calculado
en `customers-list.tsx` (que ignora completamente el campo de BD y siempre recalcula desde cero
según compras). Esto significa que un dueño puede marcar manualmente a un cliente como "VIP" en
el detalle, y la lista seguirá mostrando "Regular" si sus compras no llegan al umbral automático
— confuso.

Para esta tarea: en `SegmentBadge`/`computeSegment` de `customers-list.tsx`, si el cliente tiene
un `segment` explícito distinto de `NINGUNO` cargado en la BD, priorizalo sobre el cálculo
automático (mapeando `RECURRENTE` → "Regular", `NUEVO` → "Nuevo", `VIP` → "VIP"); si es
`NINGUNO`, seguí usando el cálculo automático como hoy. Documentá este comportamiento
claramente en el reporte.
```

**RESTRICCIONES ESTRICTAS:**
- No elimines el campo `segment` manual de `customer-detail.tsx` ni el cálculo automático — la
  tarea es hacer que convivan de forma coherente, priorizando lo manual cuando existe.
- No cambies los colores ni el diseño visual del badge ya existente.

**Archivos afectados:** `src/app/ui/customers-list.tsx`.

---

## SECCIÓN: GASTOS

### TAREA 128 · 🟠

**Descripción:** Presupuesto mensual por categoría con semáforo vs. gasto real.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Las categorías de gasto (`Expense.category`) son texto libre, sin un modelo propio (confirmá
revisando `expense-validation.ts` y cómo se listan las categorías hoy en `expenses-list.tsx`).
Creá un modelo nuevo `ExpenseBudget` (tenantId, category String, monthlyLimit Decimal,
`@@unique([tenantId, category])`) vía migración.

En Configuración o en la propia sección de Gastos (donde tenga más sentido — revisá si hay ya
una sub-sección de "categorías" en `expenses-list.tsx`), agregá una pantalla simple para que el
OWNER configure un límite mensual por categoría. En `expenses-list.tsx`, en el gráfico/lista de
"Gastos por categoría" ya existente (de la Tarea 115 del lote anterior), agregá un indicador de
semáforo (verde <70% del presupuesto, amarillo 70-100%, rojo >100%) junto a cada categoría que
tenga un `monthlyLimit` configurado, comparando contra el gasto real del mes actual en esa
categoría.
```

**RESTRICCIONES ESTRICTAS:**
- Si una categoría no tiene presupuesto configurado, no muestres ningún semáforo para ella (no
  asumas un límite por defecto).
- No dupliques el cálculo de "gasto por categoría del mes" si ya existe en `RentabilidadTab` de
  `reports.tsx` (`expByCategory`) — podés reusar la misma lógica de agregación si es sencillo,
  o replicarla localmente en `expenses-list.tsx` si no.

**Archivos afectados:** `prisma/schema.prisma` (migración, modelo nuevo),
`src/app/api/expense-budgets/route.ts` (nuevo), `src/app/ui/expenses-list.tsx`.

---

### TAREA 129 · 🟡

**Descripción:** Adjuntar comprobante al gasto: foto o PDF del ticket / factura del proveedor.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

No hay almacenamiento de archivos en la nube en este proyecto — el único precedente es el
logo de `settings.tsx`, que ni siquiera persiste realmente (ver Tarea 147 de este mismo lote
para más detalle). Agregá `receiptUrl String?` (nullable, aditivo) al modelo `Expense` vía
`npx prisma migrate dev --name add_expense_receipt_url`.

En el formulario de creación/edición de gasto (revisá `expenses-list.tsx` para encontrar el
formulario, probablemente un modal), agregá un input de archivo (`accept="image/*,.pdf"`) que
convierta el archivo a base64 con `FileReader.readAsDataURL()` (límite 2MB, mismo criterio que
el logo) y lo guarde en `receiptUrl`. Si el gasto tiene comprobante adjunto, mostrá un ícono de
clip/documento en la tabla que al hacer click abra el comprobante en una pestaña nueva
(los data URLs `data:application/pdf;base64,...` y `data:image/...;base64,...` se pueden abrir
directo con `window.open` sin backend adicional).
```

**RESTRICCIONES ESTRICTAS:**
- Límite estricto de 2MB por archivo (base64 en una columna de Postgres no escala bien para
  archivos grandes) — validá el tamaño antes de convertir y mostrá un error claro si lo supera.
- Migración nullable, sin backfill.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/modules/expenses/expense-validation.ts`,
`src/app/ui/expenses-list.tsx`.

---

### TAREA 130 · 🔴

**Descripción:** Gastos recurrentes: registrar una vez y que se repita automáticamente cada mes.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Creá un modelo nuevo `RecurringExpense` (tenantId, category, amount Decimal, description
String?, dayOfMonth Int, active Boolean @default(true), lastGeneratedMonth String? — formato
"YYYY-MM" para saber si ya se generó el gasto de este mes) vía migración.

En `expenses-list.tsx`, al crear un gasto, agregá un checkbox "Repetir automáticamente cada
mes" que, si se marca, cree además una fila en `RecurringExpense` con el mismo monto/categoría/
descripción y el día del mes actual como `dayOfMonth`.

Creá un endpoint de cron nuevo `src/app/api/cron/generate-recurring-expenses/route.ts`, con el
mismo patrón de autenticación por `Authorization: Bearer <CRON_SECRET>` que ya usa
`src/app/api/cron/expire-quotes/route.ts`. Este cron debe correr diariamente: por cada
`RecurringExpense` activo cuyo `dayOfMonth` sea hoy y `lastGeneratedMonth` no sea el mes actual,
crear un `Expense` nuevo con esos datos y actualizar `lastGeneratedMonth`. Agregá la entrada al
cron en `vercel.json` (mismo formato que la de `expire-quotes`, ej. `"0 4 * * *"` para que no
coincida con el horario de otros crons existentes).
```

**RESTRICCIONES ESTRICTAS:**
- El cron debe ser idempotente: si corre dos veces el mismo día, no debe duplicar el gasto
  (por eso `lastGeneratedMonth` — verificalo antes de crear).
- Si `dayOfMonth` es mayor a la cantidad de días del mes actual (ej. 31 en febrero), generá el
  gasto el último día del mes en vez de saltearlo.

**Archivos afectados:** `prisma/schema.prisma` (migración, modelo nuevo), `vercel.json`,
`src/app/api/cron/generate-recurring-expenses/route.ts` (nuevo), `src/app/ui/expenses-list.tsx`.

---

### TAREA 131 · 🟡

**Descripción:** Categorías de gasto personalizables por el usuario.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Las categorías de gasto (`Expense.category`) ya son texto libre a nivel de modelo — cualquier
string es válido. Confirmá en `expenses-list.tsx` cómo se ofrecen las categorías al usuario hoy
al crear un gasto (¿un `<select>` con opciones fijas hardcodeadas, o un input libre?). Si es un
`<select>` con opciones fijas, esta tarea es real: creá un modelo nuevo `ExpenseCategory`
(tenantId, name String, `@@unique([tenantId, name])`) vía migración, con un CRUD simple
(agregar/eliminar categorías) accesible desde la propia pantalla de Gastos (un botón "Gestionar
categorías" que abra un modal de lista simple). El `<select>` del formulario de gasto pasa a
poblarse desde `GET /api/expense-categories` en vez de la lista fija.

Si en cambio ya es un input libre sin lista fija, esta tarea ya está resuelta — documentalo en
el reporte sin hacer cambios, salvo que agregues igual el modelo `ExpenseCategory` para que el
usuario tenga sugerencias/autocompletado de categorías ya usadas antes (mejora menor, opcional).
```

**RESTRICCIONES ESTRICTAS:**
- No elimines categorías que ya estén en uso por gastos existentes sin advertencia — si el
  usuario intenta borrar una categoría con gastos asociados, mostrá una advertencia (no hace
  falta bloquear el borrado, los gastos existentes conservan el string de todas formas).

**Archivos afectados:** `prisma/schema.prisma` (migración, modelo nuevo, solo si aplica),
`src/app/api/expense-categories/route.ts` (nuevo, solo si aplica), `src/app/ui/expenses-list.tsx`.

---

### TAREA 132 · 🟡

**Descripción:** Proveedor asociado al gasto para trazabilidad de compras.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Revisá si ya existe un modelo `Supplier` en `prisma/schema.prisma` (se usa como relación
opcional en `Product.supplierId`) — si existe, reusalo en vez de crear un modelo nuevo. Agregá
`supplierId String?` (nullable, aditivo) al modelo `Expense`, con la relación
`supplier Supplier? @relation(fields: [supplierId], references: [id])`, vía
`npx prisma migrate dev --name add_expense_supplier`.

En el formulario de gasto de `expenses-list.tsx`, agregá un selector opcional de proveedor
(mismo patrón de selector que ya se use para elegir proveedor en productos, si existe). Mostrá
el nombre del proveedor en la tabla de gastos como columna o texto secundario, y agregá un
filtro por proveedor si el listado de proveedores no es trivialmente grande (usá el mismo
criterio de UI que los demás filtros de esta pantalla, como categoría).
```

**RESTRICCIONES ESTRICTAS:**
- Reusá el modelo `Supplier` existente — no crees una tabla de proveedores paralela.
- Migración nullable, sin backfill.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/modules/expenses/expense-validation.ts`,
`src/app/ui/expenses-list.tsx`.

---

### TAREA 133 · 🟢

**Descripción:** Categorías con íconos visuales para reconocimiento instantáneo.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/expenses-list.tsx`, las categorías hoy se distinguen solo por color
(`CATEGORY_COLORS`/`categoryColor`/`categoryBadge`, calculados por hash del nombre). Agregá un
mapa de íconos de lucide-react para las categorías más comunes en un comercio (ej.: "Alquiler" →
`Home`, "Servicios"/"Luz"/"Agua" → `Zap`, "Sueldos" → `Users`, "Insumos"/"Mercadería" →
`Package`, "Transporte" → `Truck`, "Marketing" → `Megaphone`, "Impuestos" → `Receipt`, con un
ícono genérico `Tag` como fallback para categorías no mapeadas — revisá qué categorías existen
realmente en los gastos ya cargados antes de armar la lista, no inventes categorías que nadie
usa). Mostrá el ícono junto al badge de categoría en la tabla y en el gráfico de "Gastos por
categoría".
```

**RESTRICCIONES ESTRICTAS:**
- No agregues un campo nuevo de "ícono" editable por el usuario en esta tarea — es un mapeo
  fijo en código por nombre de categoría, con fallback genérico. Si la Tarea 131 (categorías
  personalizables) ya se implementó, el ícono fallback cubre las categorías custom del usuario.
- No cambies los colores existentes, solo agregá el ícono junto a ellos.

**Archivos afectados:** `src/app/ui/expenses-list.tsx`.

---

### TAREA 134 · 🟢

**Descripción:** Total del mes visible en el encabezado de la sección Gastos (no solo en
Reportes) — ya implementado.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: esta tarea ya está implementada. En `src/app/ui/expenses-list.tsx` ya existe una
`MetricCard` "Gastos del mes" (línea ~249) con el total del mes actual, visible apenas se entra
a la sección, no solo en Reportes.

Verificá que esa tarjeta esté efectivamente arriba de todo (antes de cualquier filtro o tabla,
sin necesidad de scroll) y que el cálculo de "mes actual" sea el mes calendario en curso (no un
período configurable). Si ya cumple ambas cosas, no hagas ningún cambio de código — documentá en
el reporte que la tarea ya estaba resuelta y por qué. Si encontrás que la tarjeta requiere
scroll para verse o está mal ubicada, movela arriba del todo.
```

**RESTRICCIONES ESTRICTAS:**
- Si la tarjeta ya cumple lo pedido, NO hagas cambios solo para "justificar" la tarea — reportá
  que ya está resuelta y pasá a la siguiente.

**Archivos afectados:** `src/app/ui/expenses-list.tsx` (solo si hace falta reubicar la tarjeta).

---

### TAREA 144 · 🟢

**Descripción:** Total del mes en grande al entrar a la sección Gastos — ya implementado, es
prácticamente duplicado de la Tarea 134.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: esta tarea es casi idéntica a la Tarea 134 de este mismo lote (ambas piden que el total
del mes se vea prominente en Gastos) y ya está implementada vía la `MetricCard` "Gastos del
mes" en `expenses-list.tsx`. Para diferenciar el aporte de esta tarea específica: aumentá el
tamaño de fuente del valor en esa tarjeta en particular (ej. de `text-2xl` a `text-3xl` o
`text-4xl`, revisá el tamaño real usado hoy en `MetricCard` antes de decidir cuánto agrandarlo)
para que sea claramente el número más grande de toda la pantalla al entrar a Gastos, sin
necesidad de interpretar ningún gráfico.
```

**RESTRICCIONES ESTRICTAS:**
- No dupliques la tarjeta "Gastos del mes" — es un ajuste de tamaño sobre la misma tarjeta que
  ya resolvió la Tarea 134, no una tarjeta nueva.
- Mantené la proporción/alineación con las otras `MetricCard` de la misma fila.

**Archivos afectados:** `src/app/ui/expenses-list.tsx`.

---

### TAREA 158 · 🟡

**Descripción:** Gastos de alto monto (> umbral configurable) con confirmación de dos pasos.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Agregá `highExpenseThreshold Decimal? @db.Decimal(12,2)` (nullable — sin umbral configurado, sin
confirmación extra) al modelo `StoreSettings` vía
`npx prisma migrate dev --name add_store_settings_high_expense_threshold`. Sumá un input para
configurarlo en Configuración (sección donde tenga sentido, revisá `NegocioPanel.tsx` o la
sección de Gastos si ya tiene alguna configuración propia de la Tarea 128 de este mismo lote).

En el formulario de creación de gasto de `expenses-list.tsx`, si el monto ingresado supera
`highExpenseThreshold` (y el tenant tiene uno configurado), mostrá un modal de confirmación
adicional antes de guardar ("Este gasto de $X supera el umbral configurado de $Y — ¿confirmar?")
en vez de guardar directo al enviar el formulario.
```

**RESTRICCIONES ESTRICTAS:**
- Si no hay `highExpenseThreshold` configurado (null), el comportamiento debe ser exactamente
  el actual — sin ningún paso de confirmación extra.
- Migración nullable, sin backfill ni valor por defecto forzado.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/modules/settings/settings-validation.ts`,
`src/app/ui/settings.tsx` (o donde corresponda configurar el umbral), `src/app/ui/expenses-list.tsx`.

---

## SECCIÓN: COTIZACIONES

### TAREA 123 · 🟠

**Descripción:** Recordatorio automático antes del vencimiento (notificación interna a 2 días).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ya existe infraestructura relacionada: el modelo `Quote` ya tiene `reminderSentAt DateTime?`
(hoy solo se setea cuando el usuario envía un recordatorio manual desde
`src/app/api/quotes/[id]/send-reminder/route.ts`), y ya hay un cron diario
(`src/app/api/cron/expire-quotes/route.ts`, corre a las 3am vía `vercel.json`) que expira
cotizaciones vencidas.

Creá un cron nuevo `src/app/api/cron/remind-expiring-quotes/route.ts` (mismo patrón de
autenticación `Authorization: Bearer <CRON_SECRET>` que `expire-quotes`), que busque
cotizaciones con `status` en `DRAFT`/`SENT`, `validUntil` entre ahora y 2 días desde ahora, y
`reminderSentAt` null, y para cada una: si tiene `customerEmail`, enviá el email con
`sendQuoteEmail` (mismo que ya usa el endpoint manual) y actualizá `reminderSentAt`. Si no tiene
email, simplemente marcá `reminderSentAt` para no reintentar (documentá esto — sin email no hay
forma de notificar automáticamente por este canal). Agregá la entrada correspondiente a
`vercel.json` con un horario que no coincida con el cron de expiración existente (ej.
`"0 9 * * *"`, 9am).

El nombre de la tarea dice "notificación interna" — además del email, considerá agregar una
notificación visible dentro de la propia app (revisá si existe algún mecanismo de notificaciones
internas ya armado, por ejemplo en el dashboard o en `src/app/ui/dashboard-summary.tsx`, antes
de construir uno nuevo desde cero solo para esto).
```

**RESTRICCIONES ESTRICTAS:**
- No dupliques `sendQuoteEmail` ni la lógica de `send-reminder/route.ts` — reusalos.
- El cron debe ser idempotente (no reenviar recordatorios ya enviados, por eso el filtro por
  `reminderSentAt: null`).

**Archivos afectados:** `prisma/schema.prisma` (sin cambios, el campo ya existe), `vercel.json`,
`src/app/api/cron/remind-expiring-quotes/route.ts` (nuevo).

---

### TAREA 140 · 🟡

**Descripción:** Envío por WhatsApp con link al PDF generado directamente.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

`src/app/ui/quote-pdf.tsx` + `src/app/api/quotes/[id]/pdf/route.tsx` ya generan el PDF de una
cotización bajo demanda (revisá si el endpoint requiere autenticación de sesión — si la
requiere, un link directo a ese PDF no funcionará para un destinatario externo por WhatsApp sin
sesión iniciada; en ese caso, generá el PDF en el momento y compartilo como texto descriptivo
con los datos de la cotización en el mensaje de WhatsApp, similar al patrón de
`shareSaleWhatsApp` en `sales-list.tsx`, en vez de un link directo al PDF protegido).

En `src/app/ui/quotes-list.tsx`, agregá un botón "Enviar por WhatsApp" en el detalle o en la fila
de cada cotización que arme un mensaje con folio, cliente, ítems, total y fecha de vencimiento
(mismo formato de texto que ya usan las demás pantallas de este proyecto para compartir por
WhatsApp) y abra `https://wa.me/<teléfono-del-cliente>?text=...` si el cliente tiene teléfono
cargado, o `https://wa.me/?text=...` si no.
```

**RESTRICCIONES ESTRICTAS:**
- Si el endpoint de PDF requiere sesión autenticada, no envíes un link roto por WhatsApp —
  preferí el mensaje de texto descriptivo, documentando la limitación en el reporte.
- No dupliques la lógica de limpieza de número de teléfono si ya existe compartida desde la
  Tarea 142 de este mismo lote.

**Archivos afectados:** `src/app/ui/quotes-list.tsx`.

---

### TAREA 141 · 🟢

**Descripción:** Notas y condiciones de pago visibles en el cuerpo del PDF — el campo de notas
ya existe y ya se muestra; falta agregar condiciones de pago como concepto separado.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: `Quote.notes` ya existe y ya se renderiza en `quote-pdf.tsx` (sección "Notas", cerca de la
línea 111). Lo que falta es "condiciones de pago" como un concepto separado y explícito (ej.
"50% al confirmar, 50% contra entrega", "Pago a 30 días", etc.), que hoy el usuario tendría que
escribir manualmente dentro del campo de notas genérico.

Agregá `paymentTerms String?` (nullable, aditivo) al modelo `Quote` vía
`npx prisma migrate dev --name add_quote_payment_terms`. Sumá un input de texto separado
"Condiciones de pago" en el formulario de creación/edición de cotización (`quotes-list.tsx`), y
mostralo en `quote-pdf.tsx` como una sección propia "Condiciones de pago", separada visualmente
de "Notas" (no lo mezcles dentro del mismo bloque de texto).
```

**RESTRICCIONES ESTRICTAS:**
- No elimines ni reutilices el campo `notes` existente — `paymentTerms` es un campo nuevo y
  distinto, con su propia sección en el PDF.
- Migración nullable, sin backfill.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/modules/quotes/quote-validation.ts`,
`src/app/ui/quotes-list.tsx`, `src/app/ui/quote-pdf.tsx`.

---

### TAREA 145 · 🟡

**Descripción:** Columna "Días restantes" en la lista de cotizaciones en lugar de solo la fecha.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/quotes-list.tsx`, la columna de vencimiento hoy solo muestra la fecha formateada
(`formatDate(quote.validUntil)`), coloreada en rojo si `isExpiringSoon` (menos de 24hs, función
existente cerca de la línea 97). Agregá el cálculo de días restantes
(`Math.ceil((new Date(validUntil).getTime() - Date.now()) / (1000*60*60*24))`) y mostralo junto
a la fecha, ej. "15/07/2026 (3 días)" o "Vencida" si ya pasó. Si la Tarea 156 de este mismo lote
ya ajustó el umbral de "por vencer" a 3 días, usá exactamente ese mismo umbral/función para
decidir el color, no un cálculo separado.
```

**RESTRICCIONES ESTRICTAS:**
- No reemplaces la fecha completa por solo el conteo de días — mostrá ambas (fecha + días
  restantes entre paréntesis), para no perder información.
- Reusá `isExpiringSoon` (ajustada o no por la Tarea 156) en vez de duplicar el umbral en dos
  lugares distintos del mismo archivo.

**Archivos afectados:** `src/app/ui/quotes-list.tsx`.

---

### TAREA 156 · 🟢

**Descripción:** Indicador urgente (rojo) si la cotización vence en menos de 3 días — hoy el
umbral es de 24 horas, no 3 días, y es solo texto en rojo, no un indicador destacado.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/quotes-list.tsx`, la función `isExpiringSoon` (línea ~97) usa un umbral de
24 horas (`24 * 60 * 60 * 1000`). Cambialo a 3 días (`3 * 24 * 60 * 60 * 1000`) para que
coincida con lo que pide esta tarea.

Además de colorear el texto de la fecha en rojo (comportamiento actual), agregá un indicador
más visible: un badge pequeño "¡Por vencer!" (fondo rojo claro, texto rojo oscuro, ej. mismas
clases que usan otros badges de alerta del proyecto como el de "Vencida" en deudas) junto a la
fecha quantas cotizaciones cumplan `isExpiringSoon`.
```

**RESTRICCIONES ESTRICTAS:**
- Verificá que cambiar el umbral no rompa ningún otro uso de `isExpiringSoon` en el mismo
  archivo (revisá todas las líneas donde se llama antes de cambiar el valor).
- Si la Tarea 145 de este mismo lote ya se implementó, asegurate de que ambas usen la misma
  función/umbral sin duplicar lógica.

**Archivos afectados:** `src/app/ui/quotes-list.tsx`.

---

### TAREA 157 · 🟠

**Descripción:** Preview del PDF en pantalla antes de descargar, sin abandonar la vista.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

`src/app/api/quotes/[id]/pdf/route.tsx` ya genera el PDF de una cotización bajo demanda. En
`src/app/ui/quotes-list.tsx`, donde hoy se descarga el PDF directamente (revisá cómo se dispara
la descarga actual — probablemente `window.location.href` o similar a un endpoint), agregá un
botón "Vista previa" que en vez de descargar, haga fetch del PDF como blob
(`fetch(url).then(r => r.blob())`), cree una URL local (`URL.createObjectURL(blob)`) y la
muestre en un modal con un `<iframe>` o `<embed type="application/pdf">` ocupando la mayor parte
de la pantalla, con un botón "Descargar" dentro del mismo modal para bajar el archivo si el
usuario lo confirma, y "Cerrar" para volver a la lista sin descargar nada. Acordate de llamar
`URL.revokeObjectURL` al cerrar el modal para no dejar memoria reservada.
```

**RESTRICCIONES ESTRICTAS:**
- No cambies el comportamiento del botón de descarga directa existente (si lo hay) — agregá la
  vista previa como una opción adicional, no como reemplazo.
- Liberá siempre la URL del blob (`revokeObjectURL`) al cerrar el modal o al desmontar el
  componente, para evitar memory leaks en sesiones largas.

**Archivos afectados:** `src/app/ui/quotes-list.tsx`.

---

## SECCIÓN: CAJA

### TAREA 146 · 🟡

**Descripción:** Indicador visible en el header de la app: "Caja abierta" con balance actual.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

`src/app/ui/dashboard-summary.tsx` ya hace fetch de la sesión de caja actual (tipo
`CashRegisterSession`, estado `session`, cerca de la línea 951) — reusá ese mismo patrón de
fetch (revisá a qué endpoint le pega exactamente) en vez de inventar uno nuevo.

En `src/app/ui/app-shell.tsx`, agregá un indicador pequeño en la barra lateral (cerca de
`SidebarUser`, o en un lugar visible de la parte superior del `<aside>`) que muestre "Caja
abierta" con un punto verde si hay una sesión de caja con `status: "OPEN"` para el tenant
actual, junto con el balance actual aproximado (`openingAmount` + entradas − salidas hasta el
momento, mismo cálculo que ya usa `cash-register-close.tsx` para `expectedAmount` — no hace
falta que sea exacto al centavo en tiempo real, un fetch al cargar la página alcanza, no hace
falta polling). Si no hay caja abierta, mostrá "Caja cerrada" con un punto gris/rojo, con un
link directo a `/cash-movements` para abrirla.
```

**RESTRICCIONES ESTRICTAS:**
- No implementes actualización en tiempo real (polling o websockets) — un fetch al cargar la
  página/navegar es suficiente para este indicador.
- No dupliques el cálculo de `expectedAmount` — reusá la misma fórmula ya usada en
  `cash-register-close.tsx` (apertura + entradas − salidas).

**Archivos afectados:** `src/app/ui/app-shell.tsx`.

---

## SECCIÓN: REPORTES

### TAREA 152 · 🟡

**Descripción:** Selector de período visual: calendario con rangos predefinidos.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El selector de período en `src/app/ui/reports.tsx` (botones "Hoy"/"7 días"/"Este mes"/
"Personalizado", con inputs `type="date"` nativos para el rango personalizado) ya cubre la
funcionalidad básica pero con inputs de fecha nativos poco visuales. Agregá rangos predefinidos
adicionales dentro de la opción "Personalizado" (un pequeño dropdown o fila de chips: "Ayer",
"Esta semana", "Mes pasado", "Este trimestre", "Este año") que al seleccionarse calculen y
seteen `customStart`/`customEnd` automáticamente, sin que el usuario tenga que elegir las fechas
a mano. Los inputs de fecha nativos existentes se mantienen para el caso de un rango
verdaderamente custom.
```

**RESTRICCIONES ESTRICTAS:**
- No reemplaces los inputs de fecha nativos existentes — agregá los atajos predefinidos como
  una opción adicional dentro del mismo selector de "Personalizado".
- No agregues una librería de date-picker nueva — calculá los rangos con `Date` nativo, mismo
  criterio que ya usan `getMonthRange`/`getPeriodRange` en el archivo.

**Archivos afectados:** `src/app/ui/reports.tsx`.

---

### TAREA 153 · 🟢

**Descripción:** Comparación de dos períodos completamente custom (fechas libres).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

La Tarea 110 de un lote anterior ya corrigió `getPreviousPeriodRange` para que el período de
comparación siga automáticamente al período seleccionado (hoy vs. ayer, semana vs. semana
anterior, etc.). Esta tarea pide algo distinto y más flexible: que el usuario pueda elegir
manualmente DOS rangos de fechas completamente libres e independientes (rango A y rango B) para
comparar entre sí, sin que B tenga que ser "el período anterior a A".

Agregá un modo nuevo "Comparar períodos" en el selector superior de `reports.tsx` (junto a
Hoy/7 días/Este mes/Personalizado) que muestre dos pares de inputs de fecha (rango A, rango B).
Al activarlo, mostrá en la pestaña "Resumen general" una vista comparativa simple (podés reusar
`GrowthSummaryPanel` como base, alimentándolo con métricas calculadas para el rango A vs. rango
B en lugar de mes actual vs. mes anterior).
```

**RESTRICCIONES ESTRICTAS:**
- No reemplaces el modo de comparación automática existente (de la Tarea 110) — este es un modo
  adicional y opcional, no un reemplazo.
- No dupliques el cálculo de métricas — extraé la lógica de agregación de `metrics` a una
  función reutilizable que reciba un rango de fechas como parámetro, si es razonable hacerlo sin
  reescribir demasiado el archivo.

**Archivos afectados:** `src/app/ui/reports.tsx`.

---

### TAREA 154 · 🟢

**Descripción:** Tooltips detallados en todos los gráficos al pasar el cursor.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Los gráficos SVG de `src/app/ui/reports.tsx` (`SalesEvolutionPanel`, `CategoryDonutPanel`,
gráfico de `ReporteMensualTab`, etc.) no tienen tooltips interactivos hoy — son SVG estáticos
sin manejo de hover por punto/segmento. Agregá tooltips simples con SVG + estado de React (sin
librerías de gráficos nuevas): un `<g>` invisible más grande superpuesto a cada punto/barra/
segmento que dispare `onMouseEnter`/`onMouseLeave` actualizando un estado local
`hoveredPoint`/`hoveredSegment`, y renderizá un pequeño rectángulo + texto flotante (posicionado
cerca del cursor o del punto) con el valor exacto y la fecha/categoría correspondiente. Empezá
por `SalesEvolutionPanel` y `CategoryDonutPanel` como los dos casos principales (mismo alcance
que ya tuvo la Tarea 108 de un lote anterior para exportar a PNG, sobre los mismos componentes).
```

**RESTRICCIONES ESTRICTAS:**
- No agregues librerías de gráficos nuevas (Chart.js, Recharts, etc.) — el proyecto usa SVG
  manual en todos lados, mantené la consistencia.
- Limitá el alcance a los dos gráficos mencionados; no hace falta cubrir cada gráfico de la
  página en esta tarea.

**Archivos afectados:** `src/app/ui/reports.tsx`.

---

## SECCIÓN: PRODUCTOS

### TAREA 155 · 🟡

**Descripción:** Subir foto de producto con drag-and-drop o captura desde cámara.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El modelo `Product` no tiene ningún campo de imagen hoy. Agregá `imageUrl String?` (nullable,
aditivo) vía `npx prisma migrate dev --name add_product_image_url`. Igual que en la Tarea 147
de este mismo lote (foto de perfil de usuario), no hay almacenamiento en la nube — convertí la
imagen a base64 con `FileReader.readAsDataURL()` (límite 2MB) y guardala directo en `imageUrl`.

En el formulario de creación/edición de producto (`src/app/ui/products-inventory.tsx`), agregá
una zona de drag-and-drop (eventos `onDragOver`/`onDrop` sobre un `<div>`, además del input de
archivo tradicional con click) para soltar una imagen, y un input adicional `capture="environment"`
en el `<input type="file" accept="image/*">` para permitir sacar una foto directo desde la
cámara en dispositivos móviles. Mostrá la imagen (o un ícono de producto genérico si no tiene)
en la lista de productos y en el buscador del POS.
```

**RESTRICCIONES ESTRICTAS:**
- Límite estricto de 2MB, mismo criterio que el logo y el avatar de usuario — validá antes de
  convertir a base64.
- Migración nullable, sin backfill — productos existentes sin imagen muestran el ícono genérico.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/modules/products/product-validation.ts`,
`src/app/ui/products-inventory.tsx`, `src/app/ui/pos.tsx`.

---

## PROTOCOLO DE REPORTE

Después de cada tarea, agregá una entrada en `TAREAS/REPORTE_DE_CAMBIOS.md` con este formato:

```
### TAREA XXX — [estado: ✅ Completada / ⚠️ Parcial / ❌ Bloqueada]
- Qué se hizo:
- Archivos modificados:
- Migraciones corridas (si aplica):
- Algo ya estaba implementado de otra forma / algo quedó pendiente:
- typecheck: OK / errores (detalle)
```

### Reglas del ciclo

- Este lote tiene 38 tareas (no 20 como los anteriores) — reportá cada una apenas se completa,
  no esperes a terminar todo el lote para actualizar el reporte.
- Al terminar TODAS las tareas de este lote (o cuando Diego pida revisar avance), hacé commit de
  los cambios con un mensaje descriptivo y **corré `git push origin main`** (puede fallar por
  falta de credenciales en este entorno — está bien, Diego lo sube manualmente desde su PC).
- Si una tarea requiere una migración de Prisma, avisalo explícitamente en el reporte — hay
  MUCHAS migraciones en este lote (121, 122 no, 124, 126, 127, 129, 130, 131 tal vez, 132, 135,
  137, 138, 139, 141, 142 no, 147, 148, 150, 155, 158) — Diego necesita correr
  `npx prisma migrate deploy` en producción después de este lote.
- Varias tareas de este lote ya están total o parcialmente implementadas (103-estilo de lotes
  anteriores, acá son: 134, 143, 144, 151) — documentá exactamente qué faltaba y qué se agregó,
  no reescribas lo que ya funciona.
- Las Tareas 134 y 144 son casi duplicadas entre sí (ambas piden "total del mes visible" en
  Gastos) — ya están anotadas para no chocar entre sí, pero prestá atención si al hacerlas se
  pisa el trabajo de una con la otra.
- Las Tareas 145 y 156 comparten la misma función `isExpiringSoon` en `quotes-list.tsx` —
  hacelas en el orden que prefieras, pero no dupliques el umbral en dos lugares del archivo.
- Las Tareas 142 y 140 comparten lógica de limpieza de número de teléfono para WhatsApp —
  resolvé una primero y reusá el resultado en la otra si es sencillo.
