# SOLVEN — Tareas de Producto · Lote 1 (001–020)

> **Instrucciones para Claude (sección Código / Code):**
> Diego da órdenes desde su iPhone a través de Claude en la sección Código.
> Cuando diga "ejecuta la tarea X": encontrá la tarea por número, ejecutá **únicamente** lo que
> indica su prompt, hacé commit de los cambios con mensaje descriptivo, y actualizá
> `TAREAS/REPORTE_DE_CAMBIOS.md` con el resultado. Diego hace `git push` desde su PC cuando puede.
> **Regla de oro: no modifiques lógica de negocio, BD ni archivos fuera de los indicados. Ante la duda, menos es más.**

---

## SECCIÓN: DASHBOARD

---

### TAREA 001 · 🔴 CRÍTICO
**Filtro de fecha global en el Dashboard**

**Descripción:** Agregar un selector de período en la parte superior del Dashboard que permita al usuario filtrar todos los KPIs y gráficos entre: Hoy, Esta Semana, Este Mes, y Personalizado (rango de fechas libre).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/dashboard-summary.tsx`:
1. Agregá un estado local `dateFilter` con opciones: "today" | "week" | "month" | "custom".
2. Creá un componente de selector de período en la parte superior del componente DashboardSummary,
   antes de los KPIs. Usá botones pill con estilo Tailwind consistente con el resto del archivo.
   Para "Personalizado" mostrá dos inputs de tipo date (desde / hasta).
3. Pasá el filtro seleccionado como parámetros a todas las llamadas fetch existentes que ya
   consultan datos (ventas, caja, gastos). Modificá las URLs de fetch para incluir
   ?from=YYYY-MM-DD&to=YYYY-MM-DD según la selección.
4. Actualizá los API routes correspondientes en `src/app/api/` para aceptar y aplicar esos
   parámetros de fecha en las queries Prisma (usá gte/lte en los campos de fecha).

RESTRICCIONES ESTRICTAS:
- No modifiques el schema de Prisma.
- No cambies el diseño general ni el layout del dashboard.
- No toques ningún archivo fuera de dashboard-summary.tsx y los api routes que ya consultan datos del dashboard.
- El color violeta #7c3aed debe mantenerse en los botones activos del selector.
- Ejecutá `npm run build` al final para verificar que compila sin errores de TypeScript.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`, `src/app/api/dashboard/*` o routes similares.

---

### TAREA 002 · 🔴 CRÍTICO
**KPIs clickeables que navegan a su sección**

**Descripción:** Cada tarjeta KPI del Dashboard (Ventas, Caja, Gastos, Deudas, Inventario) debe ser un link clickeable que lleve al usuario directamente a la sección correspondiente.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/dashboard-summary.tsx`:
1. Identificá todas las tarjetas de KPI que renderizan métricas principales.
2. Envolvé cada tarjeta con un componente `<Link href="...">` de Next.js apuntando a la
   sección correspondiente:
   - Ventas del día → /sales
   - Balance de caja → /cash-movements
   - Gastos del mes → /expenses
   - Deudas pendientes → /debts
   - Stock crítico → /inventory
3. Agregá un cursor-pointer y un hover sutil (hover:ring-2 hover:ring-violet-500/30) para
   indicar que son clickeables.
4. No cambies el tamaño, color ni contenido de las tarjetas.

RESTRICCIONES ESTRICTAS:
- Solo modificá dashboard-summary.tsx.
- No toques APIs, lógica de datos ni otros componentes.
- Ejecutá `npm run build` al final para verificar TypeScript.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`

---

### TAREA 003 · 🔴 CRÍTICO
**Widget de cotizaciones pendientes de confirmar**

**Descripción:** Agregar un widget en el Dashboard que muestre las cotizaciones que están pendientes de confirmación, con número de cotización, cliente y días para vencer.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. En `src/app/ui/dashboard-summary.tsx`, el tipo ExpiringQuote ya existe. Verificá si ya
   se está fetching y renderizando. Si no:
   a. Agregá un fetch a un endpoint de cotizaciones próximas a vencer o pendientes.
   b. Creá un widget debajo de los KPIs principales con el título "Cotizaciones pendientes".
   c. Mostrá hasta 5 cotizaciones con: número (COT-XXXX), nombre del cliente, monto en ARS
      formateado con formatARS(), y días restantes con color semáforo
      (verde >7 días, amarillo 3-7, rojo <3 días).
   d. Si no hay cotizaciones, mostrá un estado vacío con ícono y texto "Sin cotizaciones pendientes".
2. Si el widget ya existe pero está incompleto, mejoralo según los puntos anteriores.

RESTRICCIONES ESTRICTAS:
- No modifiques el schema de Prisma ni las tablas existentes.
- No toques el flujo de creación/edición de cotizaciones en /quotes.
- Solo UI del dashboard + el fetch necesario.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`, posible nuevo API route para cotizaciones pendientes.

---

### TAREA 004 · 🔴 CRÍTICO
**Alerta si la caja no fue cerrada al final del día**

**Descripción:** Mostrar un banner/alerta visible en el Dashboard cuando la caja esté abierta y ya sea de noche (después de las 20hs) o sea un día distinto al último cierre registrado.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/dashboard-summary.tsx`:
1. Verificá si ya existe lógica para detectar si la caja está abierta o cerrada.
2. Agregá la siguiente lógica de alerta:
   - Si la caja está abierta Y (la hora local es >= 20:00 OR el último cierre fue en un día
     anterior al día de hoy), mostrá un banner de alerta en la parte superior del Dashboard.
   - El banner debe ser de color amarillo/naranja, con ícono de advertencia, texto:
     "⚠️ La caja sigue abierta. Recordá cerrarla antes de terminar el día."
   - Incluí un link/botón "Ir a Caja" que navegue a /cash-movements.
3. Si la caja está cerrada correctamente, no mostrés el banner.

RESTRICCIONES ESTRICTAS:
- No modifiques el flujo de apertura/cierre de caja en cash-movements.
- Solo UI en dashboard-summary.tsx + lectura de estado existente.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`

---

### TAREA 005 · 🟠 ALTO
**Ranking de vendedores del día (top 3 por monto)**

**Descripción:** Agregar un widget en el Dashboard que muestre los 3 vendedores con mayor monto de ventas del día actual, con nombre, monto y cantidad de ventas.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Creá un API route en `src/app/api/dashboard/top-sellers/route.ts` que:
   - Consulte las ventas del día actual agrupadas por sellerCode o userId.
   - Retorne los top 3 con: nombre del vendedor, totalAmount sumado, cantidadDeVentas.
   - Filtrá ventas con status activo (no devueltas).
2. En `src/app/ui/dashboard-summary.tsx`, agregá un widget "Top vendedores hoy" que:
   - Fetchee el nuevo endpoint.
   - Muestre una lista con posición (🥇🥈🥉), nombre y monto en ARS.
   - Si hay menos de 3 o no hay ventas hoy, mostrá los que haya o un estado vacío amigable.

RESTRICCIONES ESTRICTAS:
- No modifiques el schema de Prisma.
- No toques la lógica de ventas en /sales ni en el POS.
- Solo lectura de datos existentes, nuevo API route y UI en el dashboard.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`, `src/app/api/dashboard/top-sellers/route.ts` (nuevo)

---

### TAREA 006 · 🟠 ALTO
**Accesos rápidos al POS y Nuevo Gasto desde el Dashboard**

**Descripción:** Agregar botones de acción rápida en el Dashboard para ir al POS y para registrar un nuevo gasto, visibles sin necesidad de scroll.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/dashboard-summary.tsx`:
1. Agregá una fila de botones de "Acciones rápidas" en la parte superior del dashboard,
   debajo del header y el selector de fechas (si ya existe de la tarea anterior).
2. Los botones deben ser:
   - "🛒 Ir al POS" → link a /pos
   - "➕ Nuevo gasto" → link a /expenses (o al formulario de nuevo gasto si existe)
3. Estilo: botones grandes, prominentes, con fondo violeta (#7c3aed) para el POS y
   fondo gris oscuro para Nuevo Gasto. Responsive: en mobile ocupan ancho completo.

RESTRICCIONES ESTRICTAS:
- Solo modificá dashboard-summary.tsx.
- No toques la lógica de gastos ni del POS.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`

---

### TAREA 007 · 🟠 ALTO
**Gráfico combinado: ingresos vs. gastos en el mismo chart**

**Descripción:** El gráfico de 7 días del Dashboard debe mostrar dos líneas/barras: una para ventas y otra para gastos del mismo período, permitiendo comparación visual directa.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/dashboard-summary.tsx`:
1. Identificá el gráfico de ventas de los últimos 7 días que ya existe.
2. Modificá el fetch de datos del gráfico para que también traiga los gastos por día del
   mismo período.
3. Actualizá el componente de gráfico para renderizar DOS datasets:
   - Dataset 1: Ventas por día (color violeta #7c3aed o similar).
   - Dataset 2: Gastos por día (color naranja/rojo, ej: #f97316).
4. Agregá una leyenda simple debajo o arriba del gráfico: "● Ventas ● Gastos".
5. El eje Y debe ajustarse automáticamente al máximo de ambos datasets.

RESTRICCIONES ESTRICTAS:
- No modifiques la librería de gráficos ni su configuración global.
- No cambies el tamaño ni posición del widget del gráfico.
- No toques APIs fuera de las del dashboard.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`, posible ajuste en el API route del gráfico.

---

### TAREA 008 · 🟢 BAJO
**Saludo personalizado con nombre del usuario**

**Descripción:** Mostrar un saludo con el nombre del usuario autenticado y la fecha/hora actuales en la parte superior del Dashboard.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/dashboard-summary.tsx`:
1. Verificá si ya existe acceso al usuario autenticado (session, context o prop).
   Si no existe, usá useSession() de next-auth o el mecanismo de auth que ya use el proyecto.
2. Agregá en la parte superior del componente un saludo dinámico:
   - Mañana (6-12hs): "Buenos días, {nombre} ☀️"
   - Tarde (12-20hs): "Buenas tardes, {nombre} 🌤️"
   - Noche (20-6hs): "Buenas noches, {nombre} 🌙"
3. Mostrá debajo del saludo la fecha actual formateada en español:
   ej: "Domingo 28 de junio de 2026".
4. Tipografía: texto grande para el saludo, más pequeño y gris para la fecha.

RESTRICCIONES ESTRICTAS:
- No modifiques el sistema de autenticación.
- Solo UI en dashboard-summary.tsx.
- Si no podés acceder al nombre del usuario de forma segura, usá "hola" como fallback.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`

---

### TAREA 009 · 🟠 ALTO · UX
**Animación de conteo (rollup) en los números grandes del Dashboard**

**Descripción:** Los KPIs numéricos del Dashboard deben animarse con un efecto de conteo progresivo (cuenta desde 0 hasta el valor real) al cargar la página.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/dashboard-summary.tsx`:
1. Creá un custom hook `useCountUp(target: number, duration: number = 800)` dentro del
   mismo archivo que anime un número desde 0 hasta `target` usando requestAnimationFrame.
2. Aplicá el hook a los valores numéricos principales de los KPIs (ventas del día,
   balance de caja, total gastos, total deudas).
3. La animación debe ejecutarse solo la primera vez que el componente monta y los datos
   están disponibles (no en cada re-render).
4. Los valores monetarios deben animarse como número entero y luego formatearse con
   formatARS() al final de la animación.

RESTRICCIONES ESTRICTAS:
- No modifiques la lógica de fetch ni los datos.
- No uses librerías externas — solo requestAnimationFrame nativo.
- No toques nada fuera de dashboard-summary.tsx.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`

---

### TAREA 010 · 🟠 ALTO · UX
**Tooltip explicativo en cada KPI del Dashboard**

**Descripción:** Cada tarjeta KPI debe mostrar un ícono de info (ⓘ) que al hacer hover muestre un tooltip explicando cómo se calcula ese valor.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/dashboard-summary.tsx`:
1. Creá un componente `KpiTooltip({ text }: { text: string })` simple dentro del archivo:
   - Un ícono ⓘ pequeño (text-gray-400) posicionado en la esquina superior derecha de la tarjeta.
   - Al hacer hover, muestra un tooltip con fondo oscuro y el texto explicativo.
   - Usá CSS puro con Tailwind (group/hover) — sin librerías externas.
2. Aplicá el tooltip a cada KPI con el texto apropiado:
   - Ventas del día: "Total de ventas completadas en el período seleccionado."
   - Balance de caja: "Saldo actual de la caja: ingresos menos egresos registrados."
   - Gastos del mes: "Total de gastos registrados en el período seleccionado."
   - Deudas pendientes: "Suma de deudas activas de clientes aún no saldadas."
   - Stock crítico: "Productos con stock igual o por debajo del mínimo configurado."

RESTRICCIONES ESTRICTAS:
- Solo modificá dashboard-summary.tsx.
- No uses librerías de tooltip externas.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`

---

### TAREA 011 · 🟠 ALTO · UX
**Estado vacío amigable en el Dashboard**

**Descripción:** Cuando no hay ventas en el período seleccionado, el Dashboard no debe mostrar ceros sino un estado vacío con mensaje amigable y llamada a la acción.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/dashboard-summary.tsx`:
1. Detectá cuando todos los valores principales (ventas, movimientos de caja) sean 0 o
   estén vacíos para el período seleccionado.
2. En lugar de mostrar "$0" en todos los KPIs, mostrá un estado vacío centrado:
   - Ícono grande (ShoppingBag o similar de lucide-react, ya importado).
   - Título: "Sin actividad en este período"
   - Subtítulo: "Registrá tu primera venta del día desde el POS."
   - Botón: "Ir al POS" → /pos (violeta #7c3aed).
3. Si hay al menos una venta o movimiento, mostrá el dashboard normal.

RESTRICCIONES ESTRICTAS:
- Solo modificá dashboard-summary.tsx.
- No cambies la lógica de fetch ni los endpoints.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`

---

### TAREA 012 · 🟡 MEDIO · UX
**Días abreviados en español en el gráfico de 7 días**

**Descripción:** El gráfico de barras de 7 días debe mostrar las etiquetas del eje X con el nombre abreviado del día en español (Lun, Mar, Mié, Jue, Vie, Sáb, Dom) en lugar de fechas numéricas.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/dashboard-summary.tsx`:
1. Localizá el gráfico de los últimos 7 días.
2. Modificá las etiquetas del eje X para que muestren el nombre abreviado del día en
   español. Usá este mapa:
   const DIAS = ['Dom', 'Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb'];
   const label = DIAS[new Date(fecha).getDay()];
3. Si el día es hoy, la etiqueta debe ser "Hoy" en lugar del nombre del día.
4. No cambies nada más del gráfico.

RESTRICCIONES ESTRICTAS:
- Solo modificá las etiquetas del gráfico en dashboard-summary.tsx.
- No cambies colores, tamaños ni lógica de datos.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/dashboard-summary.tsx`

---

## SECCIÓN: POS — PUNTO DE VENTA

---

### TAREA 013 · 🔴 CRÍTICO
**Descuento manual por ítem en el POS**

**Descripción:** En el carrito del POS, cada ítem debe tener la opción de aplicar un descuento manual en $ o %, sin necesidad de crear una promoción previa.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/pos.tsx` (archivo grande, ~2929 líneas):
1. Buscá la estructura de datos del ítem en el carrito (CartItem o similar).
2. Agregá los campos `discount: number` y `discountType: "percent" | "fixed"` al tipo
   del ítem de carrito. Valor por defecto: 0 y "percent".
3. En la fila de cada ítem del carrito, agregá un botón pequeño "% desc" que abra
   un mini-popover inline con:
   - Toggle $ / % para elegir tipo de descuento.
   - Input numérico para el valor.
   - Botón Aplicar.
4. El precio final del ítem debe calcularse: precio × cantidad × (1 - descuento/100)
   para tipo "percent", o precio × cantidad - descuento para tipo "fixed".
5. El descuento debe reflejarse en el total del carrito.
6. Al finalizar la venta, el descuento debe incluirse en los datos enviados al API.

RESTRICCIONES ESTRICTAS:
- No modifiques el API de ventas ni el schema de Prisma en esta tarea.
- No toques el flujo de pago ni el modal de confirmación de venta.
- Solo UI del carrito y lógica de cálculo local.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/pos.tsx`

---

### TAREA 014 · 🔴 CRÍTICO
**Descuento global sobre el total del carrito**

**Descripción:** Agregar un campo en el resumen del carrito que permita aplicar un descuento global al total de la compra (en $ o %).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/pos.tsx`:
1. Buscá el panel de resumen/totales del carrito (donde se muestra el subtotal y total).
2. Agregá debajo del subtotal un campo "Descuento global" con:
   - Toggle para elegir tipo: $ o %.
   - Input numérico.
   - El total final debe actualizarse en tiempo real al escribir el descuento.
3. Asegurate de que el descuento global se aplique DESPUÉS de los descuentos por ítem
   (si los hay de la tarea anterior), es decir: descuento global sobre el subtotal ya
   descontado.
4. Incluí el descuento global en los datos que se envían al API al confirmar la venta.

RESTRICCIONES ESTRICTAS:
- No modifiques el API de ventas ni el schema de Prisma.
- No toques el flujo de pago ni otros componentes.
- Solo el panel de resumen en pos.tsx.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/pos.tsx`

---

### TAREA 015 · 🔴 CRÍTICO
**Atajos de teclado en el POS**

**Descripción:** Implementar atajos de teclado en el POS: Enter para confirmar agregar ítem, Escape para cerrar modales, F2 para iniciar una nueva venta.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/pos.tsx`:
1. Agregá un useEffect que registre un keydown listener global. Removelo en el cleanup.
2. Implementá los siguientes atajos:
   - Enter: si el buscador tiene un producto seleccionado o un resultado único, agregarlo
     al carrito. Si ya hay focus en el input de cantidad, confirmar.
   - Escape: cerrar cualquier modal o popover abierto (usá el estado de cada uno para
     cerrarlos).
   - F2: limpiar el carrito e iniciar una nueva venta (equivalente al botón "Nueva venta"
     si existe, o al estado inicial del carrito).
3. No activés los atajos cuando el usuario esté escribiendo en un input de texto
   (verificá que el target no sea un input/textarea antes de ejecutar).

RESTRICCIONES ESTRICTAS:
- No modifiques el flujo de pago ni la lógica de ventas.
- No toques otros archivos.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/pos.tsx`

---

### TAREA 016 · 🔴 CRÍTICO
**Grid de los 12 productos más vendidos en el POS**

**Descripción:** Agregar un grid visual con los 12 productos más vendidos del negocio que permita agregarlos al carrito con un solo clic, sin necesidad de buscarlos.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Creá un API route `src/app/api/pos/top-products/route.ts` que:
   - Consulte los 12 productos más vendidos (por cantidad de unidades en ventas) del
     último mes o todos los tiempos, ordenados de mayor a menor.
   - Retorne: id, nombre, salePrice, imageUrl (si existe), stock.
   - Excluí productos con stock 0 o inactivos.
2. En `src/app/ui/pos.tsx`, debajo del buscador o en un panel lateral, agregá un grid
   de 3×4 (12 tarjetas) con cada producto:
   - Imagen del producto o ícono placeholder (Package de lucide).
   - Nombre truncado a 2 líneas.
   - Precio formateado con formatARS().
   - Al hacer clic, agregarlo directamente al carrito con cantidad 1.
3. El grid debe tener un título "Más vendidos" y ser colapsable.

RESTRICCIONES ESTRICTAS:
- No modifiques el schema de Prisma.
- No toques el flujo de pago ni búsqueda existente.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/pos.tsx`, `src/app/api/pos/top-products/route.ts` (nuevo)

---

### TAREA 017 · 🟠 ALTO
**Edición directa de cantidad en el carrito sin abrir modal**

**Descripción:** La cantidad de cada ítem en el carrito debe ser editable directamente haciendo clic sobre el número, sin necesidad de abrir un modal o popup.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/pos.tsx`:
1. Buscá cómo se renderiza actualmente la cantidad de cada ítem del carrito.
2. Reemplazá el elemento estático o los botones +/- por un input numérico inline:
   - Ancho fijo pequeño (ej: w-16), centrado, sin bordes visibles en estado normal.
   - Al hacer foco, mostrar borde violeta (ring-violet-500).
   - Al perder foco (onBlur) o al presionar Enter, confirmar el valor.
   - Si el valor es 0 o vacío, eliminar el ítem del carrito.
   - Si el valor supera el stock disponible, mostrar el campo en rojo y no actualizar.
3. Mantené los botones + y - si ya existen — el input es una alternativa adicional.

RESTRICCIONES ESTRICTAS:
- No modifiques el API de ventas.
- Solo el renderizado del carrito en pos.tsx.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/pos.tsx`

---

### TAREA 018 · 🟠 ALTO
**Carrito suspendido: pausar una venta y abrir otra en paralelo**

**Descripción:** Permitir al usuario pausar la venta actual (guardar el carrito en memoria) y comenzar una nueva, para luego retomar la suspendida.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/pos.tsx`:
1. Agregá un estado `suspendedCarts: CartItem[][]` (array de carritos guardados).
2. Agregá un botón "⏸ Suspender" en la barra de acciones del carrito que:
   - Guarda el carrito actual en suspendedCarts.
   - Limpia el carrito activo para comenzar una nueva venta.
3. Si hay carritos suspendidos, mostrar un badge con el número de carritos pausados.
4. Al hacer clic en el badge/botón de carritos suspendidos, mostrar una lista simple
   con los carritos guardados (N ítems · $TOTAL). Al seleccionar uno, reemplaza el
   carrito activo con ese y lo elimina de los suspendidos.
5. Limitá a máximo 3 carritos suspendidos simultáneos.
6. Los carritos suspendidos viven solo en memoria (useState) — no persistir en DB.

RESTRICCIONES ESTRICTAS:
- No modifiques APIs ni el schema.
- Solo lógica de estado y UI en pos.tsx.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/pos.tsx`

---

### TAREA 019 · 🟠 ALTO
**Historial de la última venta visible en el POS**

**Descripción:** Mostrar en el POS un panel colapsable con el resumen de la última venta realizada (folio, ítems, total, método de pago) sin necesitar ir a /sales.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/pos.tsx`:
1. Después de completar una venta exitosamente, guardá el resumen en un estado local
   `lastSale` con: folio, ítems (nombre, cantidad, precio), total, método de pago.
2. Mostrá un panel colapsable "Última venta" en la parte inferior o lateral del POS con:
   - Número de folio (copiable al portapapeles con un clic).
   - Lista de ítems con cantidad × nombre = subtotal.
   - Total en ARS grande.
   - Método(s) de pago.
   - Botón "Ver detalle" → link a /sales/{id}.
3. El panel aparece automáticamente expandido después de cada venta y se puede colapsar.
4. Al iniciar una nueva venta, el panel se colapsa automáticamente (no desaparece).

RESTRICCIONES ESTRICTAS:
- No modifiques el API de ventas.
- Solo estado local y UI en pos.tsx.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/pos.tsx`

---

### TAREA 020 · 🟠 ALTO
**Campo de cantidad fraccionada por ítem en el POS**

**Descripción:** Permitir ingresar cantidades decimales en el carrito del POS para productos vendidos a granel (ej: 1.5 kg, 0.75 m).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/pos.tsx`:
1. Identificá el campo de cantidad al agregar un producto al carrito.
2. Modificá el input de cantidad para que acepte números decimales (step="0.001",
   type="number"). Actualmente puede estar limitado a enteros.
3. En el tipo CartItem, si la cantidad es `number`, asegurate de que no se aplique
   Math.floor() ni parseInt() al valor.
4. En el cálculo del subtotal del ítem, el resultado debe ser quantity × salePrice
   manteniendo los decimales hasta el total final, que se redondea a 2 decimales.
5. En el display del carrito, mostrá la cantidad con hasta 3 decimales si tiene parte
   decimal (ej: "1.500 kg") o sin decimales si es entero (ej: "2").

RESTRICCIONES ESTRICTAS:
- No modifiques el schema de Prisma ni los API routes.
- No toques el flujo de pago.
- Solo el campo de cantidad y los cálculos en pos.tsx.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/pos.tsx`

---

## PROTOCOLO DE REPORTE

Después de ejecutar cada tarea, el agente debe actualizar `TAREAS/REPORTE_DE_CAMBIOS.md`
con la siguiente estructura:

```
## Tarea XXX — [Nombre] — [fecha hora]
**Estado:** ✅ Completada | ⚠️ Parcial | ❌ Error
**Archivos modificados:** lista de archivos
**Cambios realizados:** descripción breve
**Notas:** problemas encontrados, decisiones tomadas, pendientes si los hay
---
```
