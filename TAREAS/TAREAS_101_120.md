# TAREAS — Lote 6 (101–120)

> Instrucciones para el agente local (Claude Code / VS Code):
> Ejecutá las tareas en orden. Después de cada tarea, agregá una entrada al final de
> `TAREAS/REPORTE_DE_CAMBIOS.md` describiendo qué se hizo, qué archivos se tocaron y si quedó
> algo pendiente o algo que ya estaba implementado de otra forma a la esperada.
>
> Regla de oro: no modifiques lógica de negocio, BD ni archivos fuera de los indicados. Ante la duda, menos es más.
>
> Antes de tocar código, correr `npx tsc --noEmit` para confirmar que partís de un estado limpio,
> y volver a correrlo antes de reportar cada tarea como completada.

---

## SECCIÓN: CONFIGURACIÓN

### TAREA 101 · 🟡

**Descripción:** Wizard de configuración inicial guiado — tour de 5 pasos al registrarse.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Construí un wizard de bienvenida de 5 pasos que se muestre una única vez a los tenants
recién registrados (primer login), guiándolos por: 1) datos del negocio (nombre, rubro),
2) primer producto o servicio, 3) forma de pago preferida, 4) invitar a un usuario (opcional,
se puede saltar), 5) resumen y botón "Empezar a vender".

Implementación sugerida:
- Nuevo componente `src/app/ui/onboarding-wizard.tsx` con estado local de paso actual,
  barra de progreso (1/5 … 5/5) y botones Atrás/Siguiente/Saltar.
- Persistí si el tenant ya completó el wizard con un campo nuevo `onboardingCompletedAt DateTime?`
  en el modelo `Tenant` (revisá `prisma/schema.prisma` — es aditivo y nullable, no rompe nada
  existente). Corré `npx prisma migrate dev --name add_tenant_onboarding_completed_at`.
- El wizard se muestra en el layout principal (revisá cómo se monta el shell de la app,
  probablemente en `src/app/layout.tsx` o el componente que envuelve el dashboard) solo si
  `onboardingCompletedAt` es null, y al terminar (o saltar) llama a un nuevo endpoint
  `PATCH /api/tenants/onboarding` que setea `onboardingCompletedAt: new Date()`.
- Los pasos 1-3 pueden reusar los endpoints ya existentes (`/api/tenants` para nombre de negocio,
  `/api/products` o `/api/services` para el primer ítem) — no dupliques lógica de creación.
```

**RESTRICCIONES ESTRICTAS:**
- No toques el flujo de login/registro existente, solo agregá el wizard como overlay posterior.
- El wizard debe poder cerrarse/saltarse en cualquier paso sin bloquear el uso de la app.
- Campo nuevo en `Tenant` debe ser nullable — no marcar `onboardingCompletedAt` como completado
  retroactivamente para tenants existentes (dejalo en null, así lo ven una vez).

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/app/ui/onboarding-wizard.tsx` (nuevo),
`src/app/api/tenants/onboarding/route.ts` (nuevo), layout principal donde se monta el shell.

---

### TAREA 102 · 🟢

**Descripción:** Indicador de "% configuración completa" para motivar llenar todos los campos.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En la sección "Mi Negocio" de Configuración (`src/app/settings/components/NegocioPanel.tsx`),
agregá una barra de progreso que muestre qué porcentaje de los campos recomendados del negocio
están completos: nombre, teléfono, email, dirección, CUIT/taxId, logoUrl (de Tarea 095),
receiptFooterMessage y receiptThankYouMessage (de Tarea 095).

Calculá el % en el cliente a partir de los datos que ya trae `StoreSettings` (revisá cómo
`NegocioPanel.tsx` obtiene y renderiza estos campos actualmente — probablemente ya hace un
fetch a `/api/settings` o similar). Mostrá una barra visual (0-100%) con un mensaje como
"Configuración completa: 6/8 campos" arriba del formulario, y un link o resaltado sutil en
los campos que faltan (podés simplemente listar los nombres de los campos vacíos debajo de
la barra, sin rediseñar el formulario entero).
```

**RESTRICCIONES ESTRICTAS:**
- Es un indicador puramente informativo/visual — no bloquees el guardado ni agregues
  validaciones obligatorias nuevas.
- No modifiques el schema de Prisma ni los endpoints de settings, solo la lectura de datos
  ya disponibles en el cliente.

**Archivos afectados:** `src/app/settings/components/NegocioPanel.tsx`.

---

### TAREA 111 · 🟡

**Descripción:** Email de notificaciones para alertas del sistema (caja, stock crítico).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ya existe infraestructura de email en `src/lib/email.ts` (usa Resend, con funciones como
`sendWelcomeEmail` y `sendTrialEndingEmail` que siguen el mismo patrón: función async,
`getResend()` interno, HTML envuelto con `wrap()`). Sumá dos funciones nuevas siguiendo
exactamente ese mismo patrón:

1. `sendLowStockAlertEmail(to: string, businessName: string, products: { name: string; stock: number }[])`
   — email con la lista de productos en stock crítico o agotados.
2. `sendCashRegisterDifferenceAlertEmail(to: string, businessName: string, difference: number)`
   — email de aviso cuando un cierre de caja (`CashRegisterSession`) cierra con una diferencia
   distinta de cero (usá el mismo umbral que ya usa `cash-register-close.tsx`:
   `Math.abs(difference) >= 0.005`).

Enganchá el envío:
- Para stock crítico: en el endpoint donde se calcula `lowStockProductsCount`
  (`src/modules/dashboard/dashboard-summary.ts`) NO agregues el envío ahí (es de solo lectura
  y se llama muy seguido); en cambio, agregá el envío en el endpoint de cierre de caja
  (`src/app/api/cash-register/close/route.ts` o el que corresponda — revisá la ruta real)
  para la alerta de diferencia, y para stock crítico agregá el envío en el endpoint que
  registra un movimiento de inventario que deja el stock por debajo del mínimo
  (revisá `src/modules/inventory` o donde se actualiza `stock` en ventas).
- El destinatario es el email del `User` con rol OWNER del tenant (`prisma.user.findFirst({ where: { tenantId, role: "OWNER" } })`).
- Agregá un toggle en Configuración (sección Notificaciones, revisá `settings.tsx` categoría
  `notificaciones`) para activar/desactivar estas alertas por email — si no hay campo para esto
  en `StoreSettings`, agregá `lowStockEmailAlerts Boolean @default(false)` y
  `cashDifferenceEmailAlerts Boolean @default(false)` vía migración aditiva.
```

**RESTRICCIONES ESTRICTAS:**
- Todo envío de email debe estar detrás del toggle correspondiente (default `false` — no
  empezar a mandar emails a tenants existentes sin que lo activen).
- No modifiques el contenido ni el remitente de los emails ya existentes en `email.ts`.
- Si `RESEND_API_KEY` no está configurada, las funciones ya devuelven silenciosamente (patrón
  existente) — no cambies ese comportamiento.

**Archivos afectados:** `src/lib/email.ts`, `prisma/schema.prisma` (migración), endpoint de
cierre de caja, endpoint de movimiento de inventario relevante, `src/app/ui/settings.tsx`.

---

## SECCIÓN: REPORTES

### TAREA 103 · 🟢

**Descripción:** Reporte de rentabilidad por producto: margen unitario × unidades vendidas
(ya existe la tabla base — falta poder ordenarla).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: esta tarea YA está mayormente implementada. En `src/app/ui/reports.tsx`, dentro de
`RentabilidadTab` (buscá `productProfitability` y la tabla "Rentabilidad por producto",
cerca de la línea 2362), ya se calcula y muestra ingresos/costo/ganancia/margen% por producto,
ordenado por ganancia descendente.

Lo que falta es hacer la tabla ordenable: agregá headers clicables (Unidades, Ingresos,
Costo, Ganancia, Margen %) que alternen entre ascendente/descendente al hacer click, con un
ícono de flecha (▲/▼) en la columna activa. Guardá el criterio de orden en un estado local
(`sortColumn`, `sortDirection`) dentro de `RentabilidadTab` y aplicá el sort sobre
`productProfitability` antes de mapear las filas.
```

**RESTRICCIONES ESTRICTAS:**
- No dupliques la tabla ni el cálculo de `productProfitability` — es un agregado de UI sobre
  el array ya calculado.
- No cambies el layout general de la pestaña Rentabilidad ni las otras tarjetas.

**Archivos afectados:** `src/app/ui/reports.tsx` (función `RentabilidadTab`).

---

### TAREA 104 · 🟠

**Descripción:** Reporte de vendedores: ventas por empleado con ranking y totales.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/reports.tsx` no existe todavía una pestaña de "Vendedores". Agregala:

1. Sumá `"Vendedores"` al array `TABS` y al tipo `Tab` (después de "Servicios", antes de
   "Clientes" es un buen lugar).
2. Creá una nueva función `VendedoresTab({ sales }: { sales: SaleRecord[] })` que agrupe las
   ventas por `sellerCode` (el campo ya existe en `SaleRecord`/`Sale`, y ya se usa como filtro
   en `src/app/ui/sales-list.tsx` — seguí el mismo patrón para extraer los códigos únicos).
   Mostrá una tabla con: código de vendedor, cantidad de ventas, monto total vendido, ticket
   promedio, ordenada de mayor a menor monto total. Arriba de la tabla, destacá el top 3 con
   tarjetas tipo podio (1°, 2°, 3°) similar a como se hace en `TopProductsPanel`.
3. Conectá la pestaña en el `switch`/cadena de `if` del render principal de `Reports()`.

Si una venta no tiene `sellerCode` (puede ser null), agrupala bajo la etiqueta "Sin asignar".
```

**RESTRICCIONES ESTRICTAS:**
- No modifiques el modelo `Sale` ni el campo `sellerCode` — es de solo lectura para este reporte.
- Reusá el helper `formatMoney` y los componentes de tarjeta existentes; no traigas librerías
  nuevas de gráficos.

**Archivos afectados:** `src/app/ui/reports.tsx`.

---

### TAREA 105 · 🟡

**Descripción:** Reporte de gastos vs. ingresos (P&L simplificado mensual) — la pestaña
"Reporte mensual" ya cubre esto; falta activar la comparación entre períodos.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: esta tarea ya está mayormente cubierta por la pestaña "Reporte mensual" en
`src/app/ui/reports.tsx` (función `ReporteMensualTab`), que ya muestra ventas, gastos,
ganancia bruta/neta y margen, comparado contra el mes anterior en cada tarjeta.

Lo que falta: el botón "Comparar con período anterior" (buscá el texto exacto, cerca de la
línea 2555) no tiene `onClick` — es decorativo. Agregale funcionalidad: al hacer click,
mostrá debajo de las tarjetas una tabla comparativa de dos columnas (mes actual vs. mes
anterior) con las mismas 6 métricas que ya están en `cards` (Ventas totales, Ventas al
contado, Ventas a crédito, Ganancia bruta, Gastos y retiros, Ganancia neta), más una columna
de variación %. Usá un estado local `showComparison` para mostrar/ocultar esa tabla.
```

**RESTRICCIONES ESTRICTAS:**
- No recalcules las métricas — ya están disponibles en las variables `cards`, `prevTotal`,
  `prevCash`, `prevCredit`, `prevExpensesTotal`, `prevGrossProfit` dentro de la misma función.
- No cambies el período de comparación (sigue siendo el mes calendario anterior).

**Archivos afectados:** `src/app/ui/reports.tsx` (función `ReporteMensualTab`).

---

### TAREA 106 · 🟡

**Descripción:** Reporte de clientes nuevos vs. recurrentes por período.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/reports.tsx`, la pestaña "Clientes" (función `ClientesTab`, cerca de la línea
1788) ya segmenta clientes por frecuencia de compra (Nuevo/Regular/Frecuente/VIP) pero no
muestra un conteo directo de "nuevos vs. recurrentes" para el período seleccionado en la
barra superior de Reportes (Hoy/7 días/Este mes/Personalizado).

Agregá arriba de la tabla existente (antes de las 3 tarjetas de resumen actuales) dos
tarjetas nuevas: "Clientes nuevos" (clientes cuya primera compra cayó dentro del período
actual — podés usar `customer.createdAt` dentro del rango `currentMonth`, que ya se pasa
como prop `sales`/`customers` filtrados) y "Clientes recurrentes" (clientes con `purchases >= 2`
en el período, usando el mismo cálculo que ya hace `customerStats`). Sumá un pequeño gráfico
de barras horizontal simple (dos barras, sin librería nueva) comparando ambos conteos.
```

**RESTRICCIONES ESTRICTAS:**
- No modifiques la lógica de segmentación (`getSegment`) existente ni la tabla principal.
- Usá los datos ya disponibles vía props (`sales`, `customers`) — no agregues fetches nuevos.

**Archivos afectados:** `src/app/ui/reports.tsx` (función `ClientesTab`).

---

### TAREA 107 · 🔴

**Descripción:** Exportar todos los reportes a PDF (actualmente solo CSV).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El endpoint `src/app/api/reports/export/route.ts` solo genera CSV para `type=ventas` y
`type=productos`. El proyecto ya tiene el patrón de PDFs establecido con `@react-pdf/renderer`
(mirá `src/app/ui/quote-pdf.tsx` + `src/app/api/quotes/[id]/pdf/route.tsx`, y también
`return-credit-note-pdf.tsx` + `src/app/api/returns/[id]/pdf/route.tsx` como segundo ejemplo).

Seguí ese mismo patrón para reportes:
1. Creá `src/app/ui/report-pdf.tsx` con un documento PDF simple (encabezado con nombre del
   negocio, tipo de reporte, rango de fechas, y una tabla de filas — reusá los mismos datos
   que ya arma `route.ts` para CSV, no inventes columnas nuevas).
2. Creá `src/app/api/reports/export-pdf/route.tsx` que reciba los mismos query params
   (`type`, `from`, `to`) que el endpoint CSV existente, consulte los mismos datos
   (`prisma.sale.findMany` / `prisma.product.findMany` como en `export/route.ts`) y renderice
   el PDF con `@react-pdf/renderer`.
3. En `src/app/ui/reports.tsx`, en el botón "Exportar" del header (línea ~453, que hoy llama a
   `downloadReportCsv`), agregá un segundo botón o un dropdown pequeño con dos opciones:
   "Exportar CSV" (comportamiento actual) y "Exportar PDF" (nuevo, llama al endpoint PDF).
```

**RESTRICCIONES ESTRICTAS:**
- No dupliques queries: si es sencillo, extraé la consulta de datos a una función compartida
  entre el endpoint CSV y el PDF; si no es trivial sin tocar mucho el archivo existente,
  está bien repetir la query de lectura (es solo un `findMany`, no hay riesgo de inconsistencia).
- No modifiques el endpoint CSV existente más que lo estrictamente necesario para compartir código.

**Archivos afectados:** `src/app/ui/report-pdf.tsx` (nuevo), `src/app/api/reports/export-pdf/route.tsx`
(nuevo), `src/app/ui/reports.tsx`.

---

### TAREA 108 · 🟢

**Descripción:** Exportar cualquier gráfico como imagen PNG con un clic.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Los gráficos de `src/app/ui/reports.tsx` están hechos con SVG inline (por ejemplo
`SalesEvolutionPanel`, `CategoryDonutPanel`, el gráfico de `ReporteMensualTab`). Agregá un
botón pequeño "Descargar PNG" (ícono `Download` de lucide-react, tamaño 12-14px, esquina
superior derecha de la tarjeta) a `SalesEvolutionPanel` y a `CategoryDonutPanel` como primer
caso de uso.

Implementación: al hacer click, serializá el `<svg>` de esa tarjeta con `XMLSerializer`,
convertilo a un `data:image/svg+xml` URL, dibujalo sobre un `<canvas>` oculto del mismo
tamaño usando una `Image` de JS, y luego exportá el canvas con `canvas.toBlob()` /
`toDataURL("image/png")` disparando una descarga (mismo patrón de `Blob` + `URL.createObjectURL`
+ click programático que ya se usa para exportar CSV en `sales-list.tsx`).

Extraé la lógica común a una función reutilizable, por ejemplo `downloadSvgAsPng(svgElement, filename)`,
para poder reusarla fácilmente en otros gráficos en el futuro sin duplicar código.
```

**RESTRICCIONES ESTRICTAS:**
- No agregues librerías nuevas (nada de `html2canvas` ni similares) — la conversión SVG→canvas→PNG
  nativa del browser alcanza para estos SVGs simples.
- Limitá el alcance a los dos gráficos mencionados; no hace falta tocar todos los gráficos de
  la página en esta tarea.

**Archivos afectados:** `src/app/ui/reports.tsx`.

---

### TAREA 109 · 🟢

**Descripción:** Modo pantalla completa para mostrar reportes en TV o pantalla de gerencia.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En el header de `src/app/ui/reports.tsx` (junto al botón "Exportar", línea ~453), agregá un
botón con ícono de pantalla completa (`Maximize2` de lucide-react) que use la Fullscreen API
nativa del navegador (`element.requestFullscreen()` / `document.exitFullscreen()`) sobre el
contenedor raíz de Reportes. Cambiá el ícono a `Minimize2` cuando ya está en pantalla completa
(escuchá el evento `fullscreenchange` del documento para sincronizar el estado).

En modo pantalla completa, ocultá los controles de período/tabs que no sean esenciales para
visualización pasiva (opcional, solo si es simple) — como mínimo asegurate de que el contenido
se vea bien a pantalla completa sin overflow raro.
```

**RESTRICCIONES ESTRICTAS:**
- Usá solo la Fullscreen API nativa del navegador, sin librerías externas.
- No rompas el layout normal (no-fullscreen) de Reportes.

**Archivos afectados:** `src/app/ui/reports.tsx`.

---

### TAREA 110 · 🟡

**Descripción:** Flecha de tendencia en el KPI principal comparado con el período equivalente
anterior (hoy vs. ayer, semana vs. semana anterior, mes vs. mes anterior) — hoy siempre
compara contra el mes calendario anterior sin importar el filtro elegido.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: las flechas de tendencia (▲/▼) YA existen — mirá la función `formatPct` en
`src/app/ui/reports.tsx` y su uso en `MetricCard`. El problema real es que `previousMonth`
(línea ~306: `useMemo(() => getMonthRange(-1), [])`) está fijo al mes calendario anterior
SIEMPRE, sin importar qué período esté seleccionado arriba (Hoy / 7 días / Este mes /
Personalizado). Esto hace que si elegís "Hoy", el KPI se compare contra todo el mes pasado,
lo cual no tiene sentido.

Arreglá esto haciendo que el período de comparación sea equivalente al período seleccionado:
- "today" → comparar contra el día anterior.
- "week" → comparar contra los 7 días anteriores a los 7 actuales.
- "month" → comparar contra el mes calendario anterior (comportamiento actual, no lo cambies).
- "custom" → comparar contra un rango de la misma duración, inmediatamente anterior al rango elegido.

Escribí una función `getPreviousPeriodRange(period, currentMonth)` que devuelva el rango
anterior correspondiente, y usala en lugar de `getMonthRange(-1)` fijo. Actualizá el texto
"vs período anterior" en `MetricCard` para que siga siendo genérico (ya lo es, no hace falta
tocarlo) pero ahora sí compare contra el período correcto.
```

**RESTRICCIONES ESTRICTAS:**
- Esto afecta a `metrics` (que alimenta las 5 `MetricCard` del header) — no toques
  `ReporteMensualTab`, que intencionalmente siempre compara contra el mes anterior.
- Verificá que `previousSales`/`previousExpenses` (usados en otras partes de la página, como
  `ProfitabilityPanel`) sigan recibiendo datos consistentes con el nuevo rango.

**Archivos afectados:** `src/app/ui/reports.tsx`.

---

## SECCIÓN: CAJA

### TAREA 112 · 🟡

**Descripción:** Diferencia teórica vs. real al cierre: mostrar delta y pedir confirmación —
ya implementado; falta exigir una nota cuando hay diferencia.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: esta tarea ya está implementada en `src/app/ui/cash-register-close.tsx` — ya calcula
`expectedAmount`, `closingAmount` y `difference`, y ya muestra un badge "Cuadre correcto" o
"Diferencia detectada" con un modal de confirmación de dos pasos antes de cerrar. El campo
`closingNotes` (mapeado a `CashRegisterSession.closingNotes` en el schema) ya existe pero es
opcional siempre (buscá la línea `...(closingNotes.trim() ? { closingNotes: closingNotes.trim() } : {})`).

Lo que falta: hacer que la nota sea OBLIGATORIA cuando `difference !== 0` (usando el mismo
umbral que ya usa `differenceIsZero`, `Math.abs(difference) < 0.005`). Si hay diferencia y el
campo de notas está vacío, deshabilitá el botón de confirmar cierre y mostrá un mensaje breve
como "Explicá el motivo de la diferencia antes de cerrar la caja". Si no hay diferencia, la
nota sigue siendo opcional como hoy.
```

**RESTRICCIONES ESTRICTAS:**
- No toques el cálculo de `expectedAmount`/`closingAmount`/`difference` ni el modal de
  confirmación existente — solo agregá la validación de la nota.
- No migres el schema; `closingNotes` ya existe y ya es nullable.

**Archivos afectados:** `src/app/ui/cash-register-close.tsx`.

---

### TAREA 113 · 🟡

**Descripción:** Nota obligatoria en movimientos manuales de caja.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El modelo `CashMovement` (`prisma/schema.prisma`) no tiene ningún campo de texto libre para
nota/descripción hoy (solo `type`, `amount`, `source`, `referenceId`). Agregá un campo
`note String?` vía `npx prisma migrate dev --name add_cash_movement_note` (nullable, aditivo,
no rompe movimientos existentes).

En `src/app/ui/cash-movement-form.tsx` (el formulario donde se cargan movimientos manuales de
caja — revisá cómo arma el payload hoy), agregá un campo de texto "Nota / motivo" y hacelo
obligatorio (no dejar enviar el formulario si está vacío, con el mismo estilo de validación
que ya use el resto del formulario). Actualizá el endpoint correspondiente
(`src/app/api/cash-movements/route.ts` o similar) para aceptar y persistir `note`, y mostralo
en `cash-movements-list.tsx` como una columna o texto secundario debajo del tipo de movimiento.
```

**RESTRICCIONES ESTRICTAS:**
- Migración debe ser nullable — no backfillees `note` para movimientos históricos.
- La obligatoriedad de la nota aplica solo a movimientos manuales nuevos (los generados
  automáticamente por ventas/cierres, si existen, no necesitan nota — revisá el `source` antes
  de asumir).

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/app/ui/cash-movement-form.tsx`,
`src/app/api/cash-movements/route.ts` (o el endpoint real), `src/app/ui/cash-movements-list.tsx`.

---

## SECCIÓN: INVENTARIO

### TAREA 114 · 🟢

**Descripción:** Resumen de inventario visible arriba (total SKUs, valor stock, productos sin stock).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/products-inventory.tsx` no hay hoy ninguna tarjeta de resumen arriba de la
lista/tabla de productos (solo filtros y acciones de importar/exportar plantilla). Agregá 3
tarjetas de métricas arriba de la tabla, en la misma línea visual que usa `MetricCard` en
`reports.tsx` (podés copiar ese patrón simple, no hace falta reusar el componente exacto si
está en otro archivo):

1. "Total SKUs" — cantidad total de productos (`products.length` con los datos ya cargados
   en el componente).
2. "Valor total de stock" — sumatoria de `costPrice * stock` de todos los productos.
3. "Productos sin stock" — cantidad de productos con `stock === 0`.

Usá los datos ya disponibles en el estado del componente (revisá cómo se cargan los productos
hoy — probablemente ya hay un fetch a `/api/products` con el array completo en memoria antes
de paginar/filtrar en la tabla).
```

**RESTRICCIONES ESTRICTAS:**
- Son tarjetas de solo lectura calculadas en el cliente — no agregues fetches nuevos ni
  endpoints nuevos.
- No modifiques la tabla ni los filtros existentes.

**Archivos afectados:** `src/app/ui/products-inventory.tsx`.

---

## SECCIÓN: GASTOS

### TAREA 115 · 🟢

**Descripción:** Gráfico de gastos por categoría dentro de la sección Gastos — ya implementado;
falta poder filtrar la tabla al hacer click en una categoría del gráfico.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Ojo: esta tarea ya está implementada. En `src/app/ui/expenses-list.tsx` ya existe
`CategoryDonutChart` (con su helper `arcPath`) mostrando "Gastos por categoría" con colores
por categoría, cerca de la línea 391.

Lo que falta: hacé que el gráfico sea interactivo. Al hacer click en un segmento del donut o
en su ítem de leyenda correspondiente, aplicá automáticamente ese valor al filtro de categoría
existente (`filterCategory`, ya usado por los inputs de filtro de la tabla de abajo) para que
la tabla se filtre a esa categoría. Agregá un indicador visual (por ejemplo, un anillo o borde
resaltado) en el segmento/ítem de leyenda actualmente seleccionado, y un botón pequeño "Quitar
filtro" cuando haya uno activo.
```

**RESTRICCIONES ESTRICTAS:**
- No dupliques el estado de filtro: reusá `filterCategory` ya existente, no crees un segundo
  estado paralelo.
- No cambies los colores ni el cálculo de `categoryStats`/`arcPath` existentes.

**Archivos afectados:** `src/app/ui/expenses-list.tsx`.

---

### TAREA 116 · 🔴

**Descripción:** Exportar historial de gastos a CSV / Excel por período (hoy el botón
"Exportar" solo abre el diálogo de impresión).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El botón "Exportar" de `src/app/ui/expenses-list.tsx` (buscá `QuickActionButton Icon={Download}
label="Exportar"`, línea ~411) hoy solo llama a `window.print()` — no genera ningún archivo.

Reemplazalo por una exportación real a CSV, siguiendo exactamente el mismo patrón ya usado en
`sales-list.tsx` y `cash-movements-list.tsx` (función `escapeCsvValue` + construcción manual
del CSV + `Blob` + `URL.createObjectURL` + `<a>` con click programático + `revokeObjectURL` —
no traigas librerías nuevas). Las columnas deben ser: fecha, categoría, descripción, monto.
Exportá respetando los filtros de fecha/categoría/búsqueda actualmente aplicados en la lista
(usá el mismo array ya filtrado que se renderiza en la tabla, no todos los gastos sin filtrar).

Nombrá el archivo descargado `gastos-YYYYMMDD.csv` (fecha de hoy, mismo formato que ya usa
`src/app/api/reports/export/route.ts`).
```

**RESTRICCIONES ESTRICTAS:**
- No modifiques el endpoint `/api/reports/export` — esta exportación es 100% client-side sobre
  los datos ya cargados en `expenses-list.tsx`, igual que en `sales-list.tsx`.
- No cambies el comportamiento de otros botones "Exportar" ya existentes en otras pantallas.

**Archivos afectados:** `src/app/ui/expenses-list.tsx`.

---

## SECCIÓN: VENTAS

### TAREA 117 · 🟢

**Descripción:** Sticky header en la tabla de ventas al hacer scroll.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/sales-list.tsx` hay dos tablas con `<thead className="bg-slate-50">` (líneas
~583 y ~1109) que no quedan fijas al hacer scroll dentro del contenedor de la tabla. Agregá
`sticky top-0 z-10` (clases de Tailwind) a ambos `<thead>`, y verificá que el contenedor padre
con scroll (`overflow-x-auto` o similar) tenga una altura máxima definida (`max-h-...` con
`overflow-y-auto`) para que el sticky header tenga sentido — si no la tiene, agregala sin
cambiar el resto del layout.

Asegurate de que el fondo del `<thead>` (`bg-slate-50`) tape completamente el contenido que
pasa por debajo al hacer scroll (no debe quedar transparente).
```

**RESTRICCIONES ESTRICTAS:**
- No cambies el contenido de las columnas ni el orden de las filas.
- Si agregar `max-h`/`overflow-y-auto` rompe visualmente otra parte de la página, preferí
  ajustar solo el contenedor de la tabla, no el layout general de `sales-list.tsx`.

**Archivos afectados:** `src/app/ui/sales-list.tsx`.

---

### TAREA 118 · 🟠

**Descripción:** Reenvío de comprobante por WhatsApp o email desde el detalle de la venta.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El patrón de compartir por WhatsApp ya existe: mirá `shareLastSaleWhatsApp` en
`src/app/ui/pos.tsx` (construye un link `https://wa.me/?text=...` o `https://wa.me/<telefono>?text=...`
con el resumen de la venta). El envío de emails ya existe en `src/lib/email.ts` (patrón
`sendXxxEmail` con Resend).

En `src/app/ui/sales-list.tsx`, en el modal/vista de detalle de una venta (revisá cómo se abre
el detalle de una venta individual hoy), agregá dos botones: "Reenviar por WhatsApp" y
"Reenviar por email".
- WhatsApp: reusá la misma lógica de `shareLastSaleWhatsApp` (armá el texto con fecha, folio,
  ítems y total de la venta seleccionada) — si el cliente asociado tiene teléfono, abrí
  `https://wa.me/<telefono>`, si no, dejá que el usuario elija el contacto (`https://wa.me/?text=...`).
- Email: si el cliente asociado tiene email, agregá una función nueva `sendSaleReceiptEmail`
  en `src/lib/email.ts` (mismo patrón que las demás) y un endpoint
  `POST /api/sales/[id]/send-email` que la invoque. Si no hay cliente o no tiene email,
  deshabilitá el botón con un tooltip "Este cliente no tiene email cargado".
```

**RESTRICCIONES ESTRICTAS:**
- No dupliques la lógica de armado del texto del ticket — extraé/reusá lo que ya arma
  `shareLastSaleWhatsApp` si es razonable, o replicá el mismo formato exacto si extraerlo a un
  helper compartido implica tocar demasiado `pos.tsx`.
- No agregues envío automático de emails al confirmar una venta — esto es solo reenvío manual
  bajo demanda desde el detalle.

**Archivos afectados:** `src/app/ui/sales-list.tsx`, `src/lib/email.ts`,
`src/app/api/sales/[id]/send-email/route.ts` (nuevo).

---

## SECCIÓN: PRODUCTOS

### TAREA 119 · 🟡

**Descripción:** Exportar catálogo completo a CSV o PDF (hoy solo se puede descargar una
plantilla vacía para importar).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/products-inventory.tsx` el botón "Descargar plantilla CSV" (línea ~586) genera
un CSV vacío de ejemplo para importar, no el catálogo real. El endpoint
`src/app/api/reports/export/route.ts` ya soporta `?type=productos` y genera un CSV real del
catálogo completo (nombre, código, categoría, precios, stock, iva) — pero no hay ningún botón
en `products-inventory.tsx` que lo use.

Agregá un botón "Exportar catálogo" (separado del botón de plantilla de importación, que no
debe tocarse) que llame a `/api/reports/export?type=productos` y descargue el CSV resultante
(podés usar `window.location.href` como ya hace `downloadReportCsv` en `reports.tsx`, o un
fetch + blob si preferís mostrar un estado de carga).

Además, agregá soporte de PDF a ese mismo endpoint: si la Tarea 107 ya agregó
`src/app/api/reports/export-pdf/route.tsx` con soporte para `type=productos`, reusalo agregando
un segundo botón "Exportar PDF" en `products-inventory.tsx`. Si la Tarea 107 todavía no se
implementó, agregá el soporte de PDF vos mismo en un nuevo endpoint siguiendo el mismo patrón
de `@react-pdf/renderer` que se usa en `quote-pdf.tsx`.
```

**RESTRICCIONES ESTRICTAS:**
- No modifiques el flujo de importación de CSV existente (plantilla, parseo, `parseProductsCsv`).
- El export de catálogo debe respetar los filtros de categoría/búsqueda actualmente aplicados
  en la vista si es sencillo hacerlo sin duplicar mucha lógica; si no, exportá el catálogo
  completo del tenant (igual que ya hace el endpoint existente) y dejalo documentado en el reporte.

**Archivos afectados:** `src/app/ui/products-inventory.tsx`, `src/app/api/reports/export/route.ts`
(o el nuevo endpoint PDF si corresponde).

---

### TAREA 120 · 🟡

**Descripción:** Toggle Activo/Inactivo visible en la lista sin entrar al detalle.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

El modelo `Product` no tiene ningún campo de estado activo/inactivo hoy. Agregá
`active Boolean @default(true)` vía `npx prisma migrate dev --name add_product_active`
(aditivo, con default `true` para no afectar productos existentes — todos quedan activos).

En `src/app/ui/products-inventory.tsx`, agregá una columna o un switch/toggle visible
directamente en cada fila de la tabla de productos (reusá el patrón de `ToggleSwitch` si existe
uno compartido, o un switch simple con Tailwind) que permita activar/desactivar un producto sin
abrir su detalle. Al desactivar un producto, debe seguir existiendo en la BD y en el historial
de ventas pasadas, pero no debe aparecer como opción seleccionable en el POS al buscar productos
para una nueva venta (revisá `src/app/ui/pos.tsx`, el buscador de productos, y agregá el filtro
`active: true` a esa consulta específica — no a `GET /api/products` en general, que se usa en
otros lados como Reportes o Inventario donde sí queremos ver productos inactivos).

Actualizá el endpoint `PATCH /api/products/[id]` (o el que exista) para aceptar `{ active }`.
```

**RESTRICCIONES ESTRICTAS:**
- Migración debe tener `@default(true)` — ningún producto existente debe quedar desactivado
  por accidente.
- El filtro de "solo activos" aplica específicamente al buscador de productos dentro del POS al
  cargar una venta nueva; no lo apliques a Reportes, Inventario, ni a la edición de ventas ya
  realizadas.

**Archivos afectados:** `prisma/schema.prisma` (migración), `src/app/ui/products-inventory.tsx`,
`src/app/api/products/[id]/route.ts` (o el real), `src/app/ui/pos.tsx` (filtro del buscador).

---

## PROTOCOLO DE REPORTE

Después de cada tarea, agregá una entrada en `TAREAS/REPORTE_DE_CAMBIOS.md` con este formato:

```
### TAREA XXX — [estado: ✅ Completada / ⚠️ Parcial / ❌ Bloqueada]
- Qué se hizo:
- Archivos modificados:
- Migraciones corridas (si aplica):
- Algo ya estaba implementado de otra forma / algo quedó pendiente:
- typecheck: OK / errores (detalle)
```

### Reglas del ciclo

- Al terminar TODAS las tareas de este lote, hacé commit de los cambios con un mensaje
  descriptivo y **corré `git push origin main`** (puede fallar por falta de credenciales en
  este entorno — está bien, Diego lo sube manualmente desde su PC).
- Si una tarea requiere una migración de Prisma, avisalo explícitamente en el reporte —
  Diego necesita correr `npx prisma migrate deploy` en producción.
- Si una tarea ya estaba implementada total o parcialmente de otra forma a la esperada
  (varias de este lote lo están — 103, 105, 112 y 115 ya tienen buena parte de la base hecha),
  documentá exactamente qué faltaba y qué se agregó, no reescribas lo que ya funciona.
- Si una tarea depende de otra de este mismo lote (107 y 119 comparten el endpoint de export
  PDF), resolvé la que aparece primero y reusá el resultado en la segunda — no dupliques código.
