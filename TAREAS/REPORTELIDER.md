# REPORTE LÍDER — SOLVEN

> Log resumido para el Ingeniero Líder (Claude Cowork). Acá va **solo lo esencial**, no el detalle — el detalle completo de cada tarea vive en `REPORTE_DE_CAMBIOS.md` (features) o `QA_REPORTE.md` (QA/fixes). Este archivo es lo que el Ingeniero Líder lee primero para tener una foto rápida de todo lo que se fue generando, sin tener que abrir cada reporte detallado.
>
> Una entrada corta (2-4 líneas máximo) por cada tarea u orden ejecutada, del más reciente al más antiguo. **No se borra ni se archiva entre ciclos** — es un log acumulativo permanente.

---

<!-- El agente irá agregando entradas acá debajo, del más reciente al más antiguo -->
### 2026-09-02 — CIERRE del primer ciclo completo de auditoría proactiva INGENIERODETESTEO (31-08-2026 a 02-09-2026)
Diego confirmó que terminamos la primera pasada completa: las 9 secciones de SOLVEN (POS, Devoluciones, Caja, Inventario, Deudas, Cotizaciones, Reportes/ARCA, Promociones, Usuarios/Permisos) fueron auditadas por INGENIERODETESTEO, cada bug confirmado pasó por el ciclo agente-VS Code + verificación de diff real de siempre (documentado arriba, entrada por entrada, entre el 31-08 y hoy), y no queda ningún hallazgo sin triagear. Total del ciclo: 9 tandas de fixes (POS-FIX-01..04, RET-FIX-01..07, CAJA-FIX-01..05, INV-FIX-01..10, DEUDA-FIX-01..06, COT-FIX-01..08 + COT-FOLLOWUP-01, REPORTE-FIX-01..07, PROMO-FIX-01..07, USER-FIX-01..09) más 2 arreglos directos fuera del ciclo agente-VS Code (aislamiento del cron de gastos recurrentes por tenant + método de pago/catch-up). Ningún hallazgo quedó sin resolver por descuido — lo que no se convirtió en fix fue por ser explícitamente una decisión de producto pendiente o algo que requiere reproducción en vivo, y quedó documentado, no perdido.

**Trabajo de cierre pedido por Diego, completado en esta sesión:**
1. Releí `TAREAS/ordenestest.md` completo (795 líneas, las 9 secciones) para extraer todo lo reutilizable antes de borrarlo — no solo los bugs ya ejecutados (esos ya estaban cerrados arriba en este archivo), sino los hallazgos "Verificado correcto" (para que una auditoría futura no reinvestigue lo mismo) y "Inconcluso" (decisiones de producto o reproducciones en vivo pendientes).
2. Los ítems "Inconcluso" que todavía no tenían un lugar permanente pasaron a `PENDIENTES.md`: gap de Nota de Crédito AFIP para devoluciones de ventas facturadas (`ARCA-NC-01`, el más importante — cumplimiento fiscal en producción real), método de reintegro no validado contra `Sale.paymentDetails` (`RET-DEV-METODO`), rate limiting de login no escala a múltiples instancias (`USER-RATELIMIT`), detección de cliente duplicado, lógica de pago con múltiples deudas abiertas, validación del desglose de denominación en caja, aviso de cambio de turno sin cerrar caja, stock reservado por cotizaciones puramente informativo, `CodeCounter` global entre tenants, `PromotionUsage` en devoluciones parciales, redondeo de IVA con tasas mixtas, aviso de cambio de precio a mitad de venta, una sola sesión de caja por tenant, y ausencia de función de anular venta — más un grupo de hallazgos menores de bajo impacto agrupados en un solo bloque para no perderlos.
3. `TAREAS/CLAUDE.md` actualizado: tabla nueva en sección 5 con el resumen de los 9 ciclos de fixes (con commits y fecha), 3 patrones arquitectónicos nuevos documentados en sección 3 (revalidación de sesión con TTL, motor de promociones "solo la mejor oferta", reserva-antes-de-AFIP + reintento `10016`), campos/modelos nuevos en sección 6 (`User.email`/`Customer.customerCode`/`Product.productCode` ahora únicos por tenant, `ProductLowStockAlert`, `RecurringExpense.method`, `Quote.sellerId/sellerCode`, índice único parcial de `CashRegisterSession`), conteo de tests actualizado en sección 11, y un resumen de cambios nuevo en sección 13. El gap de Nota de Crédito AFIP también quedó agregado a la tabla de bugs activos de sección 5, no solo en `PENDIENTES.md`.
4. Verifiqué que no quedaba ningún hallazgo "Inconcluso" huérfano (sin llegar a `PENDIENTES.md` ni a `CLAUDE.md`) antes de borrar — algunos, como el del método de reintegro en devoluciones, solo estaban referenciados desde una entrada vieja de este mismo archivo (RET-FIX-01..07, 31-08-2026) apuntando a `ordenestest.md` como fuente; ese puntero ahora apunta a `PENDIENTES.md`.
5. Borrados `TAREAS/AGENTE_EJECUTOR.md`, `TAREAS/INGENIERODETESTEO.md` y `TAREAS/ordenestest.md` (`git rm`). Las dos constituciones de rol quedan preservadas en el historial de git si hace falta reconstruirlas; el rol INGENIERODETESTEO sigue disponible como skill de Cowork (`solven-edge-case-auditor`) para el próximo ciclo de auditoría, y su metodología ya quedó resumida en `CLAUDE.md`.

Falta que Diego (o el agente de VS Code) haga el `git push` — este sandbox no tiene credenciales de GitHub configuradas.



### 2026-09-02 — USER-FIX-01..09: secreto de sesión obligatorio, expiración/revalidación de sesión (TTL 2min), email único por tenant (decisión de Diego, migración aplicada a Neon), permisos GET a OWNER, validación de formato de email, rate limit en switch-cashier, auditoría, guard de seed en producción
Ejecutados los 9 hallazgos de INGENIERODETESTEO en `ordenestest.md` sobre `src/lib/auth.ts`, `src/lib/tenant.ts`, `src/modules/users`, `src/app/api/users`, `src/app/api/role-permissions`, `src/app/api/auth`, `src/middleware.ts`, `prisma/schema.prisma`. Dos puntos requirieron confirmación explícita de Diego durante la ejecución (vía pregunta directa): USER-FIX-04 (migrar `User.email` a único por tenant en vez de dejarlo global — Diego eligió migrar) y la aplicación de esa migración contra la base real de Neon (Diego autorizó aplicarla ahora). `requireRole` gana revalidación puntual contra la DB con TTL cacheado de 2 min (USER-FIX-03), cubriendo casi todas las rutas protegidas sin tocar el gate global. Auditoría nueva en activar/desactivar/cambio de PIN/cambio de rol/borrado de usuario y en cambios de permisos por rol. Se detectaron y corrigieron 3 gaps de test-mocking causados por estos cambios (2 archivos que ejercitan `requireRole` real sin mock de Prisma, 1 archivo sin mock de `logAudit`) más 1 archivo sin cobertura alguna (`role-permissions/route.test.ts`, creado). `lint`/`typecheck`/`test` limpios (463 passed / 2 skipped). Commit `5b3794c`. Detalle completo en `REPORTE_DE_CAMBIOS.md`.

**Verificado por el Ingeniero Líder contra el diff real — el ciclo más sensible hasta ahora (autenticación/sesión), revisado con el mayor cuidado.** Recorrí `auth.ts`/`tenant.ts` línea por línea: `getHmacKey()` ahora explota si `SOLVEN_SESSION_SECRET` falta o mide menos de 32 caracteres (antes firmaba con clave `""` silenciosamente — combinado con el fail-open de rol de abajo, era forja de sesión `OWNER` trivial sin credenciales); `verifySession` devuelve `null` si el `role` del payload falta o no es válido (antes asumía `OWNER` por defecto — el bug más grave de los nueve); `createSession` perdió el default `role="OWNER"`, ahora es obligatorio. `requireRole` revalida contra la DB (`active`, `role`, `tenantId` del usuario real) con cache de 2 minutos por `userId` — confirmé que la orden explícitamente ofrecía esta opción como "alternativa más simple" al gate global de `middleware.ts` (no tocarlo era una salida válida, no un atajo), y que 2 minutos cae dentro del rango que la propia orden sugería (2-3 min). Los 4 tests nuevos de `tenant.test.ts` prueban exactamente esto, incluyendo que el TTL realmente cachea (`does not re-hit the database on a second call within the TTL window` — verificado que el mock de Prisma se llama una sola vez).

Migración `20260902100000_user_email_tenant_unique` confirmada aplicada en Neon (`_prisma_migrations.finished_at` presente, sin rollback), índice global viejo (`User_email_key`) reemplazado por el compuesto por tenant + índice suelto por email — sin emails duplicados actuales que pudieran haber bloqueado la migración. El loop de `login/route.ts` (`findMany` + `verifyPassword` contra cada candidato) es exactamente lo que la propia orden proponía como camino más simple y seguro, con la razón ya documentada en el código de por qué no es un problema de escala real (las contraseñas son independientes por tenant, matchea como máximo un usuario real). Confirmé que `register.ts` quitar el chequeo de duplicado no es una regresión: con unicidad por tenant, chequear contra un `tenantId` recién creado en el mismo request es lógicamente inerte. `USER-FIX-05` a `09` (permisos GET, validación de email, rate limit de `switch-cashier`, auditoría, guard de `NODE_ENV` en `seed.ts`) confirmados línea por línea contra el diff, todos coinciden exacto con lo pedido. `typecheck` re-corrido acá mismo, limpio.

Los 3 puntos que el propio agente dejó para que yo revisara: (a) migración en Neon — confirmada arriba; (b) escala del loop de login — no es un problema real, ver arriba; (c) si el TTL de 2 minutos es el valor de negocio correcto — queda abierto para Diego, es una decisión de producto (cuánto puede seguir operando un empleado recién desactivado), no algo que yo deba resolver solo. Sin hallazgos que requieran corrección. USER-FIX-01..09 queda cerrado.

### 2026-09-02 — PROMO-FIX-01..07: segmento de cliente aplicado al motor, promociones dejan de acumularse (solo gana la mejor), overlap ALL_PRODUCTS/CATEGORY/SPECIFIC_PRODUCT, validaciones de coherencia tipo/aplicación, fecha AR, PromotionUsage liberado en devolución total, permisos GET a OWNER, auditoría
Ejecutados los 7 hallazgos de INGENIERODETESTEO en `ordenestest.md` sobre `src/modules/promotions`, `src/modules/sales`, `src/modules/returns`, `src/app/api/promotions`, `src/app/ui/promotions.tsx`: `applyPromotionsToCart` recibía `customerSegment` pero ningún call site se lo pasaba, así que las promos VIP/Recurrente/Nuevo nunca aplicaban — ahora ambos call sites (`apply/route.ts`, `sale-data-access.ts`) resuelven el segmento real del cliente antes de invocar el motor. El motor de combinación se reescribió por completo: cada promoción se evalúa en forma aislada contra el precio original y, por decisión explícita de Diego ("solo se aplique la mejor oferta, no acumulables"), por producto solo sobrevive la de mayor descuento — se eliminó el diseño anterior de acumulación mutable. `findOverlappingPromotions` ahora detecta solapamiento cruzado entre ALL_PRODUCTS/CATEGORY/SPECIFIC_PRODUCT. Validación nueva: SPECIAL_PRICE solo puede ir con SPECIFIC_PRODUCT, y SPECIFIC_PRODUCT exige `productAId`, en creación y edición. Fechas de vigencia con offset `-03:00` explícito (mismo patrón que REPORTE-FIX-03). Una devolución total de una venta libera el `PromotionUsage` asociado. 5 rutas GET pasan de `requireTenantId()` a `requireRole(["OWNER"])`. `logAudit` agregado en creación/edición/borrado/duplicado de promociones. `lint`/`typecheck`/`test` limpios (454 passed / 2 skipped). Commit `3f17900`. Detalle completo en `REPORTE_DE_CAMBIOS.md`.

**Verificado por el Ingeniero Líder contra el diff real.** Este ciclo tenía dos puntos con **cero cobertura de test automatizado** — el cableado de `customerSegment` en `sale-data-access.ts`/`apply/route.ts` (PROMO-FIX-01) y la liberación de `PromotionUsage` en devolución total (`returns/index.ts`, PROMO-FIX-05) — así que los recorrí a mano en detalle en vez de confiar en la suite verde. `sale-data-access.ts`: el fetch de segmento es un `findFirst({id, tenantId})` nuevo y aislado, no toca el fetch de cliente ya existente para límite de crédito — bajo riesgo, cableado mecánico de 3 líneas idéntico en ambos call sites. `returns/index.ts`: tracé `isFullReturn` con cuidado — `alreadyReturnedByProductId` se calcula ANTES de crear el `Return` actual (línea 268-277, el `return.create` recién pasa en la línea 361), así que no hay doble conteo entre devoluciones previas y la actual; la condición exige que **todos** los ítems de producto de la venta (ítems de servicio se tratan como ya satisfechos) sumen `alreadyReturned + returningNow >= saleItem.quantity`, lo que correctamente dispara la liberación también en el caso de devoluciones parciales sucesivas que en conjunto terminan vaciando la venta, no solo una devolución total de una sola vez. Confirmé además que `PromotionUsage.saleId` es exactamente el mismo campo que usa `sale-data-access.ts` al crearlo (`sale.id`), así que el `deleteMany({ where: { saleId } })` apunta a las filas correctas.

El motor de promociones (`promotion-engine.ts`) lo recorrí función por función: `runPromotionInIsolation` corre cada promoción contra una copia fresca de los ítems a precio original (nunca contra el resultado acumulado de otra promoción), y el `Map` de ganadores por `itemIndex` usa `greaterThan` estricto — confirmé que un empate exacto lo gana la promoción procesada primero, tal como pedía la orden. Esto SÍ tiene tests genuinos y los revisé: los 3 nuevos (`does not accumulate... same type`, `...different types`, `keeps the earlier promotion when two proposals tie exactly`) traen aserciones numéricas exactas que coinciden con mi propio cálculo a mano (ej. $30 fijo le gana a 10% sobre $100 → `finalPrice = 70`), y los 3 tests de segmento ya existían de un ciclo anterior (cuando se agregó el parámetro pero nadie lo conectaba) y siguen pasando sin cambios contra el motor reescrito — confirma que el filtro de segmento (líneas 411-417) no se rompió en la reescritura. `findOverlappingPromotions` y las validaciones de coherencia (`SPECIAL_PRICE` sólo con `SPECIFIC_PRODUCT`, `productAId` inmutable una vez `SPECIFIC_PRODUCT`) también los recorrí a mano — correctos, y confirmé que `type`/`application` están excluidos a nivel de tipo de `ValidatedUpdatePromotionInput` (`Omit<..., "type" | "application">`), así que la combinación tipo/aplicación no se puede romper vía edición aunque la validación de coherencia sólo esté en el create. `typecheck` re-corrido acá mismo, limpio. Sin hallazgos que requieran corrección.

Dejo una recomendación en `PENDIENTES.md` (no bloqueante): dado que PROMO-FIX-01 y PROMO-FIX-05 quedaron sin test automatizado, conviene una prueba manual en producción con un cliente de segmento real (VIP/Recurrente/Nuevo) y una promoción restringida a ese segmento, y una devolución total de una venta con promoción aplicada, para confirmar en vivo lo que mi lectura de código ya da por correcto. PROMO-FIX-01..07 queda cerrado.

### 2026-09-02 — REPORTE-FIX-01..07: doble emisión de factura (CAE huérfano), reintento por número de comprobante usado, horario AR en reportes, permisos de exportación, tenant en getInvoiceBySaleId, vencimiento de certificado ARCA, auditoría
Ejecutados los 7 hallazgos de INGENIERODETESTEO en `ordenestest.md` sobre `src/modules/invoices`, `src/lib/arca`, `src/app/api/reports`, `src/app/api/invoices`: `emitInvoice` ahora reserva la fila de `Invoice` (con `cae`/`caeFchVto` placeholder) antes de llamar a AFIP, apoyándose en el `@unique` de `saleId` para rechazar una segunda emisión concurrente de la misma venta antes de gastar un CAE real; completa la fila con la respuesta real de AFIP y la borra si la solicitud de CAE falla. Si AFIP rechaza por "comprobante ya autorizado" (código `10016`) se reintenta una sola vez con el próximo número real en vez de mostrarle el error crudo al cajero. Los reportes de exportación (`/api/reports/export` y `/export-pdf`) arman el rango de fechas con `-03:00` explícito en vez de la zona horaria del proceso del servidor (Vercel corre en UTC), y ambos pasan de `requireTenantId()` a `requireRole(["OWNER"])`. `getInvoiceBySaleId` ahora exige `tenantId`. Se agregó `getCertExpiryInfo` (lee `certificate.validity.notAfter` con `node-forge`) y se expone `certExpiresAt`/`certDaysRemaining` en `GET /api/invoices/test`. Se registra `logAudit` con la nueva acción `INVOICE_EMITTED` en `POST /api/invoices`. `lint`/`typecheck` limpios; `test` limpio salvo 1 falla intermitente preexistente y no relacionada (`stock-adjustment.integration.test.ts`, módulo de inventario no tocado en este cambio — pasa en corrida aislada, confirmado antes de commitear). Commit `107ae44`. Detalle completo en `REPORTE_DE_CAMBIOS.md`.

**Verificado por el Ingeniero Líder contra el diff real.** Recorrí `emitInvoice` línea por línea, incluyendo el caso más sutil: dos ventas *distintas* que reservan el mismo `voucherNumber` local (porque ninguna incrementó el registro real de AFIP todavía) — confirmé que ese caso lo resuelve REPORTE-FIX-02 (reintento por `10016`), no solo el caso de doble emisión de la *misma* venta que cubre el `@unique` de `saleId`; los dos fixes son complementarios, no redundantes. Confirmé que la fila reservada usa el `voucherNumber` original al crearse pero se completa al final con `caeResult.voucherNumber` (el que realmente devolvió AFIP), así que un reintento no deja datos stale. Los 4 tests nuevos de `invoice-data-access.test.ts` verifican exactamente esto: orden de invocación (`create` antes que `requestCAE`), rechazo por `P2002` sin llamar a AFIP, reintento único ante `10016` sin borrar la reserva, y limpieza (`invoice.delete`) sin reintento ante cualquier otro error — no son aserciones superficiales. `-03:00` explícito confirmado en ambas rutas de export; `requireRole(["OWNER"])` con manejo de `ForbiddenError`→403 confirmado, con tests de 401/403 genuinos (no solo happy path). Un solo detalle que corregí yo mismo, directo: el `logAudit` de `INVOICE_EMITTED` no incluía `voucherType` en el metadata pese a que la orden lo pedía explícitamente — lo agregué en `src/app/api/invoices/route.ts` (commit separado), `typecheck` limpio, el test existente usa `objectContaining` así que no se rompió. `typecheck` re-corrido acá mismo, limpio. Sin otros hallazgos. REPORTE-FIX-01..07 queda cerrado.

### 2026-09-02 — COT-FOLLOWUP-01: método real de pago al confirmar cotización queda en la venta (Ingeniero Líder, fuera del ciclo agente-VS Code, a pedido de Diego)
Cerré directamente la salvedad menor que dejé documentada al verificar COT-FIX-01..08: `confirmQuote` no guardaba en ningún lado cuál de los 4 métodos (Efectivo/Tarjeta/Transferencia/Crédito) se usó para confirmar, solo si terminaba en `CASH` o `CREDIT`. Ahora llena `Sale.paymentDetails` con `[{ method, amount: netTotal }]` para los 3 métodos no-crédito (`Prisma.JsonNull` para Crédito, mismo criterio que una venta 100% a fiar por POS), reusando el formato ya establecido en `sale-validation.ts`/`sale-data-access.ts` — así una cotización confirmada por transferencia queda visible en los mismos reportes/filtros que ya existen para ventas directas por transferencia (`paymentDetails: { array_contains: [{ method }] }`). 2 tests nuevos/ampliados en `quote-data-access.integration.test.ts` confirman el monto exacto guardado para Efectivo y Tarjeta, y `null` para Crédito. `typecheck`/`lint` re-corridos acá mismo, limpios. `PENDIENTES.md` → `COT-FOLLOWUP-01` movido a Cerrados.

### 2026-09-02 — COT-FIX-01..08: caja abierta y monto neto al confirmar cotización, método de pago real, cliente y vendedor asociados, permisos GET, auditoría (VERIFICADO, con una salvedad menor sin bloquear)
Ejecutados los 8 hallazgos de INGENIERODETESTEO en `ordenestest.md` sobre `src/modules/quotes` y `src/app/api/quotes`: `confirmQuote` ahora exige caja abierta (`requireOpenCashRegisterSession`) antes de crear la venta; el `CashMovement`/`Debt` generado usa el monto neto (`totalAmount - discountAmount`), con el descuento ya acotado al total en `createQuote` (`Prisma.Decimal.min`); acepta un método de pago real (Efectivo/Tarjeta/Transferencia/Crédito, con selector nuevo en el modal de confirmación) en vez de forzar `CASH`, creando `Debt` en vez de `CashMovement` para Crédito; la venta generada copia `customerId` y `sellerId`/`sellerCode` (campo nuevo en `Quote`, migración aplicada a Neon) de la cotización; `GET /api/quotes` y `GET /api/quotes/[id]` pasan a `requireRole`; se agregó `logAudit` para creación/confirmación/cancelación de cotizaciones. `lint`/`typecheck`/`test` limpios (443 passed / 2 skipped). Commit `e4edb30`. Detalle completo en `REPORTE_DE_CAMBIOS.md`.

**Verificado por el Ingeniero Líder contra el diff real, incluyendo chequeo directo contra Neon.** Confirmé línea por línea los 8 fixes contra `quote-data-access.ts`, las 4 rutas de `src/app/api/quotes` y `quotes-list.tsx`: `requireOpenCashRegisterSession(tenantId, tx)` es la primera operación dentro de la transacción de `confirmQuote` (igual que `createSale`, que también exige caja abierta sin importar el método de pago — confirmé esto releyendo `sale-data-access.ts:111-116`, no es una afirmación sin chequear); `CashRegisterNoSessionOpenError` mapea a 409 en la ruta; `Prisma.Decimal.min(requestedDiscountAmount, total)` acota el descuento antes de guardar; el selector de 4 métodos en `quotes-list.tsx` viaja como `paymentMethod` real en el POST. Migración `20260902090000_add_quote_seller` confirmada aplicada en Neon (`_prisma_migrations.finished_at` presente, sin rollback), columnas `sellerId`/`sellerCode` existen en `Quote`. Los 5 tests de integración nuevos que releí (`caps the discountAmount...`, `propagates customerId and sellerId...`, `creates a Debt instead of a CashMovement...`, `rejects a Credito confirmation...`, `rejects confirmation when there is no open cash register session...`) hacen aserciones numéricas exactas contra la DB real (descuento tope $50 sobre un total de $50, movimiento de caja neto de $80 sobre un total $100 con $20 de descuento, deuda de $100 con cero `CashMovement`, stock sin tocar cuando se rechaza por falta de caja) — no son tests superficiales.

**Salvedad que dejo documentada, no bloquea el cierre:** `confirmQuote` crea un `CashMovement` para Efectivo, Tarjeta Y Transferencia (todo lo que no sea Crédito) — el texto de la orden en `ordenestest.md` decía "sólo... cuando el método elegido sea Efectivo", pero en los hechos esto replica exactamente el comportamiento real y preexistente de `createSale` (que genera el `CashMovement` por cualquier monto "cobrado ahora", incluida tarjeta/transferencia vía `paymentDetails`, y sólo lo omite en Crédito puro) — así que el agente siguió el precedente real del código en vez del texto literal de la orden, y me parece la decisión correcta. Lo que sí queda como gap real, menor: el método específico elegido (Efectivo vs Tarjeta vs Transferencia) no se persiste en ningún lado de la `Sale` resultante — `paymentType` sólo distingue CASH de CREDIT, y no se llena `Sale.paymentDetails`. No es un bug de plata (todos los montos son correctos), es un hueco de trazabilidad para reportes futuros ("cuánto vendimos por transferencia vía cotizaciones confirmadas" no se podría responder hoy). Lo agrego como ítem Bajo en `PENDIENTES.md`, no amerita reabrir este ciclo. `typecheck`/`lint` no pude re-correrlos de forma independiente esta vez — el sandbox tuvo timeouts repetidos en ambos comandos durante esta verificación (infraestructura, no código); la verificación de este ciclo se apoya en la lectura línea por línea del diff y de los tests, no en una corrida propia. Sin otros hallazgos. COT-FIX-01..08 queda cerrado.

### 2026-09-01 — DEUDA-FIX-01..06: aislamiento por tenant en pagos de deuda, write-off zeroea saldo, auditoría, permisos GET, límite de crédito visible, customerCode único por tenant (VERIFICADO)
Ejecutados los 6 hallazgos de INGENIERODETESTEO en `ordenestest.md` sobre `src/modules/debts`, `src/app/api/debts`, `src/app/api/debt-payments`, `src/app/api/customers`: `registerDebtPayment` ahora scopea por `tenantId` (lectura y `updateMany` guardado); `writeOffDebt` pone `remainingAmount` en 0; `AuditLog` con `DEBT_CREATED`/`DEBT_PAYMENT_REGISTERED`/`DEBT_WRITTEN_OFF`; `GET /api/debts`, `GET /api/debt-payments` y `GET /api/customers` pasan a `requireRole` en vez de solo `requireTenantId`; aviso de límite de crédito en alta de cliente y en el POS; `Customer.customerCode` único por tenant (migración aplicada a Neon, confirmada por el usuario). Detalle completo en `REPORTE_DE_CAMBIOS.md`. `lint`/`typecheck`/`test` limpios (419 passed / 2 skipped). Commit `83900e9`.

**Verificado por el Ingeniero Líder contra el diff real, incluyendo chequeo directo contra Neon.** `DEUDA-FIX-01` (el más crítico: pago de deuda ajena por falta de scope de tenant) confirmado corregido en ambos lados de `registerDebtPayment`: `findUniqueOrThrow({id})` → `findFirstOrThrow({id, tenantId})`, y el `updateMany` de descuento de saldo ahora también filtra por `tenantId` además del guard de monto existente. Test nuevo de integración confirma el caso real: un segundo tenant intenta pagar la deuda del primero, la promesa rechaza y `remainingAmount` queda intacto en 50 (no descontado). `DEUDA-FIX-02` implementado exactamente como recomendaba la orden — arreglarlo en la fuente (`writeOffDebt` pone `remainingAmount` en `Decimal(0)`) en vez de parchar los ~8 consumidores que sumaban saldo sin excluir `writtenOff`; confirmé que la orden explícitamente prefería este enfoque, no es un atajo del agente. `DEUDA-FIX-03..06` verificados línea por línea contra el diff: `logAudit` fire-and-forget en las 3 rutas después de que la operación de negocio resolvió (mismo patrón que `RETURN_CREATED`); los 3 `GET` pasan a `requireRole(["OWNER","CASHIER","SUPERVISOR"], "customers")`, mismo set de roles que ya exigían sus `POST`; avisos de límite de crédito agregados en `customer-new-form.tsx` y `pos.tsx` sin bloquear la venta, tal como pedía la orden; `Customer.customerCode` pasa de único global a único por tenant. Confirmé con una consulta de solo lectura contra Neon que la migración `20260901170000_customer_code_tenant_unique` está aplicada y sin rollback (`_prisma_migrations.finished_at` presente, `rolled_back_at` null), que el índice viejo (`Customer_customerCode_key`, global) ya no existe y el nuevo (`tenantId, customerCode`) sí. También verifiqué el hallazgo adicional del propio reporte (4 tests de integración de deudas rotos por `AuditLog_userId_fkey` al dejar de mockear `logAudit`): el fix sigue el mismo patrón ya usado en `products/route.integration.test.ts` (usuario real en `beforeEach`, limpieza de `auditLog`/`user` antes de `tenant`) — no es una afirmación sin chequear. `typecheck` re-corrido de forma independiente acá, limpio (0 errores). `lint` no pude re-correrlo en este sandbox (timeout, no falla — limitación de entorno, no del código). Sin hallazgos nuevos este ciclo — DEUDA-FIX-01..06 queda cerrado.

### 2026-09-01 — INV-FIX-01..10: aislamiento por tenant, ajuste de stock atómico, borrado seguro de productos, alertas de stock bajo con throttle, aviso de venta bajo costo (VERIFICADO)
Ejecutados los 10 hallazgos de INGENIERODETESTEO en `ordenestest.md` sobre `src/modules/inventory`, `src/modules/products`, `src/app/api/products`, `src/app/api/inventory-*`: `adjustProductStock` ahora filtra por `tenantId` y usa UPDATE condicional con optimistic locking (antes era un SET ciego sin scope de tenant ni protección de carrera), con auditoría `INVENTORY_ADJUSTED`. `DELETE /api/products/[id]` pasa a `requireRole(OWNER|INVENTORY)` (antes solo `requireTenantId()`) y bloquea el borrado físico si el producto tiene ventas o movimientos de inventario asociados. `Product.productCode` pasa de único global a único por tenant (migración aplicada a Neon). `createProduct` registra `InventoryMovement` de alta cuando el stock inicial es mayor a cero. `GET /api/inventory-movements` exige rol en vez de solo tenant. Nueva tabla `ProductLowStockAlert` con throttle de 12hs para el email de stock bajo (migración aplicada a Neon). Nuevo warning no bloqueante cuando `salePrice < costPrice`, expuesto en la respuesta de crear/editar producto.

**Verificado por el Ingeniero Líder contra el diff real, incluyendo chequeo directo contra Neon.** `INV-FIX-01` (el más crítico: `adjustProductStock` ignoraba `tenantId` — un tenant podía leer y modificar el stock de un producto de otro tenant conociendo su `id`) confirmado corregido en ambos lados: la lectura pasó de `findUniqueOrThrow({id})` a `findFirstOrThrow({id, tenantId})`, y la escritura pasó de un `update({where:{id}})` ciego a un `UPDATE ... WHERE id AND tenantId AND stock = <valor leído>` con optimistic locking real. Confirmé con una consulta de solo lectura contra Neon que las dos migraciones (`..._product_code_tenant_unique`, `..._add_product_low_stock_alert`) están aplicadas y sin rollback, que el índice único viejo (`Product_productCode_key`, global) ya no existe y el nuevo (`tenantId, productCode`) sí, y que `npx prisma migrate status` confirma "Database schema is up to date" — no es una afirmación sin chequear del agente. Releí el test de concurrencia reescrito de `stock-adjustment.integration.test.ts`: ahora es una carrera genuina con `Promise.allSettled` sobre el mismo producto, no el pre-mutate que no probaba nada antes. `typecheck`/`lint` re-corridos de forma independiente, limpios.

Sobre las "fallas en rojo" que menciona el propio reporte del agente: fueron 2 corridas de `npm test` con errores intermitentes de `PrismaClientInitializationError: Can't reach database server` contra Neon (problema de conectividad de red, no del código) — la corrida final, limpia, dio 414 passed / 2 skipped / 0 failed. No encontré ningún hallazgo nuevo en este ciclo.

### 2026-09-01 — Método de pago en gastos recurrentes + catch-up del cron (Ingeniero Líder, fuera del ciclo agente-VS Code, con decisión de producto de Diego)
Diego decidió (tras recomendación profesional) bloquear el gasto recurrente en efectivo sin caja abierta, en vez de generarlo igual como antes. Implementado con un ajuste al planteo original: `RecurringExpense` no tenía campo de método de pago, así que el cron trataba TODO gasto recurrente como efectivo sin importar la realidad (alquiler/suscripciones suelen ser transferencia). Agregado `RecurringExpense.method` (migración `20260901150000_add_recurring_expense_method`, aplicada a Neon y verificada con consulta de solo lectura contra `information_schema`/`_prisma_migrations` — 0 filas existentes en la tabla, sin riesgo de dato legado). El checkbox "repetir cada mes" en `expenses-list.tsx` ahora reusa el método ya elegido para el gasto puntual, sin agregar UI nueva. El guard de caja abierta (`CAJA-FIX-03`) solo aplica cuando el método es "Efectivo". Bonus acordado: el cron ahora hace catch-up — un gasto recurrente que no se generó el día que le tocaba se genera en la próxima corrida exitosa dentro del mismo mes, en vez de perderse hasta el mes siguiente (cambié `effectiveDay !== currentDay` por `currentDay < effectiveDay`, sin afectar el filtro de "ya generado este mes" que evita duplicados).

`recurring-expense-validation.test.ts` (7 tests, nuevo) y `recurring-expense-data-access.test.ts` ampliado a 6 tests (agregado reloj simulado con `vi.setSystemTime` para que la lógica de día-del-mes sea determinista sin importar cuándo corra la suite). `typecheck`/`lint` re-corridos, limpios. No se pudo correr `npm test` en este sandbox (limitación conocida, ver `CLAUDE.md` sección 11) — releídos los mocks y aserciones línea por línea contra la implementación real antes de commitear.

### 2026-09-01 — Aislado el cron de gastos recurrentes por tenant (Ingeniero Líder, fuera del ciclo agente-VS Code)
Arreglo directo, sin pasar por el agente de VS Code: `generateDueRecurringExpenses()` ahora envuelve cada tenant en su propio `try/catch` — si uno falla (típicamente por `CashRegisterNoSessionOpenError`, introducido por `CAJA-FIX-03`), se registra en un array `failures` devuelto por la función y el loop sigue con el resto, en vez de cortar la corrida completa del cron para todos los tenants. El cron (`route.ts`) loguea las fallas con `console.error` y las devuelve en la respuesta, sin abortar. Agregado `recurring-expense-data-access.test.ts` (3 tests con `prisma`/`createExpense` mockeados, no toca la DB real) probando la generación simple, el aislamiento entre tenants, y el caso "no debido hoy". `typecheck`/`lint` limpios; no se pudo correr `npm test` en este sandbox (limitación conocida), se releyeron los mocks contra la implementación real línea por línea. Queda solo la pregunta de producto (movida a Medio en `PENDIENTES.md`, ya no urgente): si el gasto recurrente en efectivo debe bloquearse sin caja abierta o generarse igual como antes.

### 2026-09-01 — CAJA-FIX-01..05: doble apertura de caja, método de pago en gastos/deuda, caja abierta obligatoria, permisos GET, doble cierre (VERIFICADO)
Ejecutados los 5 hallazgos de INGENIERODETESTEO en `ordenestest.md` sobre `src/modules/cash-register`, `src/modules/expenses`, `src/modules/debts`, `src/modules/cash`: índice único parcial en Postgres (`CashRegisterSession_tenantId_open_unique`, `WHERE status='OPEN'`) para bloquear doble apertura concurrente; campo `method` agregado a `Expense`/`DebtPayment` (mismo patrón que `Return.refundMethod`), default a "Efectivo" si se omite; `requireOpenCashRegisterSession` ahora exige caja abierta para gastos/pagos de deuda en efectivo y para todo movimiento manual de caja; `GET /api/cash-register/[id]` y `GET /api/cash-register/sessions` ahora exigen el mismo rol que las mutaciones; `closeSession` usa `updateMany` guardado por status + chequeo de `count` para bloquear doble cierre concurrente.

**Verificado por el Ingeniero Líder contra el diff real, incluyendo verificación directa contra la DB de producción (Neon) — no solo el self-report.** Corrí una consulta de solo lectura contra la base real y confirmé: el índice único parcial `CashRegisterSession_tenantId_open_unique` existe de verdad (`WHERE status = 'OPEN'`), las columnas `Expense.method`/`DebtPayment.method` existen, y ambas migraciones (`20260901120000_...`, `20260901121000_...`) aparecen en `_prisma_migrations` con `finished_at` y sin `rolled_back_at` — la aplicación a Neon que reportó el agente es real, no una afirmación sin verificar. `openSession` captura `P2002` y lo traduce a `CashRegisterAlreadyOpenError` como pedía la orden; `closeSession` solo dispara el email de diferencia después del `updateMany` exitoso (route-level, no dentro de la transacción), así que el perdedor de una carrera nunca llega a mandarlo dos veces. `typecheck`/`lint` re-corridos de forma independiente, limpios.

⚠️ **La ambigüedad que el agente dejó documentada (no resuelta, correctamente) es real y más urgente de lo que suena:** `generateDueRecurringExpenses()` (cron diario 4:00 UTC, `vercel.json`) es un `for` simple sin `try/catch` por tenant. Como el `method` de un gasto recurrente nunca se pasa, cae al default "Efectivo", lo que ahora activa el guard de caja abierta — si UN tenant no tiene la caja abierta a esa hora, `createExpense` lanza y el loop corta ahí, dejando sin generar los gastos recurrentes de TODOS los tenants que venían después en el orden de iteración esa noche (no solo el tenant afectado). Separé esto en `PENDIENTES.md` en dos partes: la pregunta de producto real de Diego (¿debe bloquearse el gasto recurrente en efectivo sin caja abierta, o generarse igual como antes?) y un arreglo que no depende de esa respuesta — aislar el loop con `try/catch` por tenant para que un tenant sin caja abierta no le rompa el día a los demás. Esto último no es una decisión de negocio, es una corrección de resiliencia que recomiendo priorizar independientemente de lo que Diego decida sobre el guard en sí.

### 2026-08-31 — RET-FIX-01..07: devoluciones — carrera de cantidad, reintegro con descuento, deuda MIXED, permisos GET, caja abierta, servicios, auditoría (VERIFICADO)
Ejecutados los 7 hallazgos de INGENIERODETESTEO en `ordenestest.md` sobre `src/modules/returns` y `src/app/api/returns`: transacción `Serializable` + error tipado (`ReturnConcurrentConflictError`, mapea tanto `P2034` como el `P2028` observado empíricamente) para la carrera de cantidad devuelta, reintegro prorrateado por el descuento real de la venta, `Debt` de ventas MIXED ahora se reduce igual que CREDIT, `GET /api/returns` exige el mismo rol que `POST`, reintegro en efectivo exige caja abierta (mismo patrón que `createSale`), y devoluciones ahora quedan en `AuditLog` (`RETURN_CREATED`, mismo patrón fire-and-forget que `/api/sales`). RET-FIX-06 (devolver venta de Servicio) queda diferido — no es una decisión de código, es una pregunta de producto para Diego, documentada en `ordenestest.md` sin implementar.

**Verificado por el Ingeniero Líder contra el diff real:** confirmé que `SaleItem.unitPrice`/`total` siempre son precio de lista sin descontar (nunca se persiste el descuento por línea), así que el prorrateo por el ratio `discountAmount/totalAmount` de la venta completa es la única forma correcta de reconstruir el precio neto devuelto dado el modelo de datos actual — matemática verificada a mano contra el test ("prorates the refund by the sale's discount": 2 unidades a $25 con 20% off, se devuelve 1 → $20.00 exacto). El test de concurrencia usa `refundMethod: "Tarjeta"` a propósito para aislar la carrera de cantidad de la precondición de caja abierta — dos devoluciones simultáneas de la cantidad completa, una gana, la otra recibe `ReturnConcurrentConflictError`, stock vuelve a 10 una sola vez, `ReturnItem` suma exactamente 2 (no 4). Confirmé que el precedente de `P2028` bajo escritura concurrente ya existía en este código (`debt-payment-data-access.ts`, ver `PENDIENTES.md` cerrados QA-07) — no es un dato inventado. Leí los 2 archivos de test nuevos/modificados contra lo que el reporte afirmaba cubrir, coincide exacto. `typecheck`/`lint` re-corridos de forma independiente, limpios. Sin hallazgos este ciclo. RET-FIX-01..07 queda cerrado; el hallazgo de MIXED/Debt que estaba abierto en `PENDIENTES.md` desde antes de que existiera el rol formal pasa a Cerrados.

### 2026-08-31 — POS-FIX-01..04: descuentos, promociones, permisos GET /api/sales, doble confirmación de cotización (VERIFICADO)
Ejecutados los 4 bugs confirmados por INGENIERODETESTEO en `ordenestest.md` (commit `481852f`), el primer ciclo completo de esta nueva dinámica. Los descuentos manuales de ítem y el descuento global del POS, que `pos.tsx` ya calculaba y mostraba pero el backend ignoraba, ahora se validan y aplican en `createSale`; `discountAmount`/`promotionIds` dejaron de confiarse del payload del cliente y se recalculan server-side contra promociones reales vía `promotion-engine.ts` (el mismo motor que ya usaba el preview `/api/promotions/apply`), descartando en silencio cualquier `promotionId` inválido sin rechazar la venta. `GET /api/sales` ahora exige el mismo `requireRole(["OWNER","CASHIER"], "pos")` que `POST`. `confirmQuote` pasó a un `updateMany` guardado por `status: {not: "CONFIRMED"}` + chequeo de `count`, y lanza `QuoteAlreadyConfirmedError` dentro de la misma transacción — Prisma revierte sale/stock/movimientos ya creados en ese intento si pierde la carrera.

**Verificado por el Ingeniero Líder contra el diff real (no el self-report):** comparé línea por línea el orden de cálculo de descuentos del backend (`sale-data-access.ts`) contra el orden de cálculo que ya mostraba `pos.tsx` en pantalla (líneas 824-851) — coinciden exactamente, así que el bug real (cajero ve un total con descuento, backend cobraba el precio de lista) queda genuinamente cerrado, no parchado por encima. Confirmé que `QuoteAlreadyConfirmedError` está definida, exportada y mapeada a 409 en ambas rutas de cotización. Leí los 4 archivos de test nuevos/modificados — cubren exactamente lo que el reporte afirmaba (403 en GET, clamps de descuento por ítem y global, stacking, promoción inexistente ignorada, recalculo server-side ignorando un `discountAmount` forjado, confirmación única + rechazo secuencial + carrera concurrente de cotización con `Promise.allSettled`). Re-corrí `npx tsc --noEmit` y `npm run lint` de forma independiente — ambos limpios. Sin hallazgos este ciclo: todo lo revisado estaba correcto y, en varios puntos, más acotado (clamps de `Decimal` en múltiples capas) de lo que sugería solo la prosa del reporte. POS-FIX-01..04 queda cerrado.

Nota aparte (no bloquea el cierre): el commit quedó co-autorado como "Claude Sonnet 5", distinto de "Claude Opus 4.8" en todos los commits previos del agente ejecutor — vale confirmar con Diego si es un cambio intencional de modelo/herramienta en VS Code.

### 2026-08-31 — Nuevo rol formalizado: INGENIERODETESTEO
Diego formalizó la metodología de auditoría proactiva (ver entrada anterior) como un rol nuevo del equipo, con nombre e identidad propia: **INGENIERODETESTEO**. Protocolo completo en `TAREAS/INGENIERODETESTEO.md` (constitución del rol) y en el skill `solven-edge-case-auditor` de Cowork (misma metodología, invocable por nombre). Trabaja sección por sección (POS → Caja → Inventario → Devoluciones → Deudas → Cotizaciones → Reportes/ARCA → Promociones → Usuarios), leyendo código real, nunca hipotetizando sin verificar. Acumula sus hallazgos en un archivo nuevo, único y permanente — **`TAREAS/ordenestest.md`** (no uno por hallazgo como los `FIX-NN.md`) — clasificados en bug confirmado / verificado-correcto / inconcluso. El Ingeniero Líder revisa ese archivo y dejará la nota de cada ciclo acá, en `REPORTELIDER.md`, igual que con cualquier otra orden; cuando el agente de VS Code ejecute los bugs confirmados que queden ahí, pasan por el mismo ciclo de verificación de diff real de siempre. `ordenestest.md` ya arrancó con el hallazgo de la venta MIXED sin reducir deuda (ver entrada anterior), migrado al nuevo formato como primer ejemplo. Tareas manuales e integraciones externas (T3/T5/T2, Rebill, ARCA homologación, Resend, Cloudflare) quedan explícitamente diferidas a octubre — septiembre es para esta nueva dinámica de auditoría proactiva.

### 2026-08-31 — Nueva metodología: auditoría proactiva de edge cases (antes del testeo manual)
Diego propuso adelantarse a fallos que encontraría un tester humano: un rol de auditoría que, sección por sección (empezando por POS → Caja → Inventario, mismo orden que `src/modules/`), razona escenarios de negocio no triviales contra el código real (no contra suposiciones) y los deja documentados como una orden más para el agente de VS Code. Se guarda como skill de Cowork para que sea repetible entre sesiones. Primer resultado, ya en producción de ideas: pensando el caso que dio Diego como ejemplo (venta con pago dividido, devolución parcial al día siguiente), se encontró un bug real y confirmado — `processReturn` (`src/modules/returns/index.ts:317`) no reduce la `Debt` de una venta `MIXED` al procesar una devolución (solo lo hace para `CREDIT`), gap que `FEATURE-01` introdujo sin que la verificación de ayer lo cubriera porque no revisé la interacción con devoluciones. Documentado en `PENDIENTES.md` como Crítico, junto con un gap de validación relacionado (reintegro no se valida contra el desglose original de pago dividido) marcado como a confirmar con Diego, no como bug confirmado.

### 2026-08-31 — FEATURE-01 ejecutado y verificado: venta a crédito/fiado (CREDIT + MIXED)
El agente de VS Code implementó la feature más grande del backlog en 5 commits (`cb45e3d`..`aa3ee45`). Verificación línea por línea del Ingeniero Líder en las partes de plata: el monto cobrado se deriva en el servidor (nunca confía en un "fiado" que mande el cliente), `Debt` + `CashMovement` reconcilian exacto con el neto en los 3 tipos de pago (confirmado también en Reportes vía `saleCashPortion`/`saleCreditPortion`), el límite de crédito rechaza correctamente y su rollback no deja deuda huérfana ni descuenta stock (hay test que lo prueba), el bypass de OWNER usa el rol de sesión del servidor (no falseable), y `customerId` se valida contra `tenantId` antes de crear la deuda. `typecheck`/`lint` reverificados limpios; no se pudo correr `npm test` en este sandbox (rollup en Linux, limitación ya conocida) pero se leyeron los 4 tests de integración nuevos contra lo que afirman cubrir. Dos decisiones del agente no marcadas como "a revisar" en su propio reporte (a diferencia del límite de crédito, que sí lo estaba): `dueDate` de la deuda hardcodeado a 30 días, y una venta MIXED que no se cuenta en el subtítulo "N ventas" de dos tarjetas de Reportes (el monto sí reconcilia bien). Ninguna de las dos es un bug — quedan anotadas en `PENDIENTES.md` como notas de producto/UX menores, no bloquean el cierre. FEATURE-01 queda cerrado; orden archivada.

### 2026-08-30 — FIX-15 + FIX-16 ejecutados y verificados (sin hallazgos)
El agente de VS Code ejecutó los 11 fixes en 11 commits (`8677d2a`..`9200aa7`), uno por bug. Verificación del Ingeniero Líder commit por commit contra el diff real: categoría real de producto en Reportes (agregó `categoryName` al select de `listSales`, confirmado que existe en schema), "Mi Negocio" oculta el formulario completo (no solo la barra) para no-Owner con 403, placeholder de agradecimiento correcto, SKU ya no bloquea guardado, papelera del carrito pide confirmación, "Devoluciones" en Cierre de Caja usa un subtotal derivado de `sessionOutMovements` sin duplicar el descuento en "Total esperado" (verificado que `totalCashOut` ya las incluía), indicador de caja con eventos open/close. Cotización limpia contacto al cambiar cliente, selector de IVA en producto (backend ya lo soportaba), IVA del carrito del POS usa la misma fórmula exacta que `handlePrintInvoice` (comparado línea por línea), botón de factura distingue CAE real, y unificación de carritos suspendidos en `localStorage` con migración automática del formato viejo (revisado el guard `suspendedHydratedRef` para confirmar que no pisa un borrador existente al montar). `typecheck`/`lint` reverificados limpios de forma independiente. Sin hallazgos — a diferencia de FIX-14, este ciclo no tuvo bugs no reportados. FIX-15 y FIX-16 quedan cerrados; ambas órdenes archivadas.

### 2026-08-30 — Archivado `FIX-14_*.md` y `QA_REPORTE.md` (QA-CHROME-01, ciclo 29-07 al 30-08)
Con FIX-14 cerrado y los 13 hallazgos restantes ya triageados uno por uno (7 en `FIX-15`, 4 en `FIX-16`, 1 falso positivo, 1 no concluyente, todos documentados en `PENDIENTES.md`), se confirmó que los 16 hallazgos de `QA_REPORTE.md` están 100% cubiertos en otro lado (FIX-14 cerrado, FEATURE-01, FIX-15, FIX-16, PENDIENTES.md). Se archivaron ambos archivos (`git rm`) — el detalle técnico queda preservado en el historial de git y en estas notas. `TAREAS/` queda solo con las órdenes vigentes: `FEATURE-01`, `FIX-15`, `FIX-16`.

### 2026-08-30 — FIX-14 ejecutado y verificado (con 1 bug crítico corregido antes del cierre)
El agente de VS Code ejecutó FIX-14 (commits `50eddaa`, `30eecc9`, `5fb870c`): monto neto en `CashMovement`/reportes/cierre de caja, y Ajustes oculto por defecto para CASHIER/INVENTORY/READONLY. Verificación del Ingeniero Líder contra el diff real (no el self-report) encontró que el helper `saleNet` agregado en `src/app/ui/reports.tsx` se llamaba a sí mismo en vez de leer `totalAmount` — recursión infinita que crashea cualquier tab de Reportes (RangeError: Maximum call stack size exceeded) en ~20 call sites. No lo detectaba typecheck/lint/tests porque es válido en tipos y no hay test de render para `reports.tsx`. Corregido en `62abb5a` (una línea), typecheck y lint verificados limpios de nuevo tras el fix. FIX-14 queda cerrado.

### 2026-08-30 — Verificación de los 13 hallazgos QA restantes (11 confirmados, 1 falso positivo, 1 no concluyente)
Ingeniero Líder verificó contra código real (no solo el texto del QA_REPORTE) los 13 hallazgos Alto/Importante/Bajo que quedaron pendientes de QA-CHROME-01. 11 confirmados con causa raíz exacta — 7 pasaron a `FIX-15` (bugs menores independientes: categoría en Reportes, "Mi Negocio" 0/8 por 403 silencioso, placeholder de agradecimiento, SKU obligatorio, carrito sin confirmar, devoluciones no reflejadas en cierre de caja, indicador de caja sin refrescar tras evento sin listener) y 4 a `FIX-16` (cotización no limpia contacto, IVA por producto + ticket "0%", botón "Imprimir factura" sin distinguir de fiscal real, dos mecanismos de carrito suspendido inconsistentes). 1 falso positivo (selección de vendedor — el `disabled` sí funciona). 1 no concluyente (promo automática — el código parece correcto, necesita reproducción en vivo antes de ordenar un fix). Ver detalle completo en `PENDIENTES.md` (QA-CHROME-01-b/c).

### 2026-07-29 — QA-CHROME-01: recorrido completo de cliente real en producción (tenant demo, ARCA off)
Un agente de Chrome operó el tenant demo (`seed_tenant_demo`) simulando un comerciante real: catálogo, caja, ventas, cliente/deuda, cotización, devolución, promoción, cierre de caja, reportes, permisos. 14 hallazgos en `TAREAS/QA_REPORTE.md`. **Verificado por el Ingeniero Líder (3 Críticos, contra código real, no solo el texto del reporte):** (1) no existe venta a crédito/fiado real — el schema soporta `CREDIT/MIXED` pero `sale-validation.ts` fuerza siempre `CASH`; (2) `CashMovement`/reportes/cierre de caja usan `totalAmount` bruto sin restar `discountAmount` en ventas con promoción (confirmado en `sale-data-access.ts`); (3) roles no-Owner (`CASHIER`/`INVENTORY`/`READONLY`) tienen acceso a Ajustes por defecto — el permiso falla abierto y `DEFAULT_HIDDEN` solo restringe a SUPERVISOR. Bugs 2 y 3 pasaron a `FIX-14` (fix acotado). Bug 1 (venta a crédito) queda pendiente de decisión de producto con Diego antes de ordenar la implementación — no es un fix chico, es una feature a medio construir. Los hallazgos Alto/Importante/Bajo restantes quedan en `PENDIENTES.md` sin verificar todavía contra código.

### 2026-07-23 — DESIGN-01 + DESIGN-02: mergeado a main (CERRADO)
**Verificado por el Ingeniero Líder (2026-07-23):** diff completo de la rama vs `main` revisado — cero cambios en `src/modules`, `src/app/api`, `prisma`, `middleware.ts`, `tenant.ts`, `auth.ts`; sidebar oscuro intacto; grep de mojibake sobre todo `src/` da 0; texto de `settings.tsx`/`sales-list.tsx` comparado línea por línea contra `main`, idéntico salvo clases; `typecheck`/`lint` re-corridos de forma independiente, limpios. Diego revisó el preview de Vercel (deploy `dpl_FD689G1hR9peMavCdoizmbqpD4wi`) y aprobó el resultado visual. Mergeado a `main` en `b0d9b0d`. Órdenes archivadas (`git rm`), movido a Cerrados en `PENDIENTES.md`. Falta que Diego pushee desde su máquina para que el deploy de producción tome efecto.

### 2026-07-23 — DESIGN-02: correcciones sobre DESIGN-01 (misma rama)
2 correcciones puntuales sobre `design/pulido-estilo-fable`, sin rama nueva. (1) Estilo: `role-permissions-table.tsx` era una de las 4 pantallas sin tocar en DESIGN-01 — se le aplicó el lenguaje (contenedor de tabla `rounded-2xl border-slate-100 bg-white shadow-sm`, título a `text-slate-900`), consistente con el `UsersList` de al lado. Las otras 3 "pendientes" no requerían cambio: `help-page.tsx` y `unanswered-queries.tsx` son stubs de una línea (sin markup) y `user-avatar.tsx` ya cumplía (`rounded-full`, violet-600) — documentado, no forzado. (2) Bug real de encoding: la pasada mecánica de DESIGN-01 había corrompido el texto en español (mojibake UTF-8→CP1252) en `settings.tsx` y `sales-list.tsx`; revertido de forma determinística y verificado a nivel de bytes que el texto quedó idéntico a `main` (solo difieren las clases). `sales-list` estaba doble-codificado a nivel de bytes. `lint`/`typecheck`/`test` limpios (323 passed / 2 skipped). Sin mergear — sigue esperando el preview de Vercel. Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-23 — DESIGN-01: pulido visual de estilo en toda la app (Fable, una sola pasada)
Lenguaje de estilo único definido en `docs/estilo-ui.md` (tarjetas `rounded-2xl border-slate-100 shadow-sm p-5`, dropdowns `rounded-xl shadow-lg`, controles `rounded-lg`, KPIs `font-semibold tracking-tight`; `rounded-md` eliminado) y aplicado a las 29 pantallas con clases en `src/app/ui/` + canvas `slate-50` en el área de contenido del shell. Solo clases Tailwind y CSS base — cero lógica, rutas, API o schema; sidebar oscuro, PDFs y NoaChat intactos. `lint`/`typecheck`/`test` limpios (323 passed / 2 skipped). En rama `design/pulido-estilo-fable`, sin mergear — esperar revisión del preview de Vercel por Diego. Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-22 — FIX-13: seed idempotente, 401 JSON en middleware para rutas API, CRON_SECRET obligatorio fuera de development
3 fixes independientes: `scripts/seed-icase.mjs` usa `upsert` en vez de `create` (evita `P2002` al re-correr contra DB poblada); `src/middleware.ts` devuelve JSON (401/402) en vez de redirect cuando la ruta es `/api/*`, sin cambiar el comportamiento de páginas; los 3 cron routes ahora rechazan si falta `CRON_SECRET` fuera de `development`, en vez de dejar pasar todo. Sin tests previos en los archivos tocados (no se agregaron nuevos, por regla de la orden). `typecheck`/`lint`/`test` sin errores (323 passed, 2 skipped). Detalle en `REPORTE_DE_CAMBIOS.md`.

**Verificado por el Ingeniero Líder (2026-07-22):** diff de `551ac74` confirmado línea por línea contra la orden — los 3 fixes son exactamente lo pedido, sin cambios fuera de alcance. `typecheck` reverificado independientemente (limpio). Orden archivada (`git rm`). Los 3 ítems (QA-01/02/04) se movieron a Cerrados en `PENDIENTES.md`.

### 2026-07-18 — FIX-12: requireTenantId() ahora devuelve 401 en vez de propagar la excepción
Hallazgo de TESTS-01: `subscription/route.ts:7` y `dashboard/summary/route.ts:7` llamaban `requireTenantId()` fuera de todo try/catch, así que sin sesión el `UnauthorizedError` se propagaba sin capturar (probable 500 en vez del 401 esperado). Envuelto con el patrón estándar del proyecto en ambos archivos; test existente de `subscription` actualizado para esperar 401 en vez de la excepción propagada. No se tocó ninguna otra lógica. `typecheck`/`lint`/`test` sin errores (323 passed, 2 skipped). Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-18 — TESTS-01: cobertura para quotes, reports, users, subscription, webhooks
Los 5 módulos que `CLAUDE.md` sección 11 listaba sin ningún test ya tienen cobertura base (camino feliz + errores principales), 69 tests nuevos, solo archivos de test — ningún archivo de producción tocado. Hallazgo sin corregir (regla de la orden): `src/app/api/subscription/route.ts:7` llama `requireTenantId()` fuera de try/catch, así que sin sesión la respuesta no es un 401 sino un rechazo no capturado (probable 500 en producción); mismo patrón en `dashboard/summary/route.ts:7`, fuera de alcance. `typecheck`/`lint`/`test` sin errores (323 passed, 2 skipped). Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-18 — FIX-11: cambio de contraseña ahora funciona de verdad
Bug documentado en `CLAUDE.md` sección 5: el endpoint no verificaba sesión, comparaba contra la env var global `SOLVEN_PASSWORD` en vez del hash real del usuario, y nunca escribía `newPassword` en la DB — mentía éxito. Reescrito para usar `getSession()`, verificar contra `user.password` con `verifyPassword()`, y persistir con `hashPassword()` + `prisma.user.update({ where: { id, tenantId } })`. Se mantuvo el shape de error como string plano (no el helper `_shared/responses`) porque `settings.tsx` ya lo parsea así. Test nuevo con 4 casos. `typecheck`/`lint`/`test` sin errores (258 passed, 2 skipped; 1 falla de conexión a Neon en la corrida completa confirmada flaky y no relacionada, aislada pasó limpio). Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-18 — FIX-10: ivaRate de Servicios y Presupuestos, ya no hardcodeado en 0.21
Bug documentado en `CLAUDE.md` sección 5. `Service` y `QuoteItem` no tenían campo `ivaRate` — cualquier venta o presupuesto con un servicio facturaba con IVA 21% fijo sin importar la alícuota real. Se agregó `ivaRate Float @default(0.21)` a ambos modelos (migración aplicada contra Neon, solo `ADD COLUMN`, sin tocar datos existentes), UI de servicios con selector de alícuota reutilizando `IVA_RATES` de products, y se reemplazó el `0.21` fijo en `sale-data-access.ts`, `quote-data-access.ts` y `pos.tsx` por el valor real del servicio/producto. No se tocó `src/lib/arca/*` ni `src/modules/invoices/*` (ya leen `ivaRate` por ítem). `typecheck`/`lint`/`test` sin errores (254 passed, 2 skipped). Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-18 — FIX-08: ARCA ya no confía en items/total del cliente
Hallazgo de máxima prioridad del Ingeniero Líder (bug documentado en `CLAUDE.md` sección 5, nunca corregido). `emitInvoice()` ahora carga la venta real desde la DB con `where: { id, tenantId }` (rechaza `saleId` de otro tenant o inexistente) y recalcula `items`/`total` del comprobante desde `sale.items`/`sale.totalAmount` — `input.items`/`input.total` se eliminaron del tipo `EmitInvoiceInput`, imposible volver a pasarlos por error. `docTipo` en la API ahora se valida contra los 3 valores permitidos. No se tocó `src/lib/arca/*` (WSAA/WSFE intactos). Reconciliación exacta por construcción: `totalAmount` ya es la suma de los ítems, sin necesidad de ajuste. Tests nuevos desde cero (no existían): 15 tests (5 unitarios de `emitInvoice` + 10 de la ruta), todo mockeado, nada pega a AFIP real. `typecheck`/`lint`/`test` sin errores (250 passed, 2 skipped). Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-17 — UI-02: buscador, filtro de motivo, resumen y badge en historial de devoluciones
Pestaña "Historial" de Devoluciones ganó buscador (folio o cliente, con debounce), filtro por motivo, 2 chips de resumen y badge de color por motivo en cada fila. El folio fabricado (fragmento del CUID) se reemplazó por el folio real de la venta, plomado desde `listReturns()`/`getReturnById()`. El chip de monto es de la página actual, no del período completo (`listReturns` no agrega el total period-wide) — se etiqueta explícitamente "en esta página" para no confundir. `typecheck`/`lint`/`test` sin errores (235 passed, 2 skipped). Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-17 — FIX-07: método de reintegro al procesar una devolución
Bug real reportado por Diego: toda devolución generaba un `CashMovement` en Efectivo sin importar cómo se pagó realmente la venta, porque `Sale.paymentType` siempre es `"CASH"` aunque el pago haya sido mixto. Se agregó `Return.refundMethod` (nullable, sin enum) y un selector en el formulario; el `CashMovement` sólo se crea si el reintegro elegido es Efectivo. La rama de ventas a crédito no se tocó. `GET /api/sales` ya traía `paymentDetails` sin cambios (usa `include`). `typecheck`/`lint`/`test` sin errores (235 passed, 2 skipped). Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-17 — UI-01: Devoluciones pasó de menú principal a pestaña dentro de Ventas
Reorganización de UI pedida por Diego, no un bug. Se sacó "Devoluciones" del nav y de su ruta standalone (`/returns`, eliminada), y ahora vive como tercera pestaña en `Pos` (`Venta actual` / `Historial` / `Devoluciones`), reutilizando el componente `Returns` sin su header de página completa. La visibilidad por rol se preservó exacta (misma fórmula que `visibleNavItems` de `app-shell.tsx`). `typecheck`/`lint`/`test` sin errores (228 passed, incluida la prueba de concurrencia que QA-FIX-06 dejó estable). Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-17 — Cierre y archivo de QA_REPORTE.md (ciclo de QA 1)
Releído completo el reporte original de QA (10 hallazgos: 2 críticos, 3 altos, 4 medios + 1 surgido como efecto colateral, 2 bajos). Los 9 que eran bugs reales quedaron resueltos y verificados vía QA-FIX-01 a 04; el único punto que sigue "abierto" (gastos recurrentes sin pantalla de gestión) nunca fue un bug — es una feature fuera de alcance, a criterio de Diego. Se archiva `TAREAS/QA_REPORTE.md`: el detalle técnico completo de cada hallazgo queda preservado en el historial de git de los commits QA-FIX-01 a 04. Ciclo de QA 1 cerrado por completo.

### 2026-07-17 — QA-FIX-06: error crudo de Prisma en pagos de deuda concurrentes
Cierra el riesgo detectado al verificar QA-FIX-05. Se reprodujo el código real (`P2028`, "Unable to start a transaction in the given time") en 5 de 10 corridas aisladas del test de concurrencia; en las 10 corridas la integridad de datos se mantuvo intacta (sin sobrepago real). Se amplió el `catch` de `registerDebtPayment` para tratar `P2028` igual que `P2034` (reintento y luego `DebtPaymentAmountError`), sin tocar el `updateMany` que protege la plata. `typecheck`/`lint` sin errores; test corrido 5 veces tras el fix, 5/5 OK. Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-17 — QA-FIX-05: POS no volvía a pedir vendedor/comprobante tras cobrar
Bug nuevo (fuera del ciclo de QA, reportado por Diego en uso real). `submitSale()` en `pos.tsx` reseteaba el carrito tras una venta pero no `saleGateResult`, permitiendo cobrar de nuevo con el vendedor/comprobante de la venta anterior sin pasar por `SaleGateModal`. Fix de una línea (`setSaleGateResult(null)`), un solo archivo. `typecheck`/`lint` sin errores; `npm test` con la misma falla de concurrencia preexistente y no relacionada de QA-FIX-04 (esta vez también falló aislada, no solo en el suite completo — a seguir de cerca). Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-16 — QA-FIX-04: ocultar CashRegisterIndicator para roles sin acceso a Caja
Cierra el último ítem abierto del ciclo de QA 1. `CashRegisterIndicator` (`app-shell.tsx`) ahora recibe `role`/`rolePermissions` y no renderiza nada (ni dispara los `fetch`) para roles sin acceso configurado a `cashMovements`, con el mismo default `OWNER`/`CASHIER` que usa el backend desde QA-FIX-03; `OWNER`/`CASHIER` no cambian. Sin test nuevo (no hay infraestructura de tests de componentes React en el proyecto). `npm run typecheck`/`lint` sin errores; `npm test` con una falla de concurrencia preexistente y no relacionada (confirmada flaky, pasa aislada). Detalle en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-16 — QA-FIX-03: cierre de los 8 hallazgos 🟠/🟡/🟢 restantes del ciclo de QA
Los 8 ítems se resolvieron sin N/A: `GET` de Caja/Ajustes ahora exige rol (con tests 403 nuevos), scripts de seed/reset apuntan a un tenant demo fijo por id, columna/buscador de Clientes usan email/teléfono reales, el cron de cotizaciones usa la plantilla correcta, "Subir logo" en Ajustes ya persiste de verdad, se eliminó un test de venta a crédito desactualizado (cobertura ya existe en otro archivo), `/debts` y `/debt-payments` quedaron bajo la sección `customers` de RolePermission, y el límite de crédito en el panel de pago ya es el real del cliente. Se encontró un efecto colateral no pedido en el ítem 1: `CashRegisterIndicator` en `app-shell.tsx` llama a `/api/cash-movements` para todos los roles sin distinción, así que `SUPERVISOR`/`INVENTORY`/`READONLY` ahora ven un saldo de caja incorrecto en el sidebar (no crashea, solo muestra el monto de apertura) — no se tocó `app-shell.tsx` por estar fuera del alcance del lote, queda documentado para decisión de Diego. `npm run typecheck`, `npm run lint` y `npm test` (228 passed, 0 fallas) sin regresiones. Detalle completo en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-16 — QA-FIX-02: RolePermission conectado a `requireRole` (decisión de Diego)
`requireRole(roles, section?)` ahora consulta `RolePermission` para bloquear roles no-OWNER cuando hay una fila `canAccess:false`; OWNER nunca puede quedar bloqueado. Se mapearon 27 endpoints de escritura a 6 de las 10 secciones (customers, products, cashMovements, quotes, returns, pos) siguiendo el mismo criterio que `app-shell.tsx` usa para la navegación; `promotions` y `settings` quedan sin efecto real porque sus endpoints son todos OWNER-only. Dos casos ambiguos (`/api/debt-payments`, `/api/debts`) quedaron sin sección y marcados "pendiente de confirmar con Diego". Typecheck y tests (220 pass, 2 fallas preexistentes no relacionadas) corridos dos veces sin regresiones. Detalle completo en `REPORTE_DE_CAMBIOS.md`.

### 2026-07-16 — QA-FIX-01: 9 endpoints sin control de rol, corregidos
Se agregó `requireRole(...)` a los 9 endpoints de escritura (debts, customers/[id], categories, subcategories, services, recurring-expenses) que sólo validaban tenant, no rol — cualquier usuario autenticado podía escribir sin importar su permiso. Verificado con typecheck, tests (212 pass, 0 regresiones) y prueba en vivo con token READONLY: los 9 devuelven 403. Detalle completo en `REPORTE_DE_CAMBIOS.md`.
