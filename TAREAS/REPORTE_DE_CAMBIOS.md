# REPORTE DE CAMBIOS — SOLVEN

> Actualizado automáticamente por Claude (Código) después de cada tarea.
> Al final del día Diego dice "revisá el reporte" → Claude marca en Notion + borra este archivo.

---

<!-- El agente irá agregando reportes aquí debajo, del más reciente al más antiguo -->

### TAREA 128 — ✅ Completada
- Qué se hizo: se agregó el modelo `ExpenseBudget` (`tenantId`, `category`, `monthlyLimit Decimal(12,2)`, `@@unique([tenantId, category])`) para configurar un presupuesto mensual por categoría de gasto. Se creó el módulo `src/modules/expense-budgets/` (validación + data access, siguiendo el patrón exacto de `src/modules/expenses/`) y la ruta `POST /api/expense-budgets` (upsert, solo `OWNER` vía `requireRole(["OWNER"])`) + `GET /api/expense-budgets` (listar, cualquier usuario autenticado vía `requireTenantId()`, ya que el semáforo debe verse aunque el usuario logueado no sea el OWNER). En `expenses-list.tsx`: se agregó un botón "Configurar presupuestos" (ícono `Settings2`, gateado a `role === "OWNER"` vía el mismo patrón de `fetch("/api/me")` que usa `settings.tsx`) junto al título "Gastos por categoría" en el sidebar, que abre un modal (`BudgetConfigModal`) con un input de límite mensual por cada categoría existente (`uniqueCategories`) y guardado individual por categoría (POST upsert). En el listado de "Gastos por categoría" (`CategoryDonutChart`), se agregó un punto de semáforo (verde <70%, amarillo 70–100%, rojo >100%) junto al nombre de cada categoría, calculado comparando el gasto real del mes actual en esa categoría (nuevo memo `monthCategoryTotals`, agrupado por categoría, separado del memo `categoryStats` existente que sigue siendo histórico/total para no romper el donut) contra `monthlyLimit` (nuevo memo `budgetByCategory`). El semáforo solo se muestra en las categorías que tienen un presupuesto configurado — no se asume ningún límite por defecto.
- Archivos modificados: `prisma/schema.prisma` (+ migración), `src/app/api/expense-budgets/route.ts` (nuevo), `src/modules/expense-budgets/expense-budget-validation.ts` (nuevo), `src/modules/expense-budgets/expense-budget-data-access.ts` (nuevo), `src/modules/expense-budgets/index.ts` (nuevo), `src/app/ui/expenses-list.tsx`.
- Migraciones corridas (si aplica): `20260714090859_add_expense_budget` (nueva tabla, aditiva, sin backfill, corrida sin reintentos) — Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: se decidió no agregar una ruta `DELETE` para quitar un presupuesto configurado (la tarea solo menciona un `route.ts`, y "menos es más") — un presupuesto se puede modificar reemplazando el límite (upsert), pero no eliminar por completo; queda como limitación menor conocida. Se evaluó reusar `expByCategory` de `RentabilidadTab` en `reports.tsx` para el cálculo de gasto mensual por categoría, pero es un `useMemo` privado (no exportado) con una forma de datos distinta (tuplas `[string, number][]`), así que — tal como permite la tarea — se replicó localmente en `expenses-list.tsx` en vez de reutilizarlo. Se evaluó ubicar la pantalla de configuración en `settings.tsx` (que ya tiene un patrón establecido de secciones de configuración), pero se descartó porque la lista de "Archivos afectados" de la tarea solo nombra `expenses-list.tsx`; se implementó como modal dentro de ese archivo para no salirse del alcance indicado. Se corrió el suite completo (`npm test`) antes de commitear: 203 tests OK, 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`, ya documentada en el reporte de la Tarea 148) + 1 falla intermitente por timing de concurrencia en Neon (`debt-payment-data-access.integration.test.ts > prevents concurrent payments from overpaying a debt`, mismo patrón intermitente ya confirmado en aislamiento en la Tarea 142 y visto de nuevo en la Tarea 151) — ninguna de las dos relacionada con los archivos tocados en esta tarea.
- typecheck: OK

### TAREA 151 — ✅ Completada
- Qué se hizo: `SegmentBadge`/`computeSegment` en `customers-list.tsx` ya calculaban el segmento (VIP/Regular/Nuevo/Inactivo) solo a partir de `totalPurchases`, ignorando el campo `Customer.segment` de la BD (editable manualmente desde `customer-detail.tsx`). Se modificó `computeSegment` para recibir el `segment` de BD del cliente y priorizarlo cuando es distinto de `NINGUNO`: `VIP` → "VIP", `RECURRENTE` → "Regular", `NUEVO` → "Nuevo". Si el campo de BD es `NINGUNO` (o no está cargado), se sigue usando el cálculo automático por `totalPurchases` exactamente como antes. Se agregó `segment` al tipo `CustomerRecord` de este archivo (no existía) para poder leerlo. No se tocaron colores, textos ni el diseño visual del badge.
- Archivos modificados: `src/app/ui/customers-list.tsx`.
- Migraciones corridas (si aplica): ninguna (el campo `Customer.segment` ya existía en el schema).
- Algo ya estaba implementado de otra forma / algo quedó pendiente: nada pendiente — se resolvió la inconsistencia documentada en la tarea entre el segmento manual (BD) y el calculado (cliente), sin eliminar ninguno de los dos sistemas. Se corrió el suite completo (`npm test`) antes de commitear: 204 tests OK, 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`, ya documentada en el reporte de la Tarea 148) + 1 falla intermitente por timing de concurrencia en Neon (`debt-payment-data-access.integration.test.ts > prevents concurrent payments from overpaying a debt`, no relacionada con esta tarea — mismo patrón intermitente ya visto en la Tarea 142).
- typecheck: OK

### TAREA 143 — ✅ Completada
- Qué se hizo: en `customers-list.tsx`, cada fila de la tabla ya tenía "Registrar pago" dentro del menú "..." (kebab); se agregó un botón directo visible en la fila (ícono `DollarSign`, ya importado en el archivo), ubicado entre "Editar" y el botón "...", que llama al mismo `onPayment` que ya usaba la opción del menú — mismo estado/modal, sin duplicar lógica. La opción "Registrar pago" del menú "..." se dejó intacta, tal como restringe la tarea.
- Archivos modificados: `src/app/ui/customers-list.tsx`.
- Migraciones corridas (si aplica): ninguna.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: nada — la tarea ya estaba resuelta en su mayor parte (modal y `onPayment` reusados sin cambios), solo faltaba el atajo visual descripto. Se corrió el suite completo (`npm test`) antes de commitear: 204 tests OK, 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`, ya documentada en el reporte de la Tarea 148) + 2 fallas intermitentes por conectividad transitoria a Neon (`customers/route.integration.test.ts`, "Can't reach database server") no relacionadas con este cambio (el archivo modificado es un componente de UI sin acceso a base de datos) — se corrió ese archivo en aislamiento y pasó limpio los 2 tests.
- typecheck: OK

### TAREA 142 — ✅ Completada
- Qué se hizo: en `customers-list.tsx` (tabla) y `customer-detail.tsx` (panel "Contacto"), donde se muestra el teléfono del cliente, ahora es un link `https://wa.me/<solo-dígitos>` que abre WhatsApp en una pestaña nueva (`target="_blank" rel="noopener noreferrer"`), con ícono `MessageCircle` (lucide-react, sin agregar librerías nuevas) para indicar que es clickeable. La limpieza del número (`phone.replace(/\D/g, "")`) reutiliza el mismo criterio que `shareSaleWhatsApp` en `sales-list.tsx`. Si el cliente no tiene teléfono cargado, no se muestra ningún link (se mantiene el "Sin teléfono" existente en la tabla; en el detalle el bloque ya era condicional y sigue sin renderizar nada).
- Archivos modificados: `src/app/ui/customers-list.tsx`, `src/app/ui/customer-detail.tsx`.
- Migraciones corridas (si aplica): ninguna.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: la columna de teléfono en la tabla de `customers-list.tsx` mostraba un número **sintético/falso** (`getCustomerPhone(id)`, generado determinísticamente a partir del id del cliente, sin relación con el teléfono real — mismo problema ya documentado en la Tarea 136). Convertir ese dato falso en un link funcional de WhatsApp habría sido activamente engañoso (abriría WhatsApp hacia un número inventado), así que se cambió la visualización de esa columna para usar el campo real `customer.phone` — esto es parte necesaria de implementar correctamente esta tarea, no un refactor no solicitado. El filtro de búsqueda de la tabla (más abajo en el mismo archivo) todavía usa `getCustomerPhone(c.id)` para buscar por teléfono — se dejó sin tocar por estar fuera del alcance de esta tarea (es un problema de datos preexistente, no de visualización), pero queda como inconsistencia pendiente: la búsqueda por teléfono no encontrará por el teléfono real. Se corrió el suite completo (`npm test`) antes de commitear: 203 tests OK, 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`, ya documentada en el reporte de la Tarea 148) + 1 falla intermitente por timing de concurrencia en Neon (`debt-payment-data-access.integration.test.ts > prevents concurrent payments from overpaying a debt`, no relacionada con esta tarea — se corrió ese archivo en aislamiento y pasó limpio los 4 tests).
- typecheck: OK

### TAREA 138 — ✅ Completada
- Qué se hizo: se agregó `Customer.creditLimit` (nullable, `Decimal(12,2)`, aditivo — sin límite si es null). Se sumó el campo al modal de edición de cliente en `customer-detail.tsx` (input numérico "Límite de crédito", justo antes del selector de Segmento, con nota aclaratoria de que genera una advertencia no bloqueante). La advertencia se implementó en `debts-list.tsx`, en el modal "Nueva deuda" (`CreateDebtModal`): al confirmar, si el cliente tiene `creditLimit` configurado, se suma su deuda actual (`remainingAmount` de sus deudas no condonadas, ya cargadas en la página) + el monto de la nueva deuda; si supera el límite, se muestra `window.confirm` ("Este cliente superaría su límite de crédito de $X — ¿confirmar de todas formas?") — si el cajero cancela, no se envía el formulario; si confirma, la deuda se crea normalmente. No es bloqueante: el dueño puede seguir prestando por encima del límite si lo confirma.
- Archivos modificados: `prisma/schema.prisma` (+ migración), `src/modules/customers/customer-validation.ts`, `src/app/ui/customer-detail.tsx`, `src/app/ui/debts-list.tsx`, `src/app/api/customers/route.test.ts` (fixture de test actualizado con el campo nuevo).
- Migraciones corridas (si aplica): `20260713225929_add_customer_credit_limit` (nullable, sin backfill, corrida sin reintentos) — Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: **la tarea pedía implementar la advertencia en `pos.tsx`, en el flujo de venta con `paymentType: "CREDIT"` — ese flujo no existe en el código actual.** Se verificó que `sale-validation.ts` rechaza en runtime cualquier venta con `paymentType: "CREDIT"` o `"MIXED"` (el comentario en el código dice explícitamente "CREDIT/MIXED solo para registros históricos en BD"), y que `pos.tsx` no tiene ningún concepto de cliente/venta a crédito — todos los pagos del POS deben sumar exactamente el total de la venta (efectivo, tarjeta, transferencia, venta web, otro). El mecanismo real por el cual un cliente "queda debiendo" en SOLVEN es el modal "Nueva deuda" de `debts-list.tsx` (`POST /api/debts`), completamente independiente del POS. Por lo tanto, siguiendo la intención de la tarea (avisar antes de que un cliente supere su límite de crédito al otorgarle más deuda) sin inventar un flujo de venta a crédito que no existe, se implementó la advertencia en ese modal real de creación de deuda en lugar de en `pos.tsx`. Esta decisión se documenta explícitamente por ser una desviación del archivo indicado en "Archivos afectados". `creditLimit` solo se agregó a `UpdateCustomerInput` (no a `CreateCustomerInput`), ya que la tarea pide el campo únicamente en el formulario de edición. Se corrió el suite completo (`npm test`) antes de commitear: 204 tests OK, 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`), ya documentada en el reporte de la Tarea 148.
- typecheck: OK

### TAREA 137 — ✅ Completada
- Qué se hizo: se agregó `Customer.taxId` (nullable, texto libre, aditivo) para búsqueda por CUIT/DNI. Se sumó el campo a `customer-validation.ts` (create y update), al formulario de nuevo cliente (`customer-new-form.tsx`) y al modal de edición en `customer-detail.tsx`, ambos con input de texto simple con ícono `IdCard`, ubicado justo después de "Dirección". En el detalle del cliente se muestra en el panel "Contacto" del sidebar junto al resto de los datos de contacto, solo si está cargado. En `customers-list.tsx` se extendió la búsqueda existente (`filteredCustomers`) para matchear también contra `c.taxId` además de nombre y teléfono. No se agregó validación de formato/checksum de CUIT/DNI — es un campo de texto libre, solo para búsqueda, tal como restringe la tarea.
- Archivos modificados: `prisma/schema.prisma` (+ migración), `src/modules/customers/customer-validation.ts`, `src/app/ui/customer-new-form.tsx`, `src/app/ui/customer-detail.tsx`, `src/app/ui/customers-list.tsx`, `src/app/api/customers/route.test.ts` (fixture de test actualizado con el campo nuevo).
- Migraciones corridas (si aplica): `20260713224538_add_customer_tax_id` (nullable, sin backfill) — tardó tres intentos por timeouts `P1002` de advisory lock en Neon (dos fallos consecutivos, el tercero exitoso); Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: el placeholder del buscador en `customers-list.tsx` ya decía "Buscar cliente por nombre, teléfono o documento..." antes de esta tarea, por lo que no fue necesario modificarlo — ya comunicaba la intención de búsqueda por documento. `taxId` se agregó tanto a `CreateCustomerInput` como a `UpdateCustomerInput` (a diferencia de `birthDate`/`internalNotes`, que solo están en update) porque la tarea pide explícitamente el campo en el formulario de alta y en el de edición. Se corrió el suite completo (`npm test`) antes de commitear: 204 tests OK, 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`), ya documentada en el reporte de la Tarea 148.
- typecheck: OK

### TAREA 136 — ✅ Completada
- Qué se hizo: se agregó un botón "Exportar" en `customers-list.tsx` (junto a "Filtros"/"Limpiar filtros", en la barra de búsqueda) que descarga un CSV `clientes-YYYYMMDD.csv` con columnas Nombre, Teléfono, Email, Código, Segmento, Dirección y Fecha de cumpleaños. Se replicó localmente el mismo patrón ya usado en `sales-list.tsx`/`expenses-list.tsx`/`cash-movements-list.tsx` (`escapeCsvValue` + construcción manual de filas + `Blob` + `URL.createObjectURL` + `<a>` con click programático), sin agregar librerías nuevas. Se exporta `filteredCustomers` (el array ya filtrado por búsqueda/segmento/deuda que se renderiza en la tabla), no la lista completa sin filtrar. El botón se deshabilita si no hay clientes para exportar.
- Archivos modificados: `src/app/ui/customers-list.tsx`.
- Migraciones corridas (si aplica): ninguna.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: `CustomerRecord` en este archivo no tenía `phone`/`email`/`address` (la tabla ya venía mostrando teléfono/email **sintéticos**, generados de forma determinística a partir del `id`/`name` vía `getCustomerPhone`/`getCustomerEmail`, sin relación con los datos reales del cliente — código preexistente, no tocado). Para que el CSV exportado sea útil para el negocio (ej. contactar clientes reales), se agregaron `phone`/`email`/`address` como campos opcionales al tipo `CustomerRecord` y se usan los valores reales devueltos por `/api/customers` (que ya incluye esos campos) solo para el export — la tabla visual sigue mostrando los valores sintéticos como antes, sin cambios, porque arreglar esa inconsistencia preexistente está fuera del alcance de esta tarea. El segmento exportado es el calculado dinámicamente (`customerMetrics[...].segment`, el mismo que se ve en el badge de la tabla), no el campo `segment` guardado en la base. Se corrió el suite completo (`npm test`) antes de commitear: 204 tests OK, 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`), ya documentada en el reporte de la Tarea 148.
- typecheck: OK

### TAREA 135 — ✅ Completada
- Qué se hizo: se agregó `Customer.birthDate` (nullable, `DateTime`, aditivo) para acciones de fidelización. Se sumó un input `type="date"` "Fecha de cumpleaños" al modal de edición de `customer-detail.tsx` (justo antes del selector de Segmento), validado en `UpdateCustomerInput`/`validateUpdateCustomerInput` (acepta `null`/vacío o una fecha parseable; rechaza fechas inválidas con "La fecha de cumpleaños es inválida."). En `customers-list.tsx` se agregó un ícono (`Cake`, violeta) junto al nombre del cliente en la tabla cuando su cumpleaños cae dentro de los próximos 7 días, comparando mes/día contra la fecha actual sin considerar el año (`isBirthdaySoon`, con tooltip "Cumpleaños próximo"). No se implementó ningún envío automático de saludos/mensajes — queda explícitamente fuera de alcance, tal como pide la tarea.
- Archivos modificados: `prisma/schema.prisma` (+ migración), `src/modules/customers/customer-validation.ts`, `src/app/ui/customer-detail.tsx`, `src/app/ui/customers-list.tsx`, `src/app/api/customers/route.test.ts` (fixture de test actualizado con el campo nuevo).
- Migraciones corridas (si aplica): `20260713222337_add_customer_birth_date` (nullable, sin backfill) — el primer intento falló con `P1002` (timeout de advisory lock en Neon), el segundo intento (retry inmediato, sin cambios) fue exitoso. Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: `birthDate` solo se agregó a `UpdateCustomerInput` (no a `CreateCustomerInput`), ya que la tarea pide el campo "en el formulario de edición" y no menciona el alta de cliente — mismo criterio aplicado en las Tareas 126/127. El indicador de cumpleaños próximo solo se muestra en la tabla de `customers-list.tsx` (no se agregó a `customer-detail.tsx` ni a ningún otro lugar) por ser el único archivo indicado en el alcance de la tarea para este propósito. Se corrió el suite completo (`npm test`) antes de commitear: 204 tests OK, 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`), ya documentada en el reporte de la Tarea 148.
- typecheck: OK

### TAREA 127 — ✅ Completada
- Qué se hizo: se agregó `Customer.internalNotes` (nullable, texto libre, aditivo) como memo interno del negocio. Se sumó al modal de edición de `customer-detail.tsx` un `<textarea>` "Notas internas" con una aclaración visual debajo ("Solo visible para el equipo — nunca se muestra al cliente en tickets, PDFs ni emails"). El campo solo se validó/expuso en `UpdateCustomerInput` (se edita desde el modal de edición, no desde el alta de cliente, tal como pide la tarea).
- Archivos modificados: `prisma/schema.prisma` (+ migración), `src/modules/customers/customer-validation.ts`, `src/app/ui/customer-detail.tsx`, `src/app/api/customers/route.test.ts` (fixture de test actualizado con el campo nuevo).
- Migraciones corridas (si aplica): `20260713221119_add_customer_internal_notes` (nullable, sin backfill) — Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: se verificó explícitamente que `internalNotes` no aparece en `quote-pdf.tsx` ni en ningún otro comprobante/documento visible al cliente (grep sobre `src/` solo lo encuentra en `customer-detail.tsx`, `customer-validation.ts` y el test), cumpliendo la restricción estricta de la tarea. Existe un panel "Notas del cliente" preexistente en el sidebar de `customer-detail.tsx` (textarea `readOnly`, sin estado, con placeholder estático y footer falso "Actualizado: hoy por Admin") que no está conectado a ningún campo real — no se tocó por no estar dentro de "Archivos afectados" ni pedido explícitamente por el prompt de la tarea (que pide el textarea únicamente en el formulario de edición). Se corrió el suite completo (`npm test`) antes de commitear: 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`), ya documentada en el reporte de la Tarea 148.
- typecheck: OK

### TAREA 126 — ✅ Completada
- Qué se hizo: se agregó `Customer.address` (nullable, texto libre, aditivo). Se sumó el campo a `customer-validation.ts` (create y update), al formulario de nuevo cliente (`customer-new-form.tsx`) y al modal de edición en `customer-detail.tsx`, ambos con input de texto simple con ícono `MapPin`. En el detalle del cliente, la dirección se muestra en el panel "Contacto" del sidebar (junto a teléfono y email) solo si está cargada. En el PDF de cotización (`quote-pdf.tsx`) se agregó una línea opcional "Dirección" debajo de los datos del cliente, visible solo si `quote.customer.address` existe — se incluyó `customer: true` en el `include` del endpoint `GET /api/quotes/[id]/pdf` para poder leerlo.
- Archivos modificados: `prisma/schema.prisma` (+ migración), `src/modules/customers/customer-validation.ts`, `src/app/ui/customer-new-form.tsx`, `src/app/ui/customer-detail.tsx`, `src/app/ui/quote-pdf.tsx`, `src/app/api/quotes/[id]/pdf/route.tsx`, `src/app/api/customers/route.test.ts` (fixture de test actualizado con el campo nuevo).
- Migraciones corridas (si aplica): `20260713211559_add_customer_address` (nullable, sin backfill) — Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: se mantuvo el modelo de dirección como texto libre (sin separar calle/ciudad/CP), tal como pide la restricción de la tarea. No se tocó `customer.ts` en API routes (`POST`/`PUT`) porque ya pasan el body completo al módulo, así que `address` fluye sin cambios adicionales. Existe código muerto preexistente en `customer-detail.tsx` (`CustomerExtra`/`readCustomerExtra`/`saveCustomerExtra`, un intento anterior de guardar dirección en `localStorage` que nunca se conectó a ningún componente) — no se tocó por estar fuera del alcance de esta tarea ("no modifiques... archivos fuera de los indicados"). Se corrió el suite completo (`npm test`) antes de commitear: 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`), ya documentada en el reporte de la Tarea 148.
- typecheck: OK

### TAREA 125 — ✅ Completada
- Qué se hizo: en el encabezado del perfil de cliente (`customer-detail.tsx`) se agregó un bloque destacado "Deuda pendiente: $X" en texto grande y rojo (`text-3xl font-bold text-rose-700`), ubicado justo debajo del nombre del cliente y antes del `customerCode`/"Cliente desde". Solo se muestra cuando `totalDebt > 0`; si el cliente no tiene deuda no se renderiza nada (no aparece "$0").
- Archivos modificados: `src/app/ui/customer-detail.tsx`.
- Migraciones corridas (si aplica): ninguna.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: `totalDebt` ya existía en el componente (`useMemo` sobre `activeDebts`, derivado del fetch existente a `/api/debts`), por lo que no se agregó ningún fetch ni cálculo nuevo — se reutilizó el valor existente. La `MetricCard` de "Deuda actual" que ya existía en el panel de métricas se dejó sin tocar; el nuevo bloque es un elemento adicional en el encabezado, tal como pide la tarea. Se corrió el suite completo (`npm test`) antes de commitear: 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`), ya documentada en el reporte de la Tarea 148.
- typecheck: OK

### TAREA 150 — ✅ Completada
- Qué se hizo: se agregó `User.pin` (nullable, hasheado con `hashPassword`/`bcryptjs`, igual que `password` — nunca se guarda ni se devuelve en texto plano). En Configuración → Usuarios, cada fila tiene una celda "PIN" con badge "Configurado"/"Sin PIN" + input numérico de 4 dígitos + botón "Guardar" (mismo patrón optimista de `handleRoleChange`/`handleToggleActive`/`handleAvatarChange`, PATCH `/api/users/[id]` con `{ pin }`). En `pos.tsx` se agregó un botón ícono (`UserCog`) "Cambiar de cajero" en el header del POS que abre un nuevo modal (`switch-cashier-modal.tsx`, mismo patrón visual que `SaleGateModal`: grilla de usuarios vía `GET /api/users` → paso 2 ingresar PIN de 4 dígitos). Se creó `POST /api/auth/switch-cashier`: valida sesión activa (`getSession`), valida el PIN contra el usuario seleccionado (`verifyUserPin`, del mismo tenant, usuario activo, nunca pide la contraseña completa) y, si es válido, emite una cookie de sesión nueva vía `createSession(...)` agregando `activeCashierId` como campo separado del `userId` original — el rol/permisos de quien inició sesión no se pierden, `activeCashierId` solo identifica quién está operando la caja en ese momento. `SessionPayload`/`createSession` en `src/lib/auth.ts` se extendieron de forma aditiva (parámetro opcional, default `null`); no fue necesario tocar `login`/`register`.
- Archivos modificados: `prisma/schema.prisma` (+ migración), `src/lib/auth.ts` (`SessionPayload` + `createSession` con `activeCashierId` opcional), `src/modules/users/user-data-access.ts` (`hasPin` en `UserSummary`/select, `updateUserPin`, `verifyUserPin`), `src/modules/users/index.ts`, `src/app/api/users/[id]/route.ts` (rama nueva en `PATCH` para `pin`), `src/app/ui/users-list.tsx` (columna PIN), `src/app/ui/pos.tsx` (botón + integración del modal). Nuevos: `src/app/api/auth/switch-cashier/route.ts`, `src/app/ui/switch-cashier-modal.tsx`.
- Migraciones corridas (si aplica): `20260713194727_add_user_pin` (nullable, sin backfill) — Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: el PIN se administra desde la tabla de usuarios (Configuración → Usuarios, `users-list.tsx`), no desde una pantalla de autoservicio por usuario — coincide con "Archivos afectados" de la tarea, que lista explícitamente `users-list.tsx` y no una pantalla de cuenta propia; hoy solo `OWNER` ve esa tabla. El modal de cambio de cajero no persiste el "cajero activo" en ningún indicador visible fuera del propio POS (no se tocó `/api/me` por no estar en el alcance) — el nombre del cajero activo queda en estado local de `pos.tsx` y se ve como tooltip del botón mientras la pestaña sigue abierta. Se corrió el suite completo (`npm test`) antes de commitear: 204 tests OK, 1 falla preexistente y no relacionada (`sales/route.integration.test.ts > creates a credit sale with debt through the API flow`), ya documentada en el reporte de la Tarea 148.
- typecheck: OK

### TAREA 149 — ✅ Completada
- Qué se hizo: se agregó una pantalla de auditoría de solo lectura sobre datos que ya se estaban grabando (no se tocó `logAudit` ni ninguno de sus call sites). Nueva categoría "Auditoría" en el sub-menú de Configuración → Mi Negocio (`settings.tsx`, patrón `DocumentosSection`), visible únicamente para `OWNER` (la categoría se filtra del sidebar/select móvil y del switch de contenido para cualquier otro rol que llegue a ese panel, ej. `INVENTORY`; el endpoint ya exigía `requireRole(["OWNER"])` de antes). La pantalla muestra tabla con fecha/hora, usuario (userCode), acción traducida a español (ej. "USER_ROLE_CHANGED" → "Cambio de rol"), entidad afectada, con paginación (usa los parámetros `page`/`limit` que ya existían en `GET /api/audit-logs`) y filtros por usuario y por tipo de acción.
- Archivos modificados: `src/app/ui/settings.tsx` (nueva categoría + `AuditLogSection`), `src/app/api/audit-logs/route.ts` (se agregó parámetro de query `userId`, faltaba — la paginación y el filtro por `action` ya existían), `src/modules/audit/audit-data-access.ts` (`listAuditLogs` acepta `userId` opcional en el `where`, mismo patrón que `action`).
- Migraciones corridas (si aplica): ninguna — no hizo falta tocar el schema, `AuditLog.userId` ya existía y ya tenía relación con `User`.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: existe una entrada "usuarios" duplicada y no conectada en el array `CATEGORIES` de `settings.tsx` (cae en `ComingSoonSection`, distinta de la pantalla real de Usuarios que vive en `/settings?s=usuarios`) — es preexistente, no se tocó ni se usó como referencia. No se agregó `SUPERVISOR` al acceso de esta pantalla: en la Tarea 139 se decidió que Supervisor no tiene acceso a Configuración en absoluto (link "Ajustes" oculto), por lo que la aclaración de la tarea ("y SUPERVISOR si la Tarea 139 ya se implementó") no aplica en este caso.
- typecheck: OK

### TAREA 148 — ✅ Completada
- Qué se hizo: se agregó un modelo `RolePermission` (`tenantId`, `role`, `section`, `canAccess`, único por `tenantId+role+section`) que le permite al `OWNER` configurar, por tenant, a qué secciones de navegación puede acceder cada rol — capa adicional de UI/navegación sobre el sistema existente de `hiddenForRoles`, no un reemplazo de la autorización real en los endpoints (ningún `requireRole` existente fue tocado). Nuevo endpoint `GET/PATCH /api/role-permissions`: `GET` abierto a cualquier usuario autenticado (`requireTenantId`, cada usuario necesita leer sus propios permisos para armar su nav), `PATCH` restringido a `OWNER` (`requireRole(["OWNER"])`), valida rol/sección contra listas fijas y bloquea explícitamente que se le quite a `OWNER` el acceso a `settings` (validado tanto en el endpoint como con un checkbox deshabilitado en la UI). `AppShell` ahora filtra `navItems` con una lógica en capas: si existe una fila configurada en `RolePermission` para `rol:sección` se usa ese valor; si no, se usa el fallback hardcodeado existente (`hiddenForRoles`). Se agregó una tabla de checkboxes (`RolePermissionsTable`, nuevo) en Ajustes → Usuarios, debajo de la lista de usuarios, con los mismos defaults hardcodeados que `app-shell.tsx` para que la grilla refleje el acceso real antes de guardar ningún cambio.
- Archivos modificados: `prisma/schema.prisma` (+ migración), `src/app/ui/app-shell.tsx`, `src/app/settings/components/UsuariosPanel.tsx`. Nuevos: `src/app/api/role-permissions/route.ts`, `src/modules/role-permissions/role-permission-validation.ts`, `src/modules/role-permissions/role-permission-data-access.ts`, `src/modules/role-permissions/index.ts`, `src/app/ui/role-permissions-table.tsx`.
- Migraciones corridas (si aplica): `20260713181937_add_role_permission` (tabla nueva, no afecta datos existentes) — Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: antes de commitear se corrió `npm test -- --run` (validación extra por ser una tarea que toca el filtro de navegación usado en toda la app) y apareció 1 falla preexistente y no relacionada: `src/app/api/sales/route.integration.test.ts > creates a credit sale with debt through the API flow` (`responseBody.data` viene `undefined`). Se confirmó con `git stash` que la falla ya existe en el HEAD commiteado *antes* de esta tarea (no la causó ningún cambio de esta sesión) — queda pendiente de investigar/reportar por separado, fuera del alcance de la Tarea 148.
- typecheck: OK

### TAREA 147 — ✅ Completada
- Qué se hizo: se agregó `User.avatarUrl` (nullable) guardado como data URL en base64 (mismo límite de 2MB que el logo del negocio, validado antes de convertir con `FileReader`, tanto en cliente como server-side). En `users-list.tsx` cada fila de la tabla de usuarios muestra el avatar (o iniciales del nombre si no tiene foto) y es clickeable para subir/cambiar la foto, que se guarda vía `PATCH /api/users/[id]` con `{ avatarUrl }`. Se creó un componente compartido `UserAvatar` (`src/app/ui/user-avatar.tsx`) reutilizado también en el selector de vendedor del POS (`SaleGateModal` en `sale-gate-modal.tsx`), que ahora muestra el avatar de cada usuario junto a su código.
- Archivos modificados: `prisma/schema.prisma` (+ migración), `src/modules/users/user-data-access.ts` (nueva función `updateUserAvatar` + `avatarUrl` en `UserSummary`/select), `src/modules/users/index.ts`, `src/app/api/users/[id]/route.ts` (rama nueva en `PATCH` para `avatarUrl`), `src/app/ui/users-list.tsx`, `src/app/ui/sale-gate-modal.tsx`. Nuevo: `src/app/ui/user-avatar.tsx`.
- Migraciones corridas (si aplica): `20260713181535_add_user_avatar_url` (nullable, sin backfill) — Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: no existía un "formulario de edición de usuario" separado (los usuarios se editan inline en la fila de la tabla — rol y estado activo ya funcionaban así), así que la subida de foto se agregó como una celda más de esa misma fila en vez de crear un modal nuevo, siguiendo el patrón existente. El preview roto del logo de `settings.tsx` (`handleFileChange`/`URL.createObjectURL` sin persistir) se dejó intacto — no es parte del alcance de esta tarea y no se usó como referencia de implementación, solo se confirmó que no había otro precedente de subida de imágenes en el proyecto.
- typecheck: OK

### TAREA 139 — ✅ Completada
- Qué se hizo: se agregó el rol `SUPERVISOR` (`OWNER | CASHIER | INVENTORY | READONLY | SUPERVISOR`) con acceso de lectura/escritura a Reportes y Clientes (incluida gestión de deudas/pagos), lectura de Ventas/POS igual que `READONLY` (sin acceso de escritura), y sin acceso a Configuración ni Caja. Se agregó `"SUPERVISOR"` al `requireRole` de `POST /api/customers` y `POST /api/debt-payments`. Se ocultaron los links "Caja" y "Ajustes" del sidebar principal (`hiddenForRoles` en `app-shell.tsx`, mecanismo que existía en el tipo pero no se usaba en ningún item) y los paneles "Mi Negocio"/"Usuarios" del sub-menú de Ajustes (`SettingsNav.tsx`) para este rol. Se agregó "Supervisor" al selector de roles y su badge en `users-list.tsx`, y su etiqueta en `SidebarUser` (`app-shell.tsx`).
- Archivos modificados: `prisma/schema.prisma` (+ migración), `src/app/api/customers/route.ts`, `src/app/api/debt-payments/route.ts`, `src/app/ui/app-shell.tsx`, `src/app/settings/components/SettingsNav.tsx`, `src/app/ui/users-list.tsx`.
- Migraciones corridas (si aplica): `20260713181107_add_user_role_supervisor` (agrega valor al enum, aditivo, no rompe usuarios existentes) — Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: se revisaron los 36 usos de `requireRole([...])` del proyecto (`Reportes` ya estaba abierto a todos los roles vía `requireTenantId`, sin cambios necesarios). Se decidió **no** agregar `SUPERVISOR` a `POST /api/debts/[id]/write-off` — condonar una deuda es más drástico que "gestionar deudas de clientes" (registrar pagos), se interpretó fuera del alcance otorgado y se dejó exclusivo de `OWNER`; si Diego quiere que Supervisor también condone deudas, es un cambio de una línea. Tampoco se agregó a ningún otro `requireRole` (productos, servicios, proveedores, promociones, facturación/ARCA, ajustes de inventario, cotizaciones, devoluciones, gastos, usuarios, audit-logs, envío de email de venta) por no estar contemplado en el alcance de la tarea — Supervisor igual puede *ver* la mayoría de esos datos porque los `GET` correspondientes ya usan `requireTenantId` sin restricción de rol (comportamiento preexistente para todos los roles, no se tocó). No se restringió `GET /api/settings` (Configuración) porque ya era de lectura abierta para todos los roles desde antes (incluido `READONLY`); cambiar eso afectaría a otros roles y la tarea pide no modificar comportamiento existente — se resolvió ocultando el link de navegación en su lugar.
- typecheck: OK
- Fix de seguimiento (mismo día): se detectó que `USER_ROLES` en `src/modules/users/user-validation.ts` no incluía `"SUPERVISOR"`, por lo que crear un usuario o cambiar su rol a Supervisor habría sido rechazado por la validación del servidor pese a que la UI ya lo ofrecía. Corregido en un commit aparte (`fix: agregar SUPERVISOR a la validacion server-side de roles de usuario`).

### TAREA 124 — ✅ Completada
- Qué se hizo: se agregó `User.lastLoginAt` (nullable) y se actualiza en `POST /api/auth/login` justo después de verificar contraseña/estado activo y antes de crear la sesión. `users-list.tsx` muestra una columna "Último acceso" con la fecha formateada o "Nunca" si es null.
- Archivos modificados: `prisma/schema.prisma`, `src/app/api/auth/login/route.ts`, `src/modules/users/user-data-access.ts` (select/tipo `UserSummary`), `src/app/ui/users-list.tsx`.
- Migraciones corridas (si aplica): `20260713180343_add_user_last_login_at` (nullable, sin backfill) — Diego debe correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: no se implementó indicador de "sesión activa" en tiempo real — las sesiones son JWT sin tabla de sesiones en BD (`src/lib/auth.ts`/`src/lib/tenant.ts` no llevan tracking de sesiones activas), y la tarea explícitamente prohíbe construir ese sistema. Solo se muestra "Último acceso".
- typecheck: OK

### TAREA 122 — ✅ Completada
- Qué se hizo: `DELETE /api/users/[id]` ahora cuenta ventas asociadas por `sellerCode` antes de borrar; si hay ventas y no viene `?confirm=true`, devuelve `{ deleted: false, salesCount }` sin borrar. En `users-list.tsx`, el modal de eliminación muestra ese número y cambia el botón a "Sí, eliminar de todas formas" para el segundo click que sí confirma el borrado.
- Archivos modificados: `src/modules/users/user-data-access.ts`, `src/app/api/users/[id]/route.ts`, `src/app/ui/users-list.tsx`.
- Migraciones corridas (si aplica): ninguna (se usa `Sale.sellerCode` existente, sin FK nueva).
- Algo ya estaba implementado de otra forma / algo quedó pendiente: el modal de confirmación ya existía; se reusó agregando el segundo paso condicional en vez de crear uno nuevo.
- typecheck: OK

### TAREA 121 — ✅ Completada
- Qué se hizo: `SidebarUser` en `app-shell.tsx` ahora muestra el rol del usuario (etiqueta corta en español) debajo del nombre, reusando la misma respuesta de `/api/me` que ya se pedía (que ya incluía `role`) sin agregar un segundo fetch.
- Archivos modificados: `src/app/ui/app-shell.tsx`.
- Migraciones corridas (si aplica): ninguna.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: `/api/me` ya devolvía `role`; solo faltaba capturarlo y renderizarlo en `SidebarUser`. Queda pendiente unificar el fetch duplicado a `/api/me` entre `SidebarUser` y `AppShell` (fuera de alcance de esta tarea).
- typecheck: OK

---

Historial de Tareas 101–120 revisado, marcado como completado en Notion y archivado por el Ingeniero Líder — 2026-07-13.
