# REPORTE DE CAMBIOS — SOLVEN

> Actualizado automáticamente por Claude (Código) después de cada tarea.
> Al final del día Diego dice "revisá el reporte" → el Ingeniero Líder verifica contra el diff real, deja su nota en REPORTELIDER.md, y vacía este archivo (no se borra el archivo en sí, se limpia el contenido para el próximo ciclo).

---

<!-- El agente irá agregando reportes aquí debajo, del más reciente al más antiguo -->

### 2026-09-04 — RET-UX-02: flujo de devolución unificado + limpieza de POS

Ejecutada la orden `TAREAS/RET-UX-02_flujo-devolucion-y-pos.md` (Ingeniero Líder), 5 secciones — la Sección 5 se agregó a la orden (commit `ac3fb0a` en `main`) mientras la Sección 4 ya estaba en curso, y se incorporó a esta misma entrega. Cotizaciones, Reportes, Ajustes y el ticket de papel térmico quedaron fuera de alcance, igual que `src/lib/arca/*` — ninguno se tocó.

**Sección 1 — sacar "Más vendidos" del POS.** Removidos de `pos.tsx` el bloque JSX colapsable, los 3 `useState` (`topProducts`, `topProductsLoading`, `topProductsOpen`) y el `useEffect` que pegaba a `/api/pos/top-products`. El endpoint se dejó sin tocar (sin uso, tal como pedía la orden), confirmado con `grep -rn "pos/top-products"` que no queda ninguna otra referencia rota. Los imports `ChevronUp`/`ChevronDown` de `lucide-react` se mantuvieron porque siguen usándose en otros lugares del archivo.

**Sección 2 — unificar "Devolver" desde Historial con el formulario real.** Se eliminó por completo `ReturnModal` (el modal viejo, con solo dos opciones de reintegro y sin las reglas de negocio del formulario bueno) de `sales-list.tsx`, junto con el estado `returningSale` y los tipos que solo usaba. Se levantó estado a `pos.tsx` (`returnPresetSale`) y se conectó `SalesList`→`onReturnRequest`→`Returns` (`presetSale`/`onPresetConsumed`), de forma que el botón "Devolver" del Historial ahora lleva directo a la pestaña "Devoluciones" con la venta ya preseleccionada (`handleSelectSale` + tab interno forzado a `"new"`), sin duplicar lógica de negocio.

**Sección 3 — diagnóstico: venta nueva no visible en Devoluciones.** Se intentó reproducir el caso con una prueba real de código (creada, corrida y luego borrada, nunca commiteada) que ejercitó exactamente las mismas formas de consulta que usan por defecto tanto Historial como Devoluciones (`createSale` seguido de `listSales`, sin filtros). **Resultado: no se pudo reproducir** — la venta apareció en ambas vistas de inmediato. Como pide explícitamente la orden ("No inventes un fix para algo que no pudiste reproducir"), no se tocó código de este flujo. La hipótesis más probable es que la percepción de Diego venía de la falta de preselección automática, ya resuelta en la Sección 2.

**Sección 4 — reintegro por método de pago real (multi-campo).** Reemplazado el binario CASH/Crédito y el `<select>` único de `refundMethod` por un campo de reintegro por cada método real de la venta (`parsePaymentDetails`), con prellenado proporcional (capado al monto original de cada método, "último campo se lleva el resto" para evitar deriva de redondeo), validación de que la suma iguale el total a devolver, tope por método, y campo de N° de operación obligatorio en cada línea de Tarjeta. Backend: `processReturn` pasa de `refundMethod?/refundReference?` a `refundDetails?: {method, amount, reference?}[]`; valida cada método contra los métodos reales de la venta (reemplaza la regla todo-o-nada anterior), valida la suma contra el total en una pasada de solo lectura *antes* de mutar stock/caja, y sostiene un mapa de "ya reintegrado por método" mezclando el `refundMethod` legado y el `refundDetails` nuevo de devoluciones parciales previas de la misma venta, para que dos devoluciones parciales sucesivas no puedan reintegrar dos veces por el mismo canal. El movimiento de caja ahora se crea solo por la suma de las líneas "Efectivo" (no por el total completo). Se agregó `Return.refundDetails Json?` (aditivo, nullable — migración `20260904112338_add_return_refund_details`, aplicada y verificada contra Neon). `route.ts` valida el nuevo shape (método válido, monto positivo, referencia obligatoria en Tarjeta) y hila `refundDetails` a `processReturn`; `logAudit` ahora guarda `refundMethods` (array) en vez de un único `refundMethod`. `ReturnConfirmStep` muestra una línea por cada reintegro real. 3 tests de integración nuevos (reparto entre métodos reales, tope por método excedido, doble reintegro en devoluciones parciales sucesivas) más los existentes actualizados al nuevo shape; se detectó y corrigió a tiempo (antes de correr la suite) que las primeras versiones de 2 de esos tests nuevos no aislaban correctamente la validación que decían probar (una mezclaba el chequeo de suma con el de tope) — corregidos usando una venta `MIXED` para separar ambos casos.

**Sección 5 — sugerencia visible: cobrar la venta nueva antes de devolver la anterior.** Banner informativo (paleta violeta, ícono `Lightbulb`, mismo patrón visual que los banners de éxito/error existentes) arriba del formulario de "Nueva devolución" en `returns.tsx`, con el texto: "Para una devolución más ágil y efectiva, te recomendamos cobrar primero la venta nueva y, una vez cobrada, devolver la totalidad de la anterior." No bloquea ni valida nada — es descartable con un `useState` local (`showChargeFirstHint`), tal como sugería la orden como mejora opcional.

`lint`, `typecheck` y `test` limpios (473 passed / 2 skipped, 74 archivos). La única falla de la corrida completa (4 tests) es **preexistente y no relacionada**: `src/modules/inventory/stock-adjustment.integration.test.ts`, mismo problema ya documentado en el reporte de CAJA-UX-01 (`deleteStockAdjustmentTestData` borra `User` antes que los `AuditLog` que lo referencian) — archivo no tocado en esta orden. Secciones 1+2 en el commit `f61212c`, Sección 4 en `e8f25fe`, Sección 5 en `1c514b9`, los 3 en la rama `design/revision-uiux-sep-2026`.

**No autocertificado como verificado — queda para el Ingeniero Líder en sesión separada**, en particular: (a) verificación visual real de que `SaleRecord` (Historial) y `Sale` (Devoluciones) no generaron ningún desajuste de campos al levantar el estado de preselección en la Sección 2 — se razonó sobre compatibilidad de tipos y pasó `typecheck`, pero no se abrió un navegador; (b) la Sección 3 se cierra como "no reproducible" con una sola técnica de reproducción (test de código exercitando las queries) — si Diego vuelve a ver el síntoma, conviene reproducirlo también a mano en el navegador antes de reabrir la sección; (c) verificación visual del formulario multi-campo de reintegro de la Sección 4 con ventas de 2 y 3 métodos reales, no solo cobertura de integración; (d) confirmar que el prellenado "proporcional, último campo se lleva el resto" es el comportamiento que Diego espera ver, ya que la orden solo pedía "un valor default razonable"; (e) confirmar que el texto exacto del banner de la Sección 5 es el tono/voseo que Diego quiere, la orden aclaraba que el texto podía pulirse.

**Evaluación explícita de `TAREAS/CLAUDE.md` (no editado en esta entrega, a propósito):** la Sección 4 de esta orden deja stale dos referencias puntuales de ese documento, que quedan para que el Ingeniero Líder las verifique y las vuelque él mismo (mismo patrón que el resto de este documento — no es rol del agente ejecutor tocar `CLAUDE.md` directamente): (1) `TAREAS/CLAUDE.md` sección 6, campo `Return.refundMethod String?`, dice **"No se valida contra `Sale.paymentDetails`"** con referencia a `PENDIENTES.md` → `RET-DEV-METODO` — eso ya no es cierto: `processReturn` ahora valida cada método de `refundDetails` contra los métodos reales de `sale.paymentDetails` (o el fallback de Efectivo puro), sin excepción. (2) `PENDIENTES.md` → `RET-DEV-METODO` (sección "Alto") queda resuelto por el mismo motivo y podría moverse a "Cerrados". También hay un patrón arquitectónico nuevo, del mismo tipo que los ya documentados en la sección 3 de `CLAUDE.md` (reserva-antes-de-AFIP, motor de promociones, revalidación de sesión con TTL): validar-antes-de-mutar con una pasada de solo lectura previa a cualquier mutación de stock/caja, y mezclar la representación legada (`refundMethod`/`totalAmount`) con la nueva (`refundDetails` JSON) al sumar lo ya reintegrado de devoluciones parciales previas — podría ameritar su propia entrada si se vuelve a necesitar este patrón en otro módulo.

### 2026-09-02 — CAJA-UX-01: Caja al día + formato profesional de PDFs y CSVs exportables

Ejecutada la orden `TAREAS/CAJA-UX-01_caja-y-formato-pdf.md` (Ingeniero Líder), 2 secciones. Cotizaciones, Reportes y Ajustes (pantallas) quedaron explícitamente fuera de alcance, igual que el ticket térmico del POS (diferido a una orden futura) — ninguno de los dos se tocó.

**Sección 1 — Caja.** (1.1) `cash-register-close.tsx` calculaba Tarjeta y Transferencia como `$0` fijo en el desglose del cierre; ahora `computePaymentMethodBreakdown` (nueva función exportada, con test unitario propio en `cash-register-close.test.ts` ya que este repo no tiene jsdom/RTL para tests de render) recorre `Sale.paymentDetails` de todas las ventas de la sesión y suma por método real, separando además crédito puro y el resto no cobrado de ventas `MIXED`; se releyó `saleNet` explícitamente para descartar el patrón de auto-recursión de FIX-14 (no aplica, la función es correcta). (1.2) El botón "Exportar" de `cash-movements-list.tsx` era un stub (`alert(...)`) — ahora llama a `exportCashMovementsToCsv`. (1.3) 7 strings de tuteo corregidas a voseo en `cash-register-open.tsx` (debés/Ingresá/Verificá/Guardá/Completá/Definí/Confirmá), sin tocar palabras fuera de la lista literal de la orden. (1.4) Se eliminaron 2 botones ocultos sin uso (`Filtros avanzados`, `Ver todas las categorías →`) en `cash-movements-list.tsx`, y el import ya no usado de `Filter`. (1.5) CSS de impresión (`print:hidden`/`print:block`) agregado al flujo "Imprimir cierre" de `cash-register-close.tsx`, siguiendo el patrón ya existente en `ClosedSessionDetailModal` — inputs/textarea interactivos se ocultan al imprimir y se reemplazan por su equivalente en texto plano (span/p hermano), y el `<aside>` con el resumen (oculto por default bajo el breakpoint `xl`) se fuerza visible con `print:block` para que aparezca en el papel sin importar el ancho de pantalla.

**Sección 2 — formato profesional de PDFs y CSVs, toda la app.** (2.1) `report-pdf.tsx` (usado por `export-pdf/route.tsx` para reportes de ventas y productos) ganó: montos formateados con `formatARS` (antes números crudos sin separador de miles ni símbolo), columnas con ancho proporcional (`flex` por columna) y alineación derecha en las numéricas, una fila de totales (cantidad de filas + suma/total relevante), timestamp "Generado el DD/MM/AAAA HH:mm" y numeración "Página X de Y" vía `render={({pageNumber,totalPages})=>...}` sobre una `View fixed` con posición absoluta en el pie — patrón estándar de `@react-pdf/renderer` para pies de página repetidos en documentos multi-página. Las filas de tabla llevan `wrap={false}` para que no se corten a la mitad entre dos páginas. (2.2) Bug de datos en `src/app/api/reports/export/route.ts`: el CSV de productos escribía el string literal `"null"` para `costPrice` nulo (interpolación directa de `null` en un template string) — ahora usa `costPrice?.toString() ?? ""`. (2.3) Los 10 puntos de generación de CSV de la app (1 servidor + 9 cliente) ahora anteponen BOM UTF-8 (`﻿`) para que Excel en Windows no rompa tildes/ñ al abrir el archivo con doble clic. Del lado servidor (`export/route.ts`, tipos "ventas" y "productos") se sumó directamente. Del lado cliente se creó un helper compartido nuevo, `src/lib/csv.ts` (`escapeCsvValue` + `downloadCsv`), y se reemplazó la lógica duplicada de `escapeCsvValue`/`Blob`/`URL.createObjectURL` que cada archivo tenía copiada por su cuenta en los 9 puntos: `sales-list.tsx`, `returns.tsx`, `reports.tsx` (`exportServicesCsv`), `products-inventory.tsx` (plantilla de importación), `InventoryTab.tsx`, `expenses-list.tsx`, `debts-list.tsx`, `customers-list.tsx`, `cash-movements-list.tsx`. No se cambió el delimitador (`,`) en ningún caso, tal como pedía la orden.

`lint`, `typecheck` limpios. `test`: 470 passed / 4 failed / 2 skipped (74 archivos, 1 archivo fallando). Las 4 fallas son **preexistentes y no relacionadas** — las 4 son del mismo archivo, `src/modules/inventory/stock-adjustment.integration.test.ts` (su propia función de limpieza `deleteStockAdjustmentTestData` borra `User` antes que todos los `AuditLog` que lo referencian, violando `AuditLog_userId_fkey`), archivo no tocado en esta orden, confirmado con `git status`/`git log` (último commit sobre ese archivo es `a87c934`, de INV-FIX-01..10) y reproducido de forma determinística corriendo ese archivo solo, dos veces. Commits `8a93a20` (Sección 1) y `f77ee61` (Sección 2) en la rama `design/revision-uiux-sep-2026`, pusheados a origin.

**No autocertificado como verificado — queda para el Ingeniero Líder en sesión separada**, en particular: (a) la falla preexistente de `stock-adjustment.integration.test.ts` — confirmar si ya era conocida o si hace falta abrir una orden aparte para arreglar el orden de borrado en `deleteStockAdjustmentTestData`; (b) verificación visual real de la impresión del cierre de caja (CSS `print:*`) contra un PDF/impresión real, no solo lectura del CSS; (c) verificación visual del PDF de reportes (columnas proporcionales, salto de página con footer fijo) abriendo un PDF generado real, en especial con un dataset lo bastante grande como para forzar más de una página; (d) confirmar que la fila de totales agregada a `report-pdf.tsx` (cantidad + suma para ventas, cantidad + stock total para productos) es la información que Diego espera ver ahí, ya que la orden no especificaba el contenido exacto de esa fila.

### 2026-09-02 — RET-UX-01: búsqueda por documento en Nueva devolución, N° de operación de tarjeta y tarjeta obligatoria si la venta se pagó con tarjeta

Ejecutada la orden `TAREAS/RET-UX-01_devoluciones-nueva.md` (Ingeniero Líder), 4 secciones, todas acotadas a la pestaña "Nueva devolución" de `src/app/ui/returns.tsx` — `ReturnHistoryPanel` (pestaña "Historial") no se tocó, confirmado por grep antes y después.

**Sección 1 — búsqueda de venta por chips, incluyendo documento.** El input único de texto libre se reemplazó por 4 chips ("N° de venta", "Cliente", "Documento", "Fecha"), mismo patrón visual que los chips de categoría de `pos.tsx` (`rounded-full`, violeta activo). Al elegir un chip se muestra un input específico (selector de fecha para "Fecha"); cambiar de chip limpia el valor anterior. Se corrigió el bug de fondo que motivó la orden: `returns.tsx` pedía `/api/sales` sin ningún parámetro de querystring, y como `listSales` pagina con `limit=20` por defecto, las ventas más viejas quedaban inalcanzables por búsqueda aunque el cliente escribiera el término correcto. Ahora se manda `limit=50` (se eligió subir el límite en vez de implementar "cargar más" real, por ser la opción más simple que la propia orden habilitaba explícitamente) más `q=<valor>` para "N° de venta"/"Cliente" (ya soportado por `listSales`) o `from`/`to` (mismo día) para "Fecha". "Documento" es un filtro nuevo: `listSales` (`src/modules/sales/sale-data-access.ts`) suma `customer.taxId` al `OR` existente (`contains`, insensitive) y agrega `taxId: true` al `select` del customer (no se muestra en la lista, solo se usa para filtrar — no se tocó el tipo `SaleListRecord`, el campo extra pasa sin problema por tipado estructural, confirmado con `typecheck` limpio). Debounce de 300ms reutilizando el mismo patrón de dos pasos que ya usaba `ReturnHistoryPanel`. Subtítulo actualizado a "Buscá por N° de venta, cliente, documento o fecha". Decisión no especificada por la orden: ningún chip viene preseleccionado por defecto (el usuario ve la lista general hasta elegir un criterio).

**Sección 2 — angostar "Nueva devolución".** El wrapper pasó de `max-w-screen-xl` (1280px) a `max-w-5xl`, manteniendo `mx-auto`. Se eligió `5xl` sobre `4xl` por dar mejor aprovechamiento al grid de 2 columnas a ~1366px sin quedar angosto a ~1920px — verificación razonada sobre los anchos de columna resultantes, no verificación visual en navegador real (queda para la revisión de Diego contra el preview de Vercel).

**Sección 3 — N° de operación/cupón de tarjeta.** Input nuevo debajo del selector de método de reintegro, visible solo cuando `refundMethod === "Tarjeta"`, obligatorio (bloquea `canSubmit`). Requirió `Return.refundReference String?` nuevo en `prisma/schema.prisma`, migración `20260902153835_add_return_refund_reference` — **aplicada y verificada contra la base real de Neon con una consulta de solo lectura** (no alcanzaba con que el schema tuviera el campo): `information_schema.columns` confirma la columna (`text`, nullable) y `_prisma_migrations` confirma la fila con `finished_at` seteado y `rolled_back_at` nulo. El campo se hila por `POST /api/returns` (valida 400 — "Debés indicar el número de operación o cupón de la tarjeta." — si `refundMethod` es Tarjeta y falta o viene vacío) y por `processReturn` (nuevo parámetro `refundReference`, se guarda en `tx.return.create`, se anula junto con `refundMethod` en ventas `CREDIT`). Se muestra en el paso de confirmación (`ReturnConfirmStep`) cuando corresponde.

**Sección 4 — la más importante de la orden: tarjeta obligatoria si la venta se pagó con tarjeta.** Si algún elemento de `Sale.paymentDetails` tiene `method === "Tarjeta"` (pago total o parcial), el reintegro tiene que ser con tarjeta, sí o sí. Frontend (`returns.tsx`): si la venta seleccionada tiene un pago con tarjeta, el selector de reintegro solo ofrece "Tarjeta" (se filtran las demás opciones del `<select>`) más un texto explicando por qué — combina las dos variantes que la orden sugería (sacar opciones / mostrar deshabilitado con explicación) en vez de elegir solo una. Backend (`processReturn`, `src/modules/returns/index.ts`): se confirmó primero que no existía ningún parser de `paymentDetails` del lado del servidor para reutilizar, así que se escribió uno nuevo (`parsePaymentDetailsServerSide`) y se agregó la validación — lanza `ReturnValidationError` si la venta se pagó con tarjeta y `refundMethod !== "Tarjeta"`, sin condicionarla a `paymentType` (coincide con el ejemplo literal de la orden y el planteo general de la sección). Testing — la parte que la orden marcó como más importante —: 2 tests de integración nuevos en `index.integration.test.ts` (`processReturn — refundMethod`): uno confirma el rechazo (venta pagada con tarjeta, reintegro pedido en Efectivo → `ReturnValidationError`, no se crea ningún `Return`), otro confirma el caso aceptado (mismo escenario, reintegro Tarjeta con `refundReference`, se persiste `refundMethod`/`refundReference` correctos).

**Efecto secundario detectado y corregido antes de dar la tarea por terminada:** la nueva validación de `refundReference` en `POST /api/returns` (Sección 3) hacía que 2 tests preexistentes de `src/app/api/returns/route.test.ts` (que mandaban `refundMethod: "Tarjeta"` sin `refundReference`, de antes de esta orden) cortaran en 400 antes de llegar a `processReturn`. Como `vi.clearAllMocks()` en el `beforeEach` de ese archivo limpia el historial de llamadas pero **no vacía la cola de `mockResolvedValueOnce`/`mockRejectedValueOnce` no consumida**, el valor encolado por esos 2 tests quedaba pendiente y lo consumía el siguiente test que sí llamaba a `processReturn`, desincronizando en cascada las aserciones de los tests posteriores (12 tests fallando en la primera corrida completa, todos en ese único archivo). Se corrigieron los 2 tests de origen agregando `refundReference` a su body, y se sumó el argumento nuevo (`undefined` a secas) a un tercer `toHaveBeenCalledWith` que también empezó a fallar por el mismo motivo (el mock ahora se llama siempre con 7 argumentos posicionales, no 6). Confirmado con una segunda corrida completa: 0 fallas.

`lint`, `typecheck` y `test` limpios (465 passed / 2 skipped, 70 archivos). Commit `95310f0` en la rama `design/revision-uiux-sep-2026` (pusheada a origin por primera vez en esta tarea — no existía todavía en el remoto). **No mergeada a `main`** — queda pendiente del preview automático de Vercel y la aprobación visual de Diego, tal como pide la orden.

**No autocertificado como verificado — queda para el Ingeniero Líder en sesión separada**, en particular: (a) la verificación visual real del ancho `max-w-5xl` contra el preview de Vercel a ~1366px y ~1920px (acá solo se razonó sobre las columnas resultantes, no se abrió un navegador), (b) confirmar que `limit=50` sin paginación real es aceptable como solución permanente para Sección 1 o si en algún momento hace falta "cargar más" de verdad, (c) revisar si la decisión de no precargar ningún chip por defecto es la experiencia que Diego espera, (d) confirmar que combinar "sacar opciones" + texto explicativo en Sección 4 (frontend) es el tratamiento visual correcto y no genera confusión.
