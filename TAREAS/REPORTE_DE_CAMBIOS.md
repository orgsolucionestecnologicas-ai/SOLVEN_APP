# REPORTE DE CAMBIOS — SOLVEN

> Actualizado automáticamente por Claude (Código) después de cada tarea.
> Al final del día Diego dice "revisá el reporte" → Claude marca en Notion + borra este archivo.

---

<!-- El agente irá agregando reportes aquí debajo, del más reciente al más antiguo -->

### TAREA 109 — estado: ✅ Completada
- Qué se hizo: Agregué un botón de pantalla completa en el header de Reportes (junto al botón "Exportar"), con ícono `Maximize2`/`Minimize2` de lucide-react. Agregué `rootRef` (useRef<HTMLDivElement>) sobre el contenedor raíz del componente `Reports()` y una función `toggleFullscreen()` que llama a `rootRef.current.requestFullscreen()` o `document.exitFullscreen()` según corresponda. Agregué estado `isFullscreen` sincronizado mediante un listener del evento `fullscreenchange` en `document` (con cleanup en el `useEffect`), que además controla qué ícono se muestra. Cuando `isFullscreen` es true, el contenedor raíz agrega `overflow-y-auto bg-white` para que el contenido scrollee correctamente dentro de la pantalla completa sin overflow raro — no oculté tabs ni controles de período (la tarea lo marcaba como opcional y el layout ya se ve bien a pantalla completa sin tocar nada más).
- Archivos modificados: `src/app/ui/reports.tsx` (imports `Maximize2`/`Minimize2`, estado `isFullscreen` + `rootRef`, función `toggleFullscreen`, botón nuevo en el header, `ref` en el contenedor raíz).
- Migraciones corridas (si aplica): no.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: nada pendiente. No oculté controles en modo pantalla completa (paso opcional del prompt) para mantener el cambio mínimo y no arriesgar el layout normal.
- typecheck: OK (lint también OK). No hay test file dedicado para `reports.tsx`.

### TAREA 108 — estado: ✅ Completada
- Qué se hizo: Agregué una función reutilizable `downloadSvgAsPng(svgElement, filename)` en `reports.tsx`: clona el nodo SVG, le fija `width`/`height` explícitos (tomados de `viewBox.baseVal`, ya que el SVG original solo tiene `viewBox` + CSS `width: 100%`, sin atributos explícitos — necesario para que la rasterización sea consistente), lo serializa con `XMLSerializer`, arma una data URL `data:image/svg+xml` (no Blob URL, para evitar taint del canvas), la carga en un `Image`, la dibuja sobre un `<canvas>` (con fondo blanco y escala 2x para mayor nitidez) y descarga el resultado como PNG con el mismo patrón de Blob+`createObjectURL`+click ya usado en `sales-list.tsx`. Agregué un botón "Descargar PNG" (ícono `Download`) en `SalesEvolutionPanel` (junto al botón "Por día" existente) y en `CategoryDonutPanel` (nuevo encabezado en fila con el título "Ventas por categoría"), cada uno con su propio `useRef<SVGSVGElement>` apuntando al `<svg>` correspondiente.
- Archivos modificados: `src/app/ui/reports.tsx` (import `useRef`, función `downloadSvgAsPng`, `SalesEvolutionPanel`, `CategoryDonutPanel`).
- Migraciones corridas (si aplica): no.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: nada pendiente. La tarea coincidía de forma bastante literal con el código existente (los dos paneles con gráficos SVG citados en el prompt son exactamente `SalesEvolutionPanel` y `CategoryDonutPanel`).
- typecheck: OK (lint también OK). No hay test file dedicado para `reports.tsx`.

### TAREA 107 — estado: ✅ Completada
- Qué se hizo: Seguí el patrón ya existente de `quote-pdf.tsx` + `quotes/[id]/pdf/route.tsx` (`@react-pdf/renderer`, ya instalado). Creé `src/app/ui/report-pdf.tsx` con un componente genérico `ReportPDFDocument({ businessName, reportTitle, rangeLabel, headers, rows })` — encabezado violeta con nombre del negocio + título del reporte + rango, y una tabla simple en orientación landscape (A4 apaisado, porque el reporte de ventas tiene 6 columnas y el de productos 8). Creé `src/app/api/reports/export-pdf/route.tsx`, que recibe los mismos query params (`type`, `from`, `to`) que `export/route.ts` (CSV). Repetí las mismas queries `prisma.sale.findMany`/`prisma.product.findMany` con el mismo `where`/`include` que ya usa el endpoint CSV (no extraje una función compartida — son un solo `findMany` cada una, sin riesgo de inconsistencia, tal como permite la restricción) y arma headers/filas con las mismas columnas que ya usa el CSV para cada tipo (ventas: fecha/folio/cliente/forma de pago/productos/total; productos: nombre/código/categoría/precio costo/precio venta/stock/stock mínimo/IVA) — no inventé columnas nuevas. En `reports.tsx`, agregué `downloadReportPdf(type, from, to)` (mismo patrón que `downloadReportCsv`, redirige a la URL del endpoint) y un segundo botón "Exportar PDF" junto a cada botón "Exportar CSV" existente, en `VentasTab` (línea ~1467) y `ProductosTab` (línea ~1634) — no agregué un dropdown para mantenerlo simple. El botón "Exportar" del header principal (que el prompt asumía que llamaba a `downloadReportCsv` en la línea ~453) en realidad llama a `window.print()` y no está atado a ningún `type` de CSV/PDF — no lo toqué, ya que agregar ahí un export PDF no tiene un `type` claro (esa pestaña no filtra por ventas/productos) y las dos ubicaciones reales donde existe `downloadReportCsv("ventas"/"productos")` son las que corresponden 1 a 1 con el endpoint PDF nuevo.
- Archivos modificados: `src/app/ui/report-pdf.tsx` (nuevo), `src/app/api/reports/export-pdf/route.tsx` (nuevo), `src/app/ui/reports.tsx` (helper `downloadReportPdf` + botón "Exportar PDF" en `VentasTab` y `ProductosTab`).
- Migraciones corridas (si aplica): no.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: discrepancia documentada arriba sobre la ubicación real del botón "Exportar" del header (llama a `window.print()`, no a `downloadReportCsv`) — el PDF se conectó donde realmente existe el mecanismo de export por `type`, en `VentasTab`/`ProductosTab`. No se tocó el endpoint CSV existente.
- typecheck: OK (lint también OK). No hay test file dedicado para `reports.tsx` ni para el endpoint nuevo.

### TAREA 106 — estado: ✅ Completada
- Qué se hizo: Agregué dos tarjetas nuevas arriba de las 3 tarjetas de resumen existentes en `ClientesTab`: "Clientes nuevos" (clientes cuyo `createdAt` cae dentro del período seleccionado en la barra superior de Reportes) y "Clientes recurrentes" (clientes con `purchases >= 2` en el período, calculado sobre `customerStats`, el mismo array ya usado por la tabla principal — no dupliqué esa lógica). Debajo agregué un gráfico de barras horizontal simple con 2 barras (Nuevos / Recurrentes, con ancho proporcional al máximo de ambos), hecho con `<div>`s y Tailwind, sin librería nueva. Para calcular "clientes nuevos" necesitaba el rango de fechas del período seleccionado (`currentMonth.start`/`currentMonth.end`, ya calculado en `Reports()` vía `getPeriodRange`) — el prompt asumía que `customers` ya venía filtrado por período al pasarse como prop, pero en realidad `ClientesTab` recibe la lista completa de clientes sin filtrar (igual que las demás pestañas). En vez de fetchear de nuevo o filtrar la lista completa a mano, agregué dos props nuevas `periodStart`/`periodEnd` a `ClientesTab` (Date), alimentadas desde el `currentMonth` que ya existe en `Reports()` — no es un fetch nuevo, es un valor ya calculado que solo faltaba pasar hacia abajo.
- Archivos modificados: `src/app/ui/reports.tsx` (función `ClientesTab` y su punto de invocación en `Reports()`).
- Migraciones corridas (si aplica): no.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: discrepancia documentada arriba (prop `customers` no venía pre-filtrada por período como asumía el prompt; se resolvió agregando `periodStart`/`periodEnd` como props en lugar de un fetch nuevo). No toqué `getSegment` ni la tabla "Clientes por valor" existente.
- typecheck: OK (lint también OK). No hay test file dedicado para `reports.tsx`.

### TAREA 105 — estado: ✅ Completada
- Qué se hizo: La tarea ya estaba mayormente implementada como advertía el prompt — `ReporteMensualTab` ya calculaba `prevTotal`, `prevCash`, `prevCredit`, `prevExpensesTotal`, `prevGrossProfit` y las mostraba como % de variación en cada tarjeta. Solo faltaba el botón "Comparar con período anterior", que no tenía `onClick`. Agregué estado local `showComparison` (useState, default `false`) y le puse `onClick={() => setShowComparison(v => !v)}` al botón. Debajo de la grilla de tarjetas, agregué una tabla comparativa (visible solo si `showComparison` es true) con las mismas 6 métricas que ya usa el array `cards` (Ventas totales, Ventas al contado, Ventas a crédito, Ganancia bruta, Gastos y retiros, Ganancia neta), en columnas: mes actual / mes anterior / variación %. Armé un array `comparisonRows` que reutiliza exactamente las mismas variables ya calculadas (`totalAmount`, `prevTotal`, `cashAmount`, `prevCash`, `creditAmount`, `prevCredit`, `grossProfit`, `prevGrossProfit`, `expensesTotal`, `prevExpensesTotal`) y la función `pctChange` ya existente — no recalculé nada nuevo.
- Archivos modificados: `src/app/ui/reports.tsx` (función `ReporteMensualTab`).
- Migraciones corridas (si aplica): no.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: nada pendiente. Noté (no lo toqué, preexistente y fuera de alcance) que la tarjeta "Ganancia neta" usa las mismas variables que "Ganancia bruta" (`grossProfit`/`prevGrossProfit`) en vez de un cálculo de ganancia neta separado — mantuve la tabla de comparación consistente con ese mismo comportamiento ya existente en `cards`, tal como exige la restricción de no recalcular métricas.
- typecheck: OK (lint también OK). No hay test file dedicado para `reports.tsx`.

### TAREA 104 — estado: ✅ Completada
- Qué se hizo: Agregué una nueva pestaña "Vendedores" en Reportes. Agregué `sellerCode: string | null` al tipo `SaleRecord` (el campo ya venía en la respuesta real de `/api/sales` — `listSales` no usa `select` restrictivo — solo faltaba declararlo en el tipo del frontend, no toqué el modelo `Sale` ni el backend). Agregué "Vendedores" al tipo `Tab` y al array `TABS` (entre "Servicios" y "Clientes"), y la rama de render correspondiente pasándole `sales={currentSales}` (mismas ventas ya filtradas por período que usan las demás pestañas). Escribí la función `VendedoresTab({ sales })`: agrupa las ventas por `sellerCode` (las que tienen `null` van bajo la etiqueta "Sin asignar"), calcula por vendedor cantidad de ventas, monto total y ticket promedio (`total / count`), y ordena de mayor a menor monto total. Arriba de la tabla muestro una fila de 3 tarjetas tipo podio (1°/2°/3°) con badge de color (oro/plata/bronce) para los top 3, y debajo una tabla completa con todos los vendedores (código, ventas, monto total, ticket promedio), reusando `formatMoney` y las mismas clases de tabla/tarjeta que el resto de `reports.tsx` (`rounded-xl border border-slate-200 bg-white shadow-sm`, etc.). El prompt citaba `TopProductsPanel` como referencia para "tarjetas tipo podio" pero ese componente en realidad es una lista numerada simple, no tarjetas — igual construí tarjetas de podio literales para cumplir el pedido explícito del texto de la tarea.
- Archivos modificados: `src/app/ui/reports.tsx` (tipo `SaleRecord`, tipo `Tab`, array `TABS`, rama de render en `Reports()`, nueva función `VendedoresTab`).
- Migraciones corridas (si aplica): no.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: nada pendiente. No se modificó el modelo `Sale` ni `sellerCode` (de solo lectura para este reporte, tal como pedía la restricción).
- typecheck: OK (lint también OK). No hay test file dedicado para `reports.tsx`.

### TAREA 103 — estado: ✅ Completada
- Qué se hizo: La tarea ya estaba mayormente implementada como advertía el prompt — `productProfitability` en `RentabilidadTab` ya calculaba ingresos/costo/ganancia/margen% por producto, ordenado fijo por ganancia descendente. Agregué estado local `sortColumn`/`sortDirection` y una función `toggleSort(column)` (si se clickea la misma columna alterna asc/desc, si se clickea otra columna cambia de columna y arranca en desc). Convertí los 5 headers numéricos (Unidades, Ingresos, Costo, Ganancia, Margen %) en botones clicables con un ícono ▲/▼ junto al nombre de la columna activa, y agregué `sortedProductProfitability` (un `useMemo` que ordena una copia de `productProfitability` según el criterio activo) que ahora alimenta el `.map()` de las filas en vez del array original.
- Archivos modificados: `src/app/ui/reports.tsx` (función `RentabilidadTab`).
- Migraciones corridas (si aplica): no.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: nada pendiente — no dupliqué el cálculo de `productProfitability` (sigue siendo la única fuente de datos, solo se ordena una copia sobre él) ni toqué el resto de la pestaña Rentabilidad.
- typecheck: OK (lint también OK). No hay tests automatizados para `reports.tsx` (componente de UI sin archivo de test asociado); validado leyendo el resultado del sort.

### TAREA 111 — estado: ✅ Completada
- Qué se hizo: Agregué dos funciones nuevas a `src/lib/email.ts` siguiendo exactamente el patrón existente (`getResend()`, `wrap()`, `FROM`, sin tocar ninguna función existente): `sendLowStockAlertEmail(to, businessName, products)` (tabla con productos en stock crítico/agotado) y `sendCashRegisterDifferenceAlertEmail(to, businessName, difference)` (usa el mismo umbral `Math.abs(difference) >= 0.005` que ya usa `cash-register-close.tsx`). Creé un módulo nuevo `src/lib/email-alerts.ts` con dos helpers (`notifyLowStockIfEnabled`, `notifyCashDifferenceIfEnabled`) que centralizan la lógica común (leer el toggle en `StoreSettings`, buscar el `User` con rol OWNER vía `prisma.user.findFirst({ where: { tenantId, role: "OWNER" } })`, enviar el email) para no duplicarla en los dos endpoints que la usan. Enganché el envío: (1) diferencia de caja, en `PUT /api/cash-register/[id]/route.ts` (confirmé que ese es el endpoint real de cierre, no `/close` como sugería el prompt) justo después de que `closeSession()` devuelve la sesión, disparado sin bloquear la respuesta (`void notifyCashDifferenceIfEnabled(...)`, mismo patrón fire-and-forget que ya usa `logAudit` un par de líneas arriba en el mismo archivo); (2) stock crítico, en `sale-data-access.ts` → `createSale`, comparando `newStock <= product.minStock` para cada producto reducido dentro de la transacción (usando `product.minStock`, ya disponible en el `products` fetch existente) y disparando el aviso después de que la transacción confirma (también fire-and-forget). Elegí el path de ventas como único gatillo de stock crítico (el prompt permite explícitamente "o donde se actualiza stock en ventas") y no agregué un segundo gatillo en `stock-adjustment.ts` (ajustes manuales de inventario) para no duplicar lógica de notificación sin necesidad clara — si se quiere cubrir también ajustes manuales, es una extensión futura acotada. Agregué la sección "Notificaciones" real en `settings.tsx` (antes era un `ComingSoonSection` — no existía ningún toggle ahí): nuevo componente `NotificacionesSection` con dos `ToggleSwitch` (mismo patrón que los toggles de `GeneralSection`), que guarda vía `PATCH /api/settings` reenviando el objeto completo de configuración ya cargado (mitiga el bug de reemplazo total de `upsertSettings` documentado en la Tarea 101, igual que se hizo en el wizard).
- Archivos modificados: `prisma/schema.prisma` (2 campos nuevos en `StoreSettings`), `src/modules/settings/settings-validation.ts` (acepta/valida los 2 campos nuevos), `src/lib/email.ts` (2 funciones nuevas), `src/lib/email-alerts.ts` (nuevo, helpers compartidos), `src/app/api/cash-register/[id]/route.ts` (dispara alerta de diferencia), `src/modules/sales/sale-data-access.ts` (dispara alerta de stock crítico), `src/app/ui/settings.tsx` (nueva sección Notificaciones con los 2 toggles).
- Migraciones corridas (si aplica): sí — `20260713125013_add_store_settings_email_alert_toggles` (agrega `StoreSettings.lowStockEmailAlerts Boolean @default(false)` y `StoreSettings.cashDifferenceEmailAlerts Boolean @default(false)`, ambos aditivos con default `false` como exige la restricción del prompt, ya corrida en la DB de desarrollo). Diego necesita correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: el endpoint de cierre de caja real es `PUT /api/cash-register/[id]`, no `/api/cash-register/close` como sugería el prompt. La sección "Notificaciones" de `settings.tsx` no existía (era un placeholder "próximamente"); ahora tiene contenido real pero solo con estos 2 toggles — no agregué más preferencias de notificación que no fueron pedidas. No enganché el low-stock alert en `stock-adjustment.ts` (ajustes manuales de inventario), solo en el flujo de ventas, por alcance/duplicación innecesaria; documentado como decisión deliberada arriba.
- typecheck: OK (lint también OK). `npm test` completo: 2 fallas, ambas preexistentes y no relacionadas (confirmado no tocar código de deudas ni el flujo de ventas de crédito): `sales/route.integration.test.ts > creates a credit sale with debt through the API flow` (falla ya documentada en la Tarea 101) y `debt-payment-data-access.integration.test.ts > prevents concurrent payments from overpaying a debt` (falló solo en la corrida completa por contención de conexiones contra la misma DB de Neon bajo tests paralelos — confirmado pasa limpio en aislamiento).

### TAREA 101 — estado: ✅ Completada
- Qué se hizo: El prompt asumía que no existía tracking de onboarding. En realidad ya existía `Tenant.onboardingCompleted` (Boolean), un gate server-side en `src/app/dashboard/layout.tsx` que redirige a `/onboarding` si no está completo, un endpoint `POST /api/onboarding/complete`, y un wizard de 3 pasos (`onboarding-wizard.tsx`: negocio, primer producto, abrir caja). Para evitar dos mecanismos de tracking paralelos/en conflicto, reutilicé el `onboardingCompleted` existente (NO agregué `onboardingCompletedAt` ni el endpoint `PATCH /api/tenants/onboarding` del prompt) y extendí el wizard de 3 a 5 pasos: 1) negocio (nombre, rubro, moneda), 2) primer producto o servicio (ahora con toggle producto/servicio, reusa `/api/products` y `/api/services`), 3) forma de pago preferida (nueva), 4) invitar usuario opcional (nuevo, reusa `POST /api/users`, con botón "Saltear por ahora"), 5) resumen + apertura de caja (funcionalidad ya existente, no la borré por ser valiosa aunque no figuraba en los 5 pasos del prompt) + botón "Empezar a vender". Agregué también un link "Saltear configuración y empezar a vender" visible en los pasos 1-4 que completa el onboarding y va al dashboard de inmediato, para cumplir la restricción de que el wizard no debe bloquear el uso de la app en ningún paso. Mantuve el gate server-side de página completa en `dashboard/layout.tsx` (no lo convertí a overlay dentro del shell) porque ya funciona de forma confiable y reescribirlo era un riesgo desproporcionado para esta tarea.
- Archivos modificados: `prisma/schema.prisma` (2 campos nuevos en `StoreSettings`), `src/modules/settings/settings-validation.ts` (acepta/valida los 2 campos nuevos), `src/app/ui/onboarding-wizard.tsx` (reescrito, 3→5 pasos).
- Migraciones corridas (si aplica): sí — `20260713115745_add_store_settings_business_type_and_payment_method` (agrega `StoreSettings.businessType String @default("")` y `StoreSettings.preferredPaymentMethod String @default("efectivo")`, ambos aditivos con default, ya corrida en la DB de desarrollo). Diego necesita correr `npx prisma migrate deploy` en producción.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: Ya implementado de otra forma: el mecanismo de persistencia de "completado" (se reusó `onboardingCompleted` boolean en vez del campo nuevo pedido) y el paso de apertura de caja (se mantuvo, no estaba en la lista de 5 pasos del prompt pero ya funcionaba). Pendiente/no incluido: los campos nuevos `businessType` y `preferredPaymentMethod` no se agregaron a la pantalla de Configuración (`settings.tsx`) — hoy solo se cargan una vez desde el wizard y no son editables después; si se quiere permitir editarlos luego habría que agregarlos a `DocumentosSection` o `NegocioPanel` en una tarea aparte. También noté (no lo toqué, fuera de alcance): `upsertSettings` hace un reemplazo completo de `StoreSettings` en cada PATCH — si el wizard llama a `/api/settings` sin mandar todos los campos, los que no se mandan vuelven a su valor por defecto. Ya pasaba antes de esta tarea (paso 1 del wizard original ya tenía este comportamiento); lo mitigué dentro del wizard reenviando siempre los campos ya conocidos (businessName/currency/businessType/preferredPaymentMethod) en cada PATCH, pero no es un fix general del endpoint.
- typecheck: OK (lint y build también OK). Nota: `npm test` corrido completo tiene 2 fallas preexistentes no relacionadas con esta tarea (no toqué ventas, deudas ni caja): `sales/route.integration.test.ts > creates a credit sale with debt through the API flow` falla de forma consistente incluso en una rama sin mis cambios tocada (error en el propio test, `responseBody.data` viene undefined) y `cash-movements/route.integration.test.ts > lists cash movements after creation` falló solo en la corrida completa (500) pero pasa en aislamiento — parece contaminación de datos entre tests corriendo contra la misma DB de Neon, no algo introducido por esta tarea.

### TAREA 102 — estado: ✅ Completada
- Qué se hizo: `NegocioPanel.tsx` solo envolvía `<Settings />` sin traer datos propios. Lo convertí en client component con su propio `fetch("/api/settings")` (mismo patrón ya usado en `GeneralSection`/`DocumentosSection` de `settings.tsx`) y agregué una barra de progreso "Configuración completa: X/8 campos" + % arriba del formulario, calculando en el cliente cuántos de los 8 campos recomendados (nombre, teléfono, email, dirección, CUIT, logoUrl, receiptFooterMessage, receiptThankYouMessage) están completos, con la lista de campos faltantes debajo de la barra. Puramente visual/informativo, no bloquea el guardado ni agrega validaciones.
- Archivos modificados: `src/app/settings/components/NegocioPanel.tsx`.
- Migraciones corridas (si aplica): no.
- Algo ya estaba implementado de otra forma / algo quedó pendiente: nada pendiente — la tarea no tenía discrepancias con el código real, `NegocioPanel.tsx` era efectivamente un wrapper vacío como suponía el prompt.
- typecheck: OK (lint también OK).

Historial de Tareas 081–100 revisado, marcado como completado en Notion y archivado por el Ingeniero Líder — 2026-07-13.
