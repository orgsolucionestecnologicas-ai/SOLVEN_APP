# SOLVEN — Tareas de Producto · Lote 5 (081–100)

> **Instrucciones para Claude (sección Código / Code):**
>
> Cuando diga "ejecuta la tarea X": encontrá la tarea por número, ejecutá **únicamente** lo que
> indica su prompt, hacé commit de los cambios con mensaje descriptivo, y actualizá
> `TAREAS/REPORTE_DE_CAMBIOS.md` con el resultado. sube todo a github.
> **Regla de oro: no modifiques lógica de negocio, BD ni archivos fuera de los indicados. Ante la duda, menos es más.**
> Fuente: base de datos Notion "SOLVEN — Gestión de Tareas", tareas TAREA-081 a TAREA-100.

---

## SECCIÓN: DEVOLUCIONES

---

### TAREA 081 · 🟡 MEDIO
**Nota de crédito de la devolución en PDF descargable**

**Descripción:** Poder descargar un comprobante en PDF de cada devolución procesada, a modo de nota de crédito para el cliente.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: el proyecto ya tiene un patrón completo de generación de PDF con
   `@react-pdf/renderer` para cotizaciones — `src/app/ui/quote-pdf.tsx` (documento) y
   `src/app/api/quotes/[id]/pdf/route.tsx` (endpoint que lo renderiza y devuelve como
   `application/pdf`). Replicá exactamente ese mismo patrón para devoluciones.
2. Creá `src/app/ui/return-credit-note-pdf.tsx` con un componente `ReturnCreditNotePDFDocument`
   (mismo estilo visual que `QuotePDFDocument`: header con el nombre del negocio, datos de la
   devolución) que muestre: número/id corto de la devolución, fecha, venta de origen, motivo
   (`reasonCategory`/`reasonNote` de la Tarea 077), lista de productos devueltos con cantidad y
   precio unitario, y el monto total devuelto — encabezado del documento como "Nota de crédito".
3. Creá `src/app/api/returns/[id]/pdf/route.tsx` (mismo patrón que
   `src/app/api/quotes/[id]/pdf/route.tsx`: busca la devolución vía `listReturns`/una función
   puntual `getReturnById` si hace falta agregarla a `src/modules/returns/index.ts` sin modificar
   `processReturn`, obtiene `businessName` de `StoreSettings`, renderiza el documento a buffer y
   lo devuelve con los headers de PDF correspondientes).
4. En `ReturnHistoryPanel` (`src/app/ui/returns.tsx`, agregado en la Tarea 075), agregá un botón
   "Descargar nota de crédito" (ícono `FileText` o similar) en cada fila del historial que abra
   `/api/returns/[id]/pdf` en una nueva pestaña, mismo patrón que el link de descarga de PDF de
   cotizaciones.

RESTRICCIONES ESTRICTAS:
- No modifiques `processReturn` ni el schema de Prisma.
- No modifiques `quote-pdf.tsx` ni el endpoint de PDF de cotizaciones.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/return-credit-note-pdf.tsx` (nuevo), `src/app/api/returns/[id]/pdf/route.tsx` (nuevo), `src/modules/returns/index.ts` (solo si hace falta una función de lectura puntual), `src/app/ui/returns.tsx`

---

### TAREA 082 · 🟡 MEDIO · UX
**Advertencia si el producto ya tiene stock 0 y no puede reponerse**

**Descripción:** Al procesar una devolución, si se decide NO reponer el stock de un producto que ya está en 0 unidades disponibles, advertir claramente que ese producto seguirá sin stock disponible para la venta.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: el checkbox "Reponer al inventario" por producto (Tarea 079) ya existe en
   `src/app/ui/returns.tsx`, tildado por defecto. El formulario de "Nueva devolución" hoy no
   conoce el stock actual de cada producto (solo tiene los datos de la venta original, sin
   `product.stock`).
2. Al cargar el detalle de la venta seleccionada, obtené el stock actual de los productos
   involucrados (podés usar el endpoint `GET /api/products` ya existente filtrando por los
   `productId` de la venta, o el que uses en otras pantallas del proyecto para traer stock — no
   crees un endpoint nuevo si `/api/products` ya permite resolverlo).
3. Por cada producto de la venta cuyo checkbox "Reponer al inventario" esté destildado (o se
   destilde), si el stock actual de ese producto es 0, mostrá una advertencia visible junto a la
   línea del producto (ej. texto ámbar/rojo con ícono `AlertTriangle`: "Este producto ya está sin
   stock y no se repondrá — seguirá sin stock disponible para la venta").
4. Esta advertencia es informativa, no bloquea el envío de la devolución.

RESTRICCIONES ESTRICTAS:
- No modifiques `processReturn` ni `/api/returns`.
- No agregues un endpoint nuevo si `/api/products` ya alcanza para resolver el stock actual.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/returns.tsx`

---

### TAREA 083 · 🟡 MEDIO · UX
**Confirmación de dos pasos con resumen: qué se devuelve + impacto en stock + monto**

**Descripción:** Antes de confirmar una devolución, mostrar un resumen final de qué se va a devolver, qué productos van a reponerse al inventario y cuáles no, y el monto total, pidiendo una confirmación explícita.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En el formulario "Nueva devolución" de `src/app/ui/returns.tsx` (mismo patrón de confirmación de
dos pasos ya usado en otras partes del proyecto para ajustes de stock — un paso intermedio dentro
del mismo formulario, no un modal aparte):
1. Al hacer clic en "Procesar devolución", en lugar de enviar directamente, mostrá un paso de
   confirmación con un resumen: lista de productos a devolver con su cantidad, qué productos se
   reponen al inventario y cuáles no (Tarea 079), el motivo seleccionado (Tarea 077), y el monto
   total a devolver — con dos botones "Volver" (regresa al formulario sin enviar nada) y
   "Confirmar devolución" (recién ahí dispara el `POST /api/returns` existente).
2. Si el usuario cancela/vuelve, debe conservar todo lo ya cargado en el formulario (cantidades,
   motivo, checkboxes de reposición) sin perder los datos.

RESTRICCIONES ESTRICTAS:
- No modifiques `processReturn` ni `/api/returns`.
- No modifiques el schema de Prisma.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/returns.tsx`

---

## SECCIÓN: DEUDAS

---

### TAREA 084 · 🟠 ALTO
**Deudas vencidas automáticamente marcadas con badge 'Vencida' en rojo**

**Descripción:** Poder marcar automáticamente como "Vencida" una deuda cuya fecha límite de pago ya pasó, distinguiéndola visualmente de una deuda pendiente normal.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: el modelo `Debt` no tiene hoy ningún campo de fecha de vencimiento — solo
   `totalAmount`, `remainingAmount`, `customerId`, `createdAt`. No existe ningún concepto de
   deuda "vencida" en el código actual.
2. Agregá el campo opcional `dueDate DateTime?` al modelo `Debt` en `prisma/schema.prisma`. Corré
   `npx prisma migrate dev --name add-debt-due-date` (el campo queda `null` para deudas
   existentes, que simplemente no se considerarán vencidas hasta que se les asigne fecha).
3. En `src/app/ui/debts-list.tsx`, en la tabla principal (`paginatedDebts`) y en
   `DebtDetailModal`, calculá si una deuda está vencida: `dueDate !== null && new Date(dueDate) <
   new Date() && Number(remainingAmount) > 0`. Si está vencida, mostrá un badge rojo "Vencida" en
   lugar de (o además de) "Pendiente" en la columna Estado, y aplicá el mismo criterio en el
   detalle.
4. El tipo `DebtRecord` en el cliente debe incluir `dueDate: string | null`, y `GET /api/debts`
   (`src/app/api/debts/route.ts` / `listDebts` en `debt-data-access.ts`) debe devolver ese campo
   (ya viene incluido automáticamente al no estar excluido del `select`/`include` por defecto de
   Prisma — verificalo antes de tocar la consulta).

RESTRICCIONES ESTRICTAS:
- Esta tarea SÍ requiere migración de Prisma (campo nuevo, opcional, no rompe deudas existentes).
- No agregues todavía un formulario para cargar `dueDate` en esta tarea — eso lo cubre la Tarea
  086. Acá solo agregás el campo, la migración, y el cálculo/badge de "vencida" para cuando el
  campo tenga un valor.
- Ejecutá `npm run build` y la migración antes de dar la tarea por terminada.
```

**Archivos afectados:** `prisma/schema.prisma`, `src/app/ui/debts-list.tsx`, `src/modules/debts/debt-data-access.ts` (solo si hace falta ajustar el `select`)

---

### TAREA 085 · 🟠 ALTO
**Total global adeudado visible en el encabezado de la sección**

**Descripción:** Mostrar de forma prominente, en la parte superior de la sección de Deudas, el monto total que deben todos los clientes en conjunto.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Verificá primero: esta funcionalidad YA ESTÁ IMPLEMENTADA en `src/app/ui/debts-list.tsx`. La
tarjeta de métricas "Total deuda" (primera de las 4 tarjetas, `MetricCard` con ícono `AlertCircle`
rojo) ya muestra `totalDebt` (suma de `remainingAmount` de todas las deudas) de forma prominente,
en tamaño grande (`text-2xl font-bold`), justo debajo del encabezado "Deudas" de la sección.

Si al revisar el código confirmás que el monto total ya es claramente visible en la parte
superior de la sección, NO hagas ningún cambio: documentá en el reporte que la tarea ya estaba
implementada y salí sin modificar archivos. Si encontrás que el monto no es lo suficientemente
prominente (por ejemplo, muy chico comparado a lo esperado, o mezclado con las otras tarjetas sin
destacarse), podés agrandar levemente el texto o el estilo de esa tarjeta puntual, sin rediseñar
las otras 3 tarjetas de métricas.

RESTRICCIONES ESTRICTAS:
- No modifiques el cálculo de `totalDebt`.
- No modifiques `/api/debts` ni el schema de Prisma.
- Si hacés cambios, ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/debts-list.tsx` (solo si el monto no es suficientemente prominente)

---

### TAREA 086 · 🟠 ALTO
**Fecha de vencimiento de deuda configurable al registrarla**

**Descripción:** Al registrar una deuda nueva, poder indicar opcionalmente una fecha límite de pago.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Esta tarea depende de que la Tarea 084 (campo `Debt.dueDate` + migración) ya esté implementada.
Si todavía no lo está, implementala primero siguiendo esa tarea antes de continuar con esta.

1. Verificá primero: el botón "Nueva deuda" en el header de `src/app/ui/debts-list.tsx` (línea
   con el ícono `Plus`) NO tiene ningún `onClick` — hoy es un botón decorativo sin ninguna acción
   asociada. No existe ningún modal de creación manual de deuda en el proyecto; las deudas hoy
   solo se generan automáticamente a través de ventas a crédito (`paymentType: "CREDIT"`). El
   backend `POST /api/debts` (`createDebt` en `debt-data-access.ts`) sí existe y ya funciona,
   pero no está conectado a ninguna UI de alta manual.
2. Creá un componente `CreateDebtModal` en `src/app/ui/debts-list.tsx` con: un buscador de
   cliente (mismo patrón de búsqueda con debounce ya usado en `NewQuoteModal` de
   `quotes-list.tsx`, contra `GET /api/customers?search=...`), un campo de monto total
   obligatorio, y un campo de fecha de vencimiento opcional (`<input type="date">`).
3. Conectá el botón "Nueva deuda" para abrir ese modal. Al confirmar, hacé `POST /api/debts` con
   `{ customerId, totalAmount, dueDate }` (agregá soporte para `dueDate` en
   `CreateDebtInput`/`validateCreateDebtInput` en `debt-validation.ts` — opcional, validando que
   sea una fecha válida si viene — y en `createDebt`/`debt-data-access.ts`, guardando el campo
   agregado en la Tarea 084). Al éxito, cerrá el modal y refrescá la lista (mismo patrón
   `refreshKey` ya usado tras registrar un pago).

RESTRICCIONES ESTRICTAS:
- No modifiques cómo se generan las deudas automáticas desde ventas a crédito.
- No agregues campos fuera de cliente, monto y fecha de vencimiento opcional.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/debts-list.tsx`, `src/modules/debts/debt-validation.ts`, `src/modules/debts/debt-data-access.ts`, `src/app/api/debts/route.ts` (solo si hace falta pasar el campo nuevo)

---

### TAREA 087 · 🟡 MEDIO
**Exportar listado de deudas a CSV / Excel**

**Descripción:** Poder descargar el listado de deudas visible (con los filtros aplicados) en un archivo CSV.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/debts-list.tsx`:
1. Agregá un botón "Exportar CSV" junto a los filtros existentes (búsqueda, estado, período,
   orden).
2. Generá el CSV en el cliente (sin librerías nuevas, mismo patrón ya usado en
   `sales-list.tsx`/`cash-movements-list.tsx`: `escapeCsvValue` + `Blob` +
   `URL.createObjectURL` + click programático + `revokeObjectURL`) a partir de `filteredDebts`
   (las deudas actualmente visibles/filtradas, no solo la página actual), con columnas: Cliente,
   Deuda total, Saldo pendiente, Estado (Pendiente/Pagada/Vencida si la Tarea 084 ya está
   implementada), Fecha de creación, Fecha de vencimiento (si la Tarea 084/086 ya están
   implementadas).
3. Descargá con nombre `deudas_YYYY-MM-DD.csv` (fecha actual).

RESTRICCIONES ESTRICTAS:
- No agregues librerías externas de CSV.
- No modifiques `/api/debts` ni el schema de Prisma.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/debts-list.tsx`

---

### TAREA 088 · 🟡 MEDIO
**Enviar recordatorio de pago por WhatsApp desde la deuda**

**Descripción:** Poder enviar un recordatorio de pago por WhatsApp directamente desde una deuda pendiente, con el monto y los datos básicos ya redactados.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: el proyecto ya tiene un patrón de compartir por WhatsApp — la función
   `shareLastSaleWhatsApp` en `src/app/ui/pos.tsx`, que arma un texto con `encodeURIComponent` y
   abre `https://wa.me/?text=...` en una pestaña nueva. Replicá ese mismo patrón, pero apuntando
   al teléfono del cliente cuando esté disponible (`https://wa.me/<phone>?text=...`, limpiando el
   número de caracteres no numéricos antes de armarlo) para que el mensaje llegue directo a su
   chat en vez de abrir el selector de contacto.
2. Verificá que `GET /api/debts` incluya el teléfono del cliente (`Customer.phone`) en el
   `include`/`select` de `debt-data-access.ts` — hoy el tipo `DebtWithCustomer` solo trae
   `customer: { name: true }`; agregá `phone: true` a ese `select` sin cambiar la forma del resto
   de la respuesta.
3. En `src/app/ui/debts-list.tsx`, agregá un botón "Recordar por WhatsApp" (ícono `MessageCircle`
   de lucide-react, mismo verde que en pos.tsx) en cada fila de deuda pendiente y en
   `DebtDetailModal`, que arme un mensaje con el nombre del cliente, el saldo pendiente
   (`formatMoney`), y el nombre del negocio (podés reusar `GET /api/settings` para el
   `businessName`, mismo patrón que `PrintModal` en pos.tsx), y abra WhatsApp con ese texto
   prellenado. Si el cliente no tiene teléfono cargado, deshabilitá el botón con un tooltip
   "Cliente sin teléfono registrado".

RESTRICCIONES ESTRICTAS:
- No integres ninguna API oficial de WhatsApp Business ni envíes mensajes automáticamente — es
  siempre una acción manual del usuario vía `wa.me`, igual que el patrón ya existente en pos.tsx.
- No modifiques el schema de Prisma.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/modules/debts/debt-data-access.ts`, `src/app/ui/debts-list.tsx`

---

### TAREA 089 · 🟡 MEDIO
**Ordenar por mayor monto / más antigua / más reciente / por cliente**

**Descripción:** Poder ordenar el listado de deudas por distintos criterios: monto de mayor a menor, antigüedad (más antigua o más reciente primero), o alfabéticamente por cliente.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Verificá primero: el selector de orden en `src/app/ui/debts-list.tsx` (`sortOrder`) YA
IMPLEMENTA 3 de los 4 criterios pedidos: "Más recientes" (`recent`, por `createdAt` descendente),
"Mayor deuda" (`amount`, por `remainingAmount` descendente) y "Cliente A-Z" (`name`). Falta
únicamente el criterio de "más antigua primero".

1. Agregá una opción `"oldest"` al selector (`<option value="oldest">Más antiguas</option>`) y su
   rama correspondiente en el `useMemo` de `filteredDebts`:
   `result.sort((a, b) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime())`.
2. No modifiques los otros 3 criterios ya implementados ni el resto de la lógica de filtrado.

RESTRICCIONES ESTRICTAS:
- No modifiques `/api/debts` ni el schema de Prisma — el ordenamiento es puramente client-side
  sobre los datos ya cargados, igual que los otros 3 criterios.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/debts-list.tsx`

---

### TAREA 090 · 🟢 BAJO
**Marcar deuda como incobrable con nota (baja contable)**

**Descripción:** Poder marcar una deuda como incobrable (dar de baja contable), con una nota explicativa, sacándola del cálculo de deuda activa sin borrar su historial.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Agregá al schema de Prisma (`prisma/schema.prisma`) los campos `writtenOff Boolean
   @default(false)`, `writeOffNote String?` y `writeOffAt DateTime?` al modelo `Debt`. Corré
   `npx prisma migrate dev --name add-debt-write-off`.
2. En `src/modules/debts/debt-data-access.ts`, agregá una función `writeOffDebt(id, note,
   tenantId)` que marque la deuda como `writtenOff: true`, guarde `writeOffNote` y
   `writeOffAt: new Date()` (validá que `id` pertenezca al tenant antes de actualizar, mismo
   patrón de scoping por tenant que el resto del módulo). No modifiques `createDebt` ni
   `listDebts` más allá de lo necesario para exponer los campos nuevos.
3. Creá `PATCH /api/debts/[id]/write-off` (mismo patrón de auth con `requireRole(["OWNER"])`,
   ya que es una decisión contable sensible) que reciba `{ note: string }` y llame a esa función.
4. En `src/app/ui/debts-list.tsx`: agregá un botón/acción "Marcar como incobrable" (visible solo
   para deudas con saldo pendiente, ej. en el menú de acciones de la fila o en
   `DebtDetailModal`) que abra un pequeño modal pidiendo la nota obligatoria antes de confirmar.
   Las deudas con `writtenOff: true` deben mostrar un badge distintivo (ej. gris/slate,
   "Incobrable") en vez de "Pendiente", y **excluirse** del cálculo de `totalDebt`,
   `activeDebtsCount`, `customersWithDebtCount` y `topDebtors` (ya que fueron dadas de baja
   contable, no deben seguir contando como deuda activa), sin eliminar el registro ni su
   historial de pagos.

RESTRICCIONES ESTRICTAS:
- Esta tarea SÍ requiere migración de Prisma (3 campos nuevos, todos opcionales/con default, no
  rompen deudas existentes).
- No elimines ninguna `Debt` ni `DebtPayment` — la baja es un estado, no un borrado.
- Ejecutá `npm run build` y la migración antes de dar la tarea por terminada.
```

**Archivos afectados:** `prisma/schema.prisma`, `src/modules/debts/debt-data-access.ts`, `src/app/api/debts/[id]/write-off/route.ts` (nuevo), `src/app/ui/debts-list.tsx`

---

### TAREA 091 · 🟡 MEDIO · UX
**Barra de progreso de pago por deuda: cuánto pagó sobre el total**

**Descripción:** Mostrar visualmente, para cada deuda, qué porcentaje ya fue pagado sobre el monto total, no solo los números de deuda total y saldo pendiente.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/debts-list.tsx`:
1. En `DebtDetailModal` (donde ya se muestran los 3 valores "Deuda total" / "Pendiente" /
   "Pagado" en la grilla de 3 columnas), agregá debajo una barra de progreso horizontal simple
   (un `<div>` con fondo `bg-slate-100` y una barra interna `bg-emerald-500` cuyo ancho en % sea
   `(paidAmount / totalAmount) * 100`, con el porcentaje como texto, ej. "65% pagado").
2. Si es sencillo sin romper el layout de la tabla, agregá una versión compacta de la misma
   barra en la fila de la tabla principal (`paginatedDebts`), debajo o junto al monto de saldo
   pendiente — si no entra bien en el layout actual de la tabla, es aceptable dejarla solo en el
   detalle (`DebtDetailModal`) y documentarlo en el reporte.
3. Para deudas totalmente pagadas (`remainingAmount === 0`), la barra debe verse completa
   (100%).

RESTRICCIONES ESTRICTAS:
- No modifiques `/api/debts` ni el schema de Prisma — el cálculo es sobre datos ya disponibles
  (`totalAmount`, `remainingAmount`).
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/debts-list.tsx`

---

### TAREA 092 · 🟡 MEDIO · UX
**Monto total adeudado en grande en la parte superior de la lista**

**Descripción:** El monto total adeudado debe ser lo primero y más grande que se vea al entrar a la sección de deudas.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Esta tarea es muy similar a la Tarea 085 y comparte la misma verificación: el monto total
adeudado (`totalDebt`) ya se muestra en la primera tarjeta de métricas de
`src/app/ui/debts-list.tsx`, arriba de todo, en `text-2xl font-bold`.

Si la Tarea 085 ya fue ejecutada y ya dejó ese monto suficientemente grande/prominente, verificá
que efectivamente sea la primera tarjeta visible (ya lo es, `MetricCard Icon={AlertCircle}` es la
primera de las 4) y NO hagas ningún cambio adicional — documentá en el reporte que ya está
cubierto por la Tarea 085 y salí sin modificar archivos.

Si por algún motivo la Tarea 085 no se ejecutó o el monto no quedó destacado, aumentá el tamaño
de fuente de esa tarjeta puntual (ej. de `text-2xl` a `text-3xl` o `text-4xl`) para que sea
claramente lo más grande de la vista, sin modificar las otras 3 tarjetas de métricas.

RESTRICCIONES ESTRICTAS:
- No dupliques la tarjeta de total ni agregues una segunda igual — es la misma tarjeta que la
  Tarea 085, solo se ajusta el tamaño si hace falta.
- Si hacés cambios, ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/debts-list.tsx` (solo si hace falta agrandar el texto)

---

### TAREA 093 · 🟡 MEDIO · UX
**Click en el cliente navega al perfil completo sin perder el contexto**

**Descripción:** Al hacer clic en el nombre de un cliente desde el listado de deudas, se debe poder ir a su perfil completo, y volver atrás sin haber perdido los filtros aplicados en el listado de deudas.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: el nombre de cada cliente en la tabla de `src/app/ui/debts-list.tsx` YA ES
   un `Link` a `/customers/${debt.customerId}` — la navegación al perfil ya funciona. Lo que falta
   es la parte de "sin perder el contexto": los filtros de deudas (`searchQuery`, `filterEstado`,
   `periodFilter`, `sortOrder`, `currentPage`) viven en estado de React del componente, así que al
   navegar a `/customers/[id]` y volver con el botón "atrás" del navegador, `DebtsList` se
   remonta y esos filtros se pierden.
2. Cambiá el `Link` para que apunte a `/customers/${debt.customerId}?from=deudas` (agregando el
   query param, sin tocar el resto de la ruta).
3. Verificá primero cómo está estructurada la página de detalle de cliente (buscá el archivo real,
   probablemente `src/app/customers/[id]/page.tsx` o similar) antes de editarla. Si esa página
   detecta el parámetro `from=deudas` en la URL, agregá un enlace "← Volver a Deudas" arriba del
   contenido del perfil que navegue de regreso a `/debts` (no hace falta restaurar los filtros
   exactos, alcanza con volver a la sección; si preferís usar `router.back()` de Next.js en lugar
   de un link fijo a `/debts`, también es válido y preserva mejor el estado si el navegador lo
   permite).

RESTRICCIONES ESTRICTAS:
- No implementes persistencia completa de filtros en la URL de `/debts` en esta tarea — alcanza
  con el enlace de vuelta hacia la sección.
- No modifiques el resto de la página de perfil de cliente más allá de agregar ese enlace
  condicional.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/debts-list.tsx`, página de detalle de cliente existente (ruta real a confirmar, ej. `src/app/customers/[id]/page.tsx`)

---

## SECCIÓN: USUARIOS

---

### TAREA 094 · 🟡 MEDIO
**Estado activo / inactivo sin eliminar el historial de ventas del usuario**

**Descripción:** Poder desactivar el acceso de un usuario (para que ya no pueda iniciar sesión) sin tener que eliminarlo, preservando su historial de ventas y auditoría.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: el modelo `User` no tiene hoy ningún campo de estado activo/inactivo. La
   única forma actual de "dar de baja" un usuario es `DELETE /api/users/[id]`
   (`deleteUser` en `src/modules/users/user-data-access.ts`), un borrado real — revisá esa función
   para entender si hoy bloquea el borrado cuando el usuario tiene ventas asociadas (`sellerId`)
   o auditoría (`AuditLog.userId`) antes de decidir el mensaje de ayuda a mostrar en la UI.
2. Agregá el campo `active Boolean @default(true)` al modelo `User` en
   `prisma/schema.prisma`. Corré `npx prisma migrate dev --name add-user-active`.
3. En `src/app/api/auth/login/route.ts`, después de encontrar al usuario por email y verificar la
   contraseña, agregá una verificación: si `user.active === false`, devolvé
   `NextResponse.json({ error: "Usuario desactivado. Contactá al propietario de la cuenta." },
   { status: 401 })` sin crear la sesión.
4. Agregá una función `setUserActive(id, active, tenantId, currentUserId)` en
   `src/modules/users/user-data-access.ts` (mismo patrón de scoping por tenant y de impedir que
   un usuario se desactive a sí mismo, si `deleteUser` ya tiene esa protección, replicala acá) y
   `PATCH /api/users/[id]/active` (o extendé el `PATCH` existente en
   `src/app/api/users/[id]/route.ts` para aceptar un campo `active` además de `role` — verificá
   cuál opción es más simple sin romper el uso actual de ese endpoint para cambiar el rol).
5. En `src/app/ui/users-list.tsx`, agregá un badge de estado (Activo/Inactivo) por usuario y un
   botón para alternar el estado (ej. junto al ícono de eliminar), con una confirmación simple
   antes de desactivar. Un usuario desactivado no debe poder eliminarse todavía por esta tarea —
   la desactivación es la alternativa preferida al borrado cuando el usuario tiene historial.

RESTRICCIONES ESTRICTAS:
- Esta tarea SÍ requiere migración de Prisma (campo nuevo con default `true`, no afecta usuarios
  existentes: todos quedan activos).
- No elimines la funcionalidad de borrado existente (`DELETE /api/users/[id]`), solo agregá la
  alternativa de desactivación.
- No implementes cierre de sesión forzado de usuarios ya logueados al desactivarlos — alcanza con
  bloquear el próximo intento de login; documentalo como limitación conocida en el reporte.
- Ejecutá `npm run build` y la migración antes de dar la tarea por terminada.
```

**Archivos afectados:** `prisma/schema.prisma`, `src/app/api/auth/login/route.ts`, `src/modules/users/user-data-access.ts`, `src/app/api/users/[id]/route.ts`, `src/app/ui/users-list.tsx`

---

## SECCIÓN: CONFIGURACIÓN

---

### TAREA 095 · 🟡 MEDIO
**Personalización del ticket: logo, pie de página, mensaje de agradecimiento**

**Descripción:** Poder personalizar el ticket impreso con un logo, un pie de página y un mensaje de agradecimiento propios del negocio.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: el ticket que se imprime hoy (`handlePrintTicket` dentro de `PrintModal`, en
   `src/app/ui/pos.tsx`) arma un HTML inline con `businessName` (traído de `GET /api/settings`)
   pero sin logo, sin pie de página personalizable ni mensaje de agradecimiento — es un template
   fijo.
2. Agregá al modelo `StoreSettings` en `prisma/schema.prisma` los campos `logoUrl String
   @default("")`, `receiptFooterMessage String @default("")` y `receiptThankYouMessage String
   @default("¡Gracias por su compra!")`. Corré
   `npx prisma migrate dev --name add-receipt-customization`.
3. En `src/app/ui/settings.tsx`, dentro de la categoría existente que corresponda (revisá si ya
   hay una sección "Documentos" o similar entre las `CATEGORIES` ya definidas, o agregá los
   nuevos campos a una sección de "Ticket"/"Documentos" existente si aplica), agregá tres campos
   editables: URL de logo (input de texto simple, sin subida de archivos en esta tarea), mensaje
   de pie de página, y mensaje de agradecimiento — guardados vía el mismo endpoint `PATCH
   /api/settings` ya usado para el resto de los campos de `StoreSettings`.
4. En `handlePrintTicket` (`pos.tsx`), traé estos tres campos junto con `businessName` (mismo
   fetch a `/api/settings` ya existente) e inyectalos en el HTML del ticket: el logo como `<img
   src="...">` centrado arriba del nombre del negocio (si `logoUrl` no está vacío), el mensaje de
   agradecimiento reemplazando o acompañando el texto fijo actual, y el pie de página al final del
   ticket, antes de cerrar el `</body>`.

RESTRICCIONES ESTRICTAS:
- Esta tarea SÍ requiere migración de Prisma (3 campos nuevos con default, no rompen tenants
  existentes).
- No implementes subida de archivos/imágenes en esta tarea — el logo es una URL externa que el
  usuario pega manualmente.
- No modifiques el resto del template del ticket (estructura de ítems, totales, QR/CAE de
  factura si existe) más allá de agregar logo/pie/agradecimiento.
- Ejecutá `npm run build` y la migración antes de dar la tarea por terminada.
```

**Archivos afectados:** `prisma/schema.prisma`, `src/app/ui/settings.tsx`, `src/app/ui/pos.tsx`

---

### TAREA 096 · 🟡 MEDIO
**Datos AFIP completos: punto de venta, condición IVA, tipo de responsable**

**Descripción:** Tener disponibles todos los datos fiscales necesarios para facturación electrónica: punto de venta, condición frente al IVA y tipo de responsable.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Verificá primero: esta funcionalidad YA ESTÁ IMPLEMENTADA de punta a punta. El modelo
`TenantARCAConfig` ya tiene `cuit`, `puntoVenta` y `condicionIVA` ("RI" | "MONO"), y la sección
"Facturación Electrónica ARCA/AFIP" en `src/app/ui/settings.tsx` (`FacturacionARCASection`) ya
expone los tres campos como formulario editable, conectado a `GET`/`PATCH
/api/tenants/arca-config`. En términos de AFIP argentino, "condición IVA" (Responsable Inscripto /
Monotributista) ES el "tipo de responsable" pedido — no son dos conceptos separados que falte
agregar.

Si al revisar el código confirmás que los tres datos (CUIT, punto de venta, condición
IVA/responsable) ya están completos y son editables desde esa sección, NO hagas ningún cambio:
documentá en el reporte que la tarea ya estaba implementada, aclarando explícitamente que
"condición IVA" cubre el campo de "tipo de responsable" pedido en la tarea. Si encontrás que el
selector de `condicionIVA` solo ofrece "RI"/"MONO" y falta alguna condición fiscal real y
relevante para el proyecto (ej. "Exento"), podés agregar esa opción puntual al `<select>`
existente sin modificar el resto de la sección ni el schema.

RESTRICCIONES ESTRICTAS:
- No modifiques `TenantARCAConfig` en el schema salvo que agregues una opción de valor a
  `condicionIVA` (que ya es un `String`, no un enum — no requiere migración agregar un valor
  válido nuevo, solo la opción en el `<select>` del cliente).
- No modifiques la lógica de facturación ARCA/AFIP en sí (certificados, tokens, emisión).
- Si hacés cambios, ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/settings.tsx` (solo si falta una opción de condición fiscal)

---

### TAREA 097 · 🟠 ALTO
**Número inicial de comprobante (para migrar desde otro sistema con historial)**

**Descripción:** Permitir configurar desde qué número debe empezar la numeración de comprobantes (tickets/facturas), para negocios que ya tenían un historial de ventas en otro sistema y quieren continuar la numeración sin reiniciar desde 1.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero cómo se genera hoy `Sale.receiptNumber` (buscá dónde se calcula al crear una
   venta, probablemente en `src/modules/sales/sale-data-access.ts`, similar al patrón ya visto de
   `folio` en `confirmQuote` de `quote-data-access.ts`: se busca la última venta por
   `receiptNumber`/`folio` descendente y se suma 1). Confirmá si `receiptNumber` y `folio` son el
   mismo contador o contadores separados antes de tocar nada.
2. Agregá el campo `initialReceiptNumber Int @default(0)` al modelo `StoreSettings` en
   `prisma/schema.prisma`. Corré `npx prisma migrate dev --name add-initial-receipt-number`.
3. En el punto donde se calcula el próximo `receiptNumber` al crear una venta, cambiá la lógica
   para que, **si el tenant todavía no tiene ninguna venta registrada** (no hay una venta previa
   de la cual tomar el último número) y `initialReceiptNumber > 0`, el primer comprobante use ese
   valor configurado en lugar de arrancar en 1; para todas las ventas subsiguientes, seguí
   incrementando normalmente a partir del último `receiptNumber` real ya emitido (el campo de
   configuración solo afecta el arranque, nunca reordena ni reescribe numeración ya emitida).
4. Agregá el campo `initialReceiptNumber` como input numérico en la sección de configuración de
   documentos/facturación de `src/app/ui/settings.tsx` (junto a los campos de la Tarea 096 si
   aplica, o en la categoría "documentos" ya listada entre `CATEGORIES` si ya tiene contenido
   propio), con una nota clara de que solo aplica antes de la primera venta emitida.

RESTRICCIONES ESTRICTAS:
- Esta tarea SÍ requiere migración de Prisma (campo nuevo con default `0`, sin efecto para
  tenants que ya tienen ventas).
- No reescribas ni renumeres comprobantes ya emitidos bajo ninguna circunstancia.
- No modifiques la lógica de facturación ARCA/AFIP (esto es solo para el número de comprobante
  interno, no para la numeración fiscal de facturas electrónicas si son contadores distintos —
  verificalo antes de tocar código).
- Ejecutá `npm run build` y la migración antes de dar la tarea por terminada.
```

**Archivos afectados:** `prisma/schema.prisma`, `src/modules/sales/sale-data-access.ts` (o el archivo real donde se calcula `receiptNumber`), `src/app/ui/settings.tsx`

---

### TAREA 098 · 🟠 ALTO
**IVA por defecto configurable para nuevos productos sin IVA definido**

**Descripción:** Poder configurar cuál es la alícuota de IVA que se aplica por defecto a los productos nuevos, en vez de tener un valor fijo hardcodeado.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: `Product.ivaRate` tiene hoy `@default(0.21)` fijo en el schema de Prisma, y
   `src/app/ui/product-form.tsx` probablemente inicializa el campo IVA del formulario con ese
   mismo valor hardcodeado (0.21) al crear un producto nuevo. Los valores válidos de IVA ya están
   centralizados en `IVA_RATES` (`src/modules/products/product-validation.ts`): `[0, 0.105, 0.21,
   0.27]`.
2. Agregá el campo `defaultIvaRate Float @default(0.21)` al modelo `StoreSettings` en
   `prisma/schema.prisma`. Corré `npx prisma migrate dev --name add-default-iva-rate`.
3. En `src/app/ui/settings.tsx`, agregá un selector "IVA por defecto para productos nuevos" con
   las opciones de `IVA_RATES` (mismo formato de etiqueta ya usado en `product-form.tsx` para
   mostrar los porcentajes), guardado vía `PATCH /api/settings`.
4. En `src/app/ui/product-form.tsx`, al crear un producto nuevo (no al editar uno existente), en
   lugar de inicializar el campo de IVA con `0.21` fijo, traé el valor configurado en
   `StoreSettings.defaultIvaRate` (`GET /api/settings`, mismo patrón de fetch ya usado en ese
   archivo o en otros del proyecto) y usalo como valor inicial. Si el fetch falla o el tenant no
   configuró nada, mantené `0.21` como fallback (mismo valor que el default actual, para no
   romper el comportamiento existente).

RESTRICCIONES ESTRICTAS:
- Esta tarea SÍ requiere migración de Prisma (campo nuevo con default `0.21`, mismo valor que hoy
  está hardcodeado, así que no cambia el comportamiento hasta que alguien lo configure distinto).
- No modifiques `Product.ivaRate` en el schema (su default sigue en `0.21` como está, ya que
  representa el valor por producto, no el default configurable global).
- No cambies el IVA de productos ya existentes.
- Ejecutá `npm run build` y la migración antes de dar la tarea por terminada.
```

**Archivos afectados:** `prisma/schema.prisma`, `src/app/ui/settings.tsx`, `src/app/ui/product-form.tsx`

---

### TAREA 099 · 🟡 MEDIO · UX
**Secciones colapsables: Negocio / Fiscal / Sistema / Usuarios**

**Descripción:** Organizar la configuración en secciones colapsables (Negocio, Fiscal, Sistema, Usuarios) para que sea más fácil de navegar en vez de un formulario largo y continuo.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: hoy la navegación de Configuración (`src/app/settings/components/
   SettingsNav.tsx`) ya separa "Mi Negocio", "Usuarios" y "Suscripción" como páginas distintas
   (vía query param `?s=`). Dentro de "Mi Negocio" (`src/app/ui/settings.tsx`), sin embargo, hay
   un conjunto largo de secciones (datos del negocio, regional, ARCA/facturación, toggles de
   sistema, etc. — revisá las `CATEGORIES` ya definidas en ese archivo) que hoy se navegan con una
   barra lateral de categorías, no con secciones colapsables dentro de una misma vista.
2. Sin rediseñar toda la navegación existente ni romper `SettingsNav.tsx`, agrupá el contenido de
   `settings.tsx` en 3 bloques colapsables (usando un componente `<details>`/acordeón simple, o un
   estado `expandedSections` con toggle): "Negocio" (datos del negocio, contacto, regional),
   "Fiscal" (CUIT/IVA y la sección ARCA/AFIP existente, más los campos de las Tareas 096-098 si ya
   están implementadas), y "Sistema" (los toggles de impresora/sonidos/modo oscuro/notificaciones
   ya existentes en `TogglesConfig`). Cada bloque debe poder expandirse/colapsarse
   independientemente, con al menos uno expandido por defecto para no ocultar todo el contenido de
   entrada.
3. La sección "Usuarios" ya vive en su propia página (`UsuariosPanel`, navegable desde
   `SettingsNav`) — no hace falta convertirla en un bloque colapsable dentro de la misma vista;
   alcanza con que las 3 primeras queden agrupadas de forma colapsable dentro de "Mi Negocio".

RESTRICCIONES ESTRICTAS:
- No elimines ni renombres las categorías/páginas existentes de `SettingsNav.tsx`.
- No cambies el comportamiento de guardado de ningún campo, solo la agrupación visual.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/settings.tsx`

---

### TAREA 100 · 🟡 MEDIO · UX
**Preview en tiempo real del ticket mientras se edita el pie de página**

**Descripción:** Mientras se configura el logo, el pie de página o el mensaje de agradecimiento del ticket, mostrar una vista previa del ticket actualizándose en tiempo real.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Esta tarea depende de que la Tarea 095 (personalización de ticket: logo, pie de página, mensaje
de agradecimiento) ya esté implementada. Si todavía no lo está, implementala primero siguiendo
esa tarea antes de continuar con esta.

1. En la sección de personalización de ticket agregada en la Tarea 095
   (`src/app/ui/settings.tsx`), agregá un panel de vista previa al lado o debajo del formulario:
   un `<div>` con estilo monoespaciado angosto (mismo criterio visual que el ticket real —
   `font-family: monospace`, ancho acotado tipo recibo, fondo blanco con borde) que muestre, en
   tiempo real mientras se edita el formulario (sin necesidad de guardar):
   - El logo (`<img>`, si `logoUrl` no está vacío) centrado arriba.
   - El nombre del negocio (`businessName` ya cargado en el formulario).
   - Un par de líneas de ítems de ejemplo fijas (ej. "Producto de ejemplo x1 — $1000") solo para
     dar contexto visual, con un total de ejemplo.
   - El mensaje de agradecimiento (`receiptThankYouMessage`) y el pie de página
     (`receiptFooterMessage`) tal como quedarían impresos, actualizándose en cada cambio del
     input correspondiente (usando el mismo estado del formulario ya controlado, sin necesidad de
     un fetch adicional).
2. Este preview es puramente visual en React (no reutilices `handlePrintTicket`de `pos.tsx`, que
   genera un string HTML separado para impresión real — el preview es un componente aparte, más
   simple, solo para visualizar mientras se edita).

RESTRICCIONES ESTRICTAS:
- No modifiques `handlePrintTicket` ni el flujo real de impresión en `pos.tsx`.
- No agregues llamadas nuevas a la API — el preview se arma con el estado que ya tiene el
  formulario mientras el usuario escribe.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/settings.tsx`

---

## PROTOCOLO DE REPORTE

Después de ejecutar cada tarea, el agente debe actualizar `TAREAS/REPORTE_DE_CAMBIOS.md`
con la siguiente estructura (agregar arriba de todo, del más reciente al más antiguo):

```
## Tarea XXX — [Nombre] — [fecha hora]
**Estado:** ✅ Completada | ⚠️ Parcial | ❌ Error
**Archivos modificados:** lista de archivos
**Cambios realizados:** descripción breve
**Notas:** problemas encontrados, decisiones tomadas, pendientes si los hay
---
```

Reglas del ciclo:
1. Ejecutar únicamente la tarea pedida — no adelantar otras tareas del lote.
2. Commitear los cambios con mensaje descriptivo (`feat: ...` / `fix: ...`) antes de reportar.
3. Diego hace `git push` manualmente desde su PC — el agente no pushea, salvo que la orden
   indique explícitamente "sube todo a github".
4. No borrar tareas de este archivo ni marcarlas como completadas acá — el estado se
   actualiza en Notion y en `REPORTE_DE_CAMBIOS.md` únicamente.
5. Las tareas 084, 090, 094, 095, 097 y 098 requieren migración de Prisma
   (`npx prisma migrate dev`) — correr la migración es parte de "completar" esas tareas, no un
   paso opcional.
6. Las tareas 085, 092 y 096 pueden estar ya implementadas total o parcialmente — leer primero
   el código real antes de escribir, y si ya funcionan como se pide, documentarlo en el reporte
   sin modificar archivos innecesariamente.
7. La Tarea 086 depende de la Tarea 084 (campo `dueDate`). La Tarea 100 depende de la Tarea 095
   (campos de personalización de ticket). Verificar que la tarea de la que dependen esté resuelta
   antes de empezar.
