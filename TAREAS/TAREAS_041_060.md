# SOLVEN — Tareas de Producto · Lote 3 (041–060)

> **Instrucciones para Claude (sección Código / Code):**
>
> Cuando diga "ejecuta la tarea X": encontrá la tarea por número, ejecutá **únicamente** lo que
> indica su prompt, hacé commit de los cambios con mensaje descriptivo, y actualizá
> `TAREAS/REPORTE_DE_CAMBIOS.md` con el resultado. sube todo a github.
> **Regla de oro: no modifiques lógica de negocio, BD ni archivos fuera de los indicados. Ante la duda, menos es más.**
> Fuente: base de datos Notion "SOLVEN — Gestión de Tareas", tareas TAREA-041 a TAREA-060.

---

## SECCIÓN: CAJA

---

### TAREA 041 · 🟡 MEDIO · UX
**Movimientos por tipo con color: VENTA=verde · GASTO=naranja · RETIRO=rojo · AJUSTE=gris**

**Descripción:** En el listado de movimientos de caja, cada fila debe distinguirse visualmente por color según su origen (venta, gasto, retiro manual, ajuste).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero cómo se clasifican hoy los movimientos: el modelo `CashMovement` tiene
   `type` ("IN" | "OUT") y `source` (valores 
   
    en uso: "SALE", "EXPENSE", "DEBT_PAYMENT",
   "MANUAL", "RETURN"). No hay un valor "AJUSTE" ni "RETIRO" literal en `source` — un retiro
   manual de caja se registra como `source: "MANUAL"` con `type: "OUT"`.
2. En `src/app/ui/cash-movements-list.tsx`, agregá una función que derive una categoría visual

   a partir de `source` + `type`:
   - "SALE" → Venta (verde, ej. emerald-600/emerald-100).
   - "EXPENSE" → Gasto (naranja/orange-600).
   - "MANUAL" + type "OUT" → Retiro (rojo/rose-600).
   - "MANUAL" + type "IN", "DEBT_PAYMENT", "RETURN" u otro → Ajuste/Otro (gris/slate-600).
3. Aplicá esta categoría como color de texto o badge en cada fila del listado, junto al tipo de
   movimiento que ya se muestra. Mantené el ícono de flecha (entrada/salida) existente si lo hay,
   agregando el color como refuerzo visual, no como reemplazo de información ya mostrada.

RESTRICCIONES ESTRICTAS:
- No modifiques el schema de Prisma ni los valores reales de `source`/`type` que ya graban los
  módulos de ventas, gastos, deudas y retornos.
- Solo cash-movements-list.tsx.
- Ejecutá `npm run build` al final para verificar TypeScript.
```

**Archivos afectados:** `src/app/ui/cash-movements-list.tsx`

---

### TAREA 042 · 🟡 MEDIO · UX
**Botón 'Cerrar Caja' con confirmación de dos pasos y resumen final**

**Descripción:** El botón para cerrar la caja debe pedir una confirmación explícita de dos pasos antes de ejecutar el cierre, mostrando un resumen final de lo que se va a cerrar.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/cash-register-close.tsx`:
1. Localizá el botón final "Cerrar caja y finalizar" (el que dispara el submit del cierre).
2. Antes de ejecutar el cierre real, agregá un paso de confirmación intermedio: al hacer clic
   por primera vez, mostrá un modal/panel de confirmación con un resumen final (monto de
   apertura, monto esperado, monto contado/`closingAmount`, diferencia, y cajero) y dos botones:
   "Cancelar" y "Confirmar cierre".
3. Solo al confirmar en ese segundo paso se debe ejecutar el `PUT` real al endpoint de cierre de
   caja (el mismo que ya usa el formulario).
4. Si el usuario cancela, debe volver al formulario de cierre sin haber enviado nada al backend.

RESTRICCIONES ESTRICTAS:
- No modifiques el cálculo de `expectedAmount`, `closingAmount` ni `difference`.
- No modifiques el endpoint de cierre de caja, solo el flujo de confirmación en el cliente.
- Solo cash-register-close.tsx.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/cash-register-close.tsx`

---

## SECCIÓN: INVENTARIO

---

### TAREA 043 · 🟡 MEDIO
**Motivo obligatorio en ajustes negativos (pérdida, rotura, vencimiento, merma)**

**Descripción:** Al ajustar el stock de un producto hacia abajo, el motivo debe ser obligatorio y elegido de una lista estructurada (no texto libre), para tener trazabilidad real de las causas de merma.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: `src/app/ui/inventory-adjust-form.tsx` (formulario completo de ajuste, en
   /inventory/adjust) YA tiene un selector estructurado de tipo/motivo (`TIPOS_AJUSTE` y
   `MOTIVOS_BY_TIPO`, ambos obligatorios). El modal rápido `AdjustStockModal` dentro de
   `src/app/products/components/InventoryTab.tsx` (el que se abre con "Ajustar stock" desde la
   tabla de inventario) en cambio solo pide un campo de texto libre `reason`.
2. En `AdjustStockModal` (dentro de InventoryTab.tsx): cuando el nuevo stock ingresado sea menor
   al stock actual del producto (ajuste negativo), reemplazá el input de texto libre por un
   selector con las mismas categorías ya usadas en inventory-adjust-form.tsx: "Pérdida /
   Deterioro", "Conteo físico", "Daño", "Robo", "Donación", "Corrección", "Otro", y un segundo
   selector de motivo específico dependiente del tipo elegido (reusá `MOTIVOS_BY_TIPO` o una
   copia equivalente). Ambos campos deben ser obligatorios para poder confirmar el ajuste.
3. Si el ajuste es positivo o igual (no es una baja de stock), mantené el campo de texto libre
   actual sin cambios.
4. Componé el `reason` final enviado al API igual que ya lo hace inventory-adjust-form.tsx
   (tipo + motivo, ej. "Pérdida / Deterioro - Vencimiento de producto").

RESTRICCIONES ESTRICTAS:
- No modifiques el endpoint `/api/inventory-adjustments` ni el schema de Prisma.
- No dupliques las constantes si podés importarlas o extraerlas a un módulo compartido; si
  extraerlas complica el alcance, es aceptable duplicarlas con los mismos valores.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/products/components/InventoryTab.tsx`

---

### TAREA 044 · 🟡 MEDIO · UX
**Confirmación antes de ajuste negativo de stock (prevenir errores)**

**Descripción:** Antes de confirmar un ajuste que reduce el stock de un producto, se debe pedir una confirmación explícita para evitar bajas accidentales.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `AdjustStockModal` dentro de `src/app/products/components/InventoryTab.tsx`:
1. Cuando el nuevo stock ingresado sea menor al stock actual (`difference < 0`, ya calculado en
   el componente), al hacer clic en "Guardar ajuste" mostrá un paso de confirmación intermedio
   dentro del mismo modal (no un modal nuevo aparte) indicando claramente: producto, stock
   actual, nuevo stock y la cantidad que se va a dar de baja, con botones "Volver" y "Confirmar
   ajuste".
2. Solo al confirmar ese segundo paso se debe enviar el `POST` a `/api/inventory-adjustments`.
3. Si el ajuste es positivo o sin cambio, mantené el comportamiento actual (guardar directo, sin
   paso adicional).

RESTRICCIONES ESTRICTAS:
- No modifiques el endpoint de ajustes ni el schema de Prisma.
- No modifiques `inventory-adjust-form.tsx` (formulario completo, fuera de alcance de esta tarea).
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/products/components/InventoryTab.tsx`

---

### TAREA 045 · 🟡 MEDIO · UX
**Movimientos negativos resaltados en rojo en el historial**

**Descripción:** Los movimientos de inventario que representan una baja de stock deben resaltarse claramente en rojo en el historial, no solo en el número de cantidad.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: en `MovementsList` (dentro de
   `src/app/products/components/InventoryTab.tsx`), la cantidad (`quantityChange`) ya se muestra
   en rojo (`text-rose-700`) cuando es negativa, y el ícono circular ya cambia a
   `bg-rose-100`/`ArrowDown` para salidas. Falta resaltar la fila completa.
2. Agregá un resaltado sutil a nivel de fila (`<tr>`) cuando `quantityChange < 0`: un borde
   izquierdo rojo (ej. `border-l-2 border-rose-400`) o un fondo muy suave (`bg-rose-50/40`),
   sin romper el hover existente (`hover:bg-slate-50/50`).
3. Aplicá el mismo criterio en `RecentMovements` (sidebar de "Últimos movimientos"), como un
   borde o fondo sutil en la tarjeta de cada movimiento negativo.

RESTRICCIONES ESTRICTAS:
- No dupliques la lógica de `getMovementType` — usá directamente `m.quantityChange < 0`.
- No modifiques el endpoint de movimientos ni el schema.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/products/components/InventoryTab.tsx`

---

### TAREA 046 · 🟠 ALTO
**Filtro por producto o por fecha en el historial de movimientos**

**Descripción:** El historial de movimientos de inventario debe poder filtrarse por producto específico y por rango de fechas, además de la búsqueda de texto y las pestañas por tipo que ya existen.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/products/components/InventoryTab.tsx`, dentro de las pestañas "Movimientos",
"Entradas", "Salidas" y "Ajustes":
1. Agregá, junto al buscador de texto ya existente, un selector de producto específico (opciones
   derivadas de los productos ya cargados en `products`, con una opción "Todos los productos") y
   un filtro de rango de fechas (desde/hasta, tipo `<input type="date">`), aplicados sobre
   `movements` ya fetcheados en el cliente (sin llamadas nuevas a la API).
2. Combiná estos filtros con la búsqueda de texto y la pestaña activa ya existentes en
   `filteredMovements` (todos deben aplicarse en conjunto, no reemplazarse entre sí).
3. Al cambiar cualquier filtro, reseteá la paginación a la página 1 (mismo patrón que ya usa
   `handleTabChange`).
4. Incluí un botón "Limpiar filtros" que resetee producto y fechas (no afecta la búsqueda de
   texto ni la pestaña activa).

RESTRICCIONES ESTRICTAS:
- No modifiques `/api/inventory-movements` ni el schema.
- El filtrado es puramente client-side sobre los movimientos ya cargados.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/products/components/InventoryTab.tsx`

---

### TAREA 047 · 🟡 MEDIO
**Exportar movimientos de inventario a CSV / Excel por período**

**Descripción:** Agregar un botón para exportar a CSV los movimientos de inventario actualmente visibles/filtrados.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/products/components/InventoryTab.tsx`, en las pestañas de movimientos:
1. Agregá un botón "Exportar CSV" junto a los filtros (buscador y, si ya está implementada, la
   Tarea 046 de filtro por producto/fecha).
2. Generá el CSV en el cliente (sin librerías nuevas, mismo patrón ya usado en
   `sales-list.tsx`/`cash-movements-list.tsx`: `escapeCsvValue` + `Blob` +
   `URL.createObjectURL` + click programático + `revokeObjectURL`) a partir de
   `filteredMovements` (los movimientos actualmente visibles/filtrados), con columnas: Fecha,
   Producto, Tipo (entrada/salida/ajuste), Motivo, Cantidad.
3. Descargá con nombre `movimientos_inventario_YYYY-MM-DD.csv` (fecha actual).

RESTRICCIONES ESTRICTAS:
- No agregues librerías externas de CSV.
- No modifiques `/api/inventory-movements`.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/products/components/InventoryTab.tsx`

---

### TAREA 048 · 🟡 MEDIO
**Dashboard de inventario: valor total · productos críticos · sin movimiento**

**Descripción:** Ampliar las métricas del inventario para incluir el valor monetario total del stock y los productos que no han tenido movimientos recientes, además de las métricas de stock ya existentes.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/products/components/InventoryTab.tsx`, ya existen 4 tarjetas de métricas (Total
productos, Con stock, Stock bajo, Sin stock). Agregá:
1. Una tarjeta "Valor total de inventario": suma de `stock × costPrice` de todos los productos
   cargados (mismo patrón de cálculo client-side que la Tarea 049, podés compartir la función si
   ya la escribiste para esa tarea), formateada en ARS.
2. Una tarjeta o sección "Sin movimiento reciente": productos cuyo último `InventoryMovement`
   (por `productId`, usando los `movements` ya cargados) es de hace más de 30 días, o que no
   tienen ningún movimiento registrado. Mostrá el conteo y, al hacer clic, filtrá o resaltá esos
   productos (reusá el patrón de "Ver alertas →" ya existente para navegar a una vista filtrada).
3. Si agregar una 5ta/6ta tarjeta rompe el grid de 4 columnas, ajustá el grid
   (`lg:grid-cols-4` → `lg:grid-cols-6` o similar) sin romper el diseño responsive.

RESTRICCIONES ESTRICTAS:
- No modifiques `/api/inventory-movements` ni `/api/products`; el cálculo es sobre los datos ya
  fetcheados en el cliente.
- No modifiques el schema de Prisma.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/products/components/InventoryTab.tsx`

---

### TAREA 049 · 🟠 ALTO
**Valorización del inventario: stock actual × precio costo = valor total en $**

**Descripción:** Mostrar, para cada producto y como total general, el valor monetario del inventario (stock actual multiplicado por su precio de costo).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/products/components/InventoryTab.tsx`, pestaña "Stock actual" (`StockTable`):
1. Agregá una columna "Valor en stock" a la tabla, calculada como
   `product.stock × parseFloat(product.costPrice)` por fila, formateada en ARS.
2. Agregá un total general (suma de todas las filas visibles, no solo la página actual — usá
   `filteredProducts`, no `paginatedProducts`) visible en el header de la tabla o como resumen
   junto a la paginación.
3. Si ya implementaste la tarjeta "Valor total de inventario" de la Tarea 048, reusá la misma
   función de cálculo para evitar lógica duplicada.

RESTRICCIONES ESTRICTAS:
- No modifiques `/api/products` ni el schema de Prisma (`costPrice` ya viene en la respuesta).
- Cálculo puramente client-side.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/products/components/InventoryTab.tsx`

---

### TAREA 050 · 🟠 ALTO
**Conteo físico: ingresar stock real y que SOLVEN calcule la diferencia**

**Descripción:** Agregar un flujo dedicado de conteo físico donde se ingresa el stock real contado y el sistema calcula automáticamente la diferencia contra el stock registrado, antes de confirmar el ajuste.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: `AdjustStockModal` (en `src/app/products/components/InventoryTab.tsx`) ya
   calcula `difference = newStock - product.stock` en tiempo real mientras se escribe, pero está
   enmarcado como "Ajustar stock" genérico, no como un flujo de "Conteo físico".
2. Agregá una opción/modo "Conteo físico" (podés agregar un botón "Conteo físico" junto a "Ajuste
   de inventario" y "Entrada de stock" en el header de InventoryTab, o un toggle dentro del
   modal existente) que:
   a. Muestre claramente "Stock registrado" (`product.stock`) y un input "Stock real contado".
   b. Calcule y muestre la diferencia (`stock contado - stock registrado`) con color verde si es
      positiva, rojo si es negativa, gris si es cero, y un texto explicativo ("Sobrante de X
      unidades" / "Faltante de X unidades" / "Sin diferencia").
   c. Al confirmar, envíe el mismo `POST /api/inventory-adjustments` con `newStock` = stock
      contado y `reason` = "Conteo físico" (aplicá también la Tarea 044 de confirmación si ya
      está implementada, ya que un conteo físico con faltante es un ajuste negativo).
3. Si preferís reusar `AdjustStockModal` en vez de crear un componente nuevo, es válido siempre
   que la diferencia y el lenguaje de "conteo físico" queden claros para el usuario.

RESTRICCIONES ESTRICTAS:
- No modifiques el endpoint `/api/inventory-adjustments` ni el schema de Prisma.
- No dupliques innecesariamente el modal de ajuste si podés extenderlo.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/products/components/InventoryTab.tsx`

---

### TAREA 051 · 🟠 ALTO
**Stock mínimo y máximo configurable por producto con alertas automáticas**

**Descripción:** Permitir configurar un stock máximo por producto (el mínimo ya existe) y que las alertas de stock bajo del inventario usen el umbral configurado por producto en lugar de un valor fijo genérico.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: `Product.minStock` YA EXISTE en el schema, en `product-validation.ts` y en
   el formulario `product-form.tsx` (campo "Stock mínimo"). NO existe un campo `maxStock`. Las
   alertas de stock bajo en `InventoryTab.tsx` hoy usan un umbral fijo hardcodeado (`stock <= 5`
   en `lowStockCount`, `alertProducts`, `StockStatusBadge`, etc.), ignorando el `minStock` de cada
   producto.
2. Agregá el campo `maxStock Int?` al modelo `Product` en `prisma/schema.prisma` y corré
   `npx prisma migrate dev --name add-product-max-stock` (o el comando de migración equivalente
   ya usado en el proyecto) para generar la migración.
3. En `src/modules/products/product-validation.ts`, agregá `maxStock` opcional a
   `CreateProductInput`/`UpdateProductInput` con la misma validación que `minStock` (entero no
   negativo si está presente).
4. En `src/app/ui/product-form.tsx`, el campo "Stock mínimo" ya existe; agregá un campo análogo
   "Stock máximo" (opcional) y enviá `maxStock` en el payload de creación/edición.
5. En `src/app/products/components/InventoryTab.tsx`, reemplazá el umbral fijo `<= 5` por el
   `minStock` propio de cada producto (fallback a 5 solo si `minStock` es 0 o no está definido,
   para no romper productos existentes sin configurar) en `lowStockCount`, `alertProducts`,
   `StockStatusBadge` y `StockRow`.

RESTRICCIONES ESTRICTAS:
- Esta tarea SÍ requiere una migración de Prisma (agrega un campo nuevo). No modifiques ni
  elimines ningún otro campo del modelo Product.
- No cambies el comportamiento de ventas ni de otros módulos que ya usan `stock`/`minStock`.
- Ejecutá `npm run build` y la migración antes de dar la tarea por terminada.
```

**Archivos afectados:** `prisma/schema.prisma`, `src/modules/products/product-validation.ts`, `src/app/ui/product-form.tsx`, `src/app/products/components/InventoryTab.tsx`

---

## SECCIÓN: PRODUCTOS Y SERVICIOS

---

### TAREA 052 · 🟡 MEDIO
**Edición masiva: cambiar precio, categoría o IVA en múltiples productos a la vez**

**Descripción:** Permitir seleccionar varios productos en el listado y aplicarles un cambio masivo de precio (ajuste porcentual o fijo), categoría o alícuota de IVA.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. En `src/app/ui/products-inventory.tsx` (listado principal de productos), agregá checkboxes
   de selección por fila y un checkbox "Seleccionar todos" en el header de la tabla.
2. Cuando haya productos seleccionados, mostrá una barra de acciones masivas con:
   - Cambiar categoría (selector con las categorías ya disponibles vía `/api/categories`).
   - Cambiar alícuota de IVA (selector con los valores ya válidos: 0, 10.5%, 21%, 27% —
     mismos valores que `IVA_RATES` en `product-validation.ts`).
   - Ajustar precio de venta: por porcentaje (ej. +10%) o por monto fijo.
3. En `src/app/api/products/route.ts` o un endpoint nuevo `src/app/api/products/bulk/route.ts`
   (verificá primero si conviene reusar el PUT existente por producto o crear un endpoint de
   bulk update), agregá soporte para aplicar el cambio a la lista de IDs seleccionados, validando
   cada campo con `validateUpdateProductInput` ya existente, dentro de una transacción de Prisma.
4. Mostrá un resumen de confirmación antes de aplicar ("Vas a modificar N productos: ...") y un
   mensaje de éxito/error al finalizar.

RESTRICCIONES ESTRICTAS:
- Reusá la validación existente de `product-validation.ts`, no dupliques reglas de negocio.
- No modifiques el schema de Prisma (no hace falta para esta tarea).
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/products-inventory.tsx`, `src/app/api/products/route.ts` (o nuevo endpoint de bulk update)

---

### TAREA 053 · 🟡 MEDIO
**Proveedor asociado a cada producto para trazabilidad y pedidos**

**Descripción:** Permitir asociar un proveedor a cada producto, para saber a quién volver a comprarle y facilitar futuros pedidos de reposición.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: `src/app/ui/product-form.tsx` YA TIENE un selector "Proveedor" en la UI
   (estado `supplier`), pero está vacío (sin opciones) y no se envía al backend — es un campo
   visual sin implementar. NO existe ningún modelo `Supplier` en el schema.
2. Agregá un modelo `Supplier` a `prisma/schema.prisma` (campos mínimos: `id`, `tenantId`,
   `name`, `phone String?`, `email String?`, `createdAt`, scoped por tenant como el resto de
   modelos del proyecto) y una relación opcional `supplierId`/`supplier` en `Product`. Corré
   `npx prisma migrate dev --name add-supplier`.
3. Creá un módulo mínimo `src/modules/suppliers/` (data access + validación, mismo patrón que
   `src/modules/categories/`) y un endpoint `src/app/api/suppliers/route.ts` (GET para listar,
   POST para crear, scoped por tenantId, con `requireRole`/auth igual que el resto de endpoints).
4. En `product-form.tsx`, conectá el selector "Proveedor" a `GET /api/suppliers` (mismo patrón
   que ya usa para categorías) y el botón "+ Nuevo proveedor" a un modal simple de alta rápida
   (nombre obligatorio, teléfono/email opcionales) que llama a `POST /api/suppliers`.
5. Enviá `supplierId` en el payload de creación/edición de producto.

RESTRICCIONES ESTRICTAS:
- Esta tarea SÍ requiere migración de Prisma (modelo nuevo). No modifiques modelos existentes
  más allá de agregar la relación a Product.
- No inventes campos de proveedor fuera de los mínimos indicados.
- Ejecutá `npm run build` y la migración antes de dar la tarea por terminada.
```

**Archivos afectados:** `prisma/schema.prisma`, `src/modules/suppliers/` (nuevo), `src/app/api/suppliers/route.ts` (nuevo), `src/app/ui/product-form.tsx`

---

### TAREA 054 · 🟠 ALTO
**Unidad de medida por producto (unidad, kg, litro, metro, hora, caja)**

**Descripción:** Permitir configurar la unidad de medida de cada producto (unidad, kilogramo, litro, metro, hora, caja, etc.) y que se refleje en el listado y en el punto de venta.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: `src/app/ui/product-form.tsx` YA TIENE un selector "Unidad de medida" en la
   UI (estado `unit`, opciones en `UNITS`), pero es puramente local: no se envía al backend ni
   existe el campo en el modelo `Product`.
2. Agregá el campo `unit String @default("Unidad")` al modelo `Product` en
   `prisma/schema.prisma` y corré `npx prisma migrate dev --name add-product-unit`.
3. En `src/modules/products/product-validation.ts`, agregá `unit` opcional a
   `CreateProductInput`/`UpdateProductInput`, validando contra una lista cerrada de valores
   permitidos (mismos valores que ya están en `UNITS` en product-form.tsx: "Unidad (ud)",
   "Kilogramo (kg)", "Gramo (g)", "Litro (L)", "Mililitro (ml)", "Caja", "Bolsa", "Paquete" — y
   si el prompt de Notion pide explícitamente "hora" y "metro", sumalos a la lista), con
   fallback a "Unidad (ud)" si no es válido.
4. Conectá el selector ya existente en `product-form.tsx` para que envíe `unit` en el payload.
5. Mostrá la unidad en el listado de productos (`products-inventory.tsx`) y, si es sencillo sin
   romper el layout del carrito, junto a la cantidad en `pos.tsx` (ej. "3 kg" en vez de solo "3").

RESTRICCIONES ESTRICTAS:
- Esta tarea SÍ requiere migración de Prisma (campo nuevo con default, no rompe productos
  existentes).
- No modifiques la lógica de venta ni de stock, solo el campo informativo de unidad.
- Ejecutá `npm run build` y la migración antes de dar la tarea por terminada.
```

**Archivos afectados:** `prisma/schema.prisma`, `src/modules/products/product-validation.ts`, `src/app/ui/product-form.tsx`, `src/app/ui/products-inventory.tsx`

---

### TAREA 055 · 🟢 BAJO
**Historial de cambios de precio por producto (quién cambió, cuándo y de cuánto a cuánto)**

**Descripción:** Registrar y mostrar un historial de cambios de precio de cada producto: quién lo cambió, cuándo, y el valor anterior y nuevo.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: el proyecto YA TIENE un modelo genérico `AuditLog` (tenantId, userId,
   userCode, action, entityType, entityId, metadata Json?, createdAt) pensado exactamente para
   este tipo de trazabilidad — no hace falta crear un modelo nuevo de historial de precios.
2. En `src/modules/products/product-data-access.ts`, en `updateProduct`, cuando el `input`
   incluya `costPrice` y/o `salePrice` distintos a los valores actuales del producto, creá un
   registro en `AuditLog` con `action: "PRODUCT_PRICE_CHANGE"`, `entityType: "Product"`,
   `entityId` = id del producto, y `metadata` con `{ costPriceBefore, costPriceAfter,
   salePriceBefore, salePriceAfter }`. Verificá primero cómo se obtiene el `userId` actual en
   otros endpoints que ya escriben en `AuditLog` (buscá usos existentes de `prisma.auditLog.create`)
   para seguir el mismo patrón de autenticación/contexto.
3. Agregá un endpoint de solo lectura (o extendé uno existente) para listar el historial de
   `AuditLog` filtrado por `entityType: "Product"` y `entityId` de un producto puntual.
4. En `src/app/ui/product-detail.tsx` (o donde se muestre el detalle del producto), agregá una
   sección "Historial de precios" que liste estos cambios: fecha, usuario, precio de costo
   anterior → nuevo, precio de venta anterior → nuevo.

RESTRICCIONES ESTRICTAS:
- No crees un modelo nuevo de Prisma para esto — reusá `AuditLog`, que ya cubre el caso sin
  requerir migración.
- No modifiques el flujo de edición de producto más allá de agregar el registro de auditoría.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/modules/products/product-data-access.ts`, `src/app/ui/product-detail.tsx`, endpoint de historial (existente o nuevo, de solo lectura)

---

### TAREA 056 · 🟠 ALTO
**Cálculo automático del precio de venta sugerido: costo + % de margen deseado**

**Descripción:** Al cargar un producto, calcular automáticamente el precio de venta sugerido a partir del precio de costo más un margen de ganancia deseado (o el margen a partir del precio de venta).

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

Verificá primero: esta funcionalidad YA ESTÁ IMPLEMENTADA en `src/app/ui/product-form.tsx`. El
campo "Margen de ganancia %" y el campo "Precio de venta" ya se recalculan automáticamente y en
ambos sentidos: `handleCostPriceChange`/`handleMarginChange` actualizan el precio de venta a
partir de costo + margen, y `handleSalePriceChange` recalcula el margen a partir del precio de
venta ingresado manualmente. El panel "Resumen del producto" en el sidebar también muestra el
margen y la ganancia por unidad en tiempo real.

Si al revisar el código confirmás que este comportamiento ya funciona como se describe, NO
hagas ningún cambio: documentá en el reporte que la tarea ya estaba implementada y salí sin
modificar archivos. Si encontrás un caso roto (por ejemplo, el cálculo no se dispara al editar
un producto existente, o el margen inicial no refleja costo/venta guardados), corregí
puntualmente solo ese caso, sin reescribir la lógica existente que ya funciona.

RESTRICCIONES ESTRICTAS:
- No reescribas el cálculo de margen si ya funciona correctamente.
- Si hacés cambios, ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/product-form.tsx` (solo si hay un caso roto que corregir)

---

### TAREA 057 · 🟠 ALTO
**Alerta visual cuando el precio de costo supera o iguala al precio de venta**

**Descripción:** Advertir visualmente cuando un producto tiene un precio de venta menor o igual a su precio de costo, ya que implica vender a pérdida o sin margen.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

1. Verificá primero: en `src/app/ui/product-form.tsx`, el panel "Resumen del producto" ya
   muestra "Ganancia por unidad" en rojo cuando `profitPerUnit < 0`, pero un margen exactamente
   en 0 (costo == venta) se muestra en verde (`profitPerUnit >= 0`), lo cual no alerta sobre el
   caso de margen cero.
2. Agregá una alerta explícita (banner o texto destacado, ej. con ícono `AlertTriangle` de
   lucide-react) cuando `costNum > 0 && saleNum <= costNum`, con un mensaje claro como "El precio
   de venta es menor o igual al costo: esta venta generará pérdida o margen cero", visible tanto
   en el formulario de alta/edición como, si es sencillo, en la lista de productos
   (`products-inventory.tsx`) para productos ya guardados en esa condición.
3. Esta alerta es informativa: no debe bloquear el guardado del producto.

RESTRICCIONES ESTRICTAS:
- No modifiques la validación del backend (`product-validation.ts`) para bloquear este caso; es
  una decisión de negocio válida en ciertos escenarios (ej. liquidación).
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/product-form.tsx`, `src/app/ui/products-inventory.tsx`

---

### TAREA 058 · 🟠 ALTO
**Indicador de margen % visible directamente en la lista de productos**

**Descripción:** Mostrar el margen de ganancia porcentual de cada producto directamente en el listado, sin tener que entrar al detalle.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/products-inventory.tsx` (listado principal de productos):
1. Agregá una columna "Margen %" calculada como
   `((salePrice - costPrice) / costPrice) * 100` (mismo cálculo que ya usa product-form.tsx),
   con manejo del caso `costPrice === 0` (mostrar "—" o "N/A" en vez de dividir por cero).
2. Coloreá el valor: verde si el margen es saludable (ej. > 20%), ámbar si es bajo (entre 0% y
   20%), rojo si es negativo o cero (podés alinear estos umbrales con los que uses en la Tarea
   060 de badges, para que ambas tareas sean consistentes entre sí).

RESTRICCIONES ESTRICTAS:
- No modifiques `/api/products` (costPrice y salePrice ya vienen en la respuesta).
- Cálculo puramente client-side.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/products-inventory.tsx`

---

### TAREA 059 · 🟡 MEDIO · UX
**Ordenamiento por margen, stock o más vendido (columna clickeable)**

**Descripción:** Permitir ordenar el listado de productos haciendo clic en los encabezados de columna, por margen de ganancia, stock o cantidad vendida.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/products-inventory.tsx`:
1. Hacé clickeables los encabezados de las columnas "Stock" y "Margen %" (esta última agregada
   en la Tarea 058; si esa tarea todavía no está implementada, agregá primero una columna de
   margen equivalente antes de ordenar por ella). Al hacer clic, ordená la lista ascendente, y
   al volver a hacer clic en la misma columna, descendente (mismo patrón que un sort table
   estándar; incluí un ícono de flecha que indique la dirección activa).
2. Para "más vendido": si el endpoint `GET /api/products` no expone la cantidad vendida por
   producto, verificá primero si existe algún endpoint de reportes que ya calcule ventas por
   producto (revisá `src/modules/dashboard` y `src/app/ui/reports.tsx`) antes de agregar lógica
   nueva. Si no hay forma simple de obtenerlo sin duplicar lógica de agregación pesada, es
   aceptable implementar el ordenamiento por Stock y Margen % en esta tarea y dejar "más
   vendido" documentado como pendiente en el reporte, explicando qué endpoint haría falta.
3. El ordenamiento debe aplicarse sobre los productos ya cargados en la página actual (cliente),
   sin romper la paginación existente del backend.

RESTRICCIONES ESTRICTAS:
- No modifiques `/api/products` salvo que sea estrictamente necesario para "más vendido", y en
  ese caso documentalo claramente en el reporte antes de tocar el endpoint.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/products-inventory.tsx`

---

### TAREA 060 · 🟡 MEDIO · UX
**Badge rojo/naranja en productos con margen negativo o menor al 10%**

**Descripción:** Marcar visualmente en el listado los productos cuyo margen de ganancia es negativo o muy bajo, para que sean fáciles de detectar y corregir.

**Prompt para el agente:**
```
Leé el archivo CLAUDE.md para contexto del proyecto.

En `src/app/ui/products-inventory.tsx` (si ya implementaste la columna de margen de la Tarea
058, reusá ese mismo cálculo; si no, calculá el margen igual que en esa tarea):
1. Agregá un badge junto al nombre o la columna de margen de cada producto:
   - Rojo ("Margen negativo") si `margin < 0`.
   - Naranja ("Margen bajo") si `0 <= margin < 10`.
   - Sin badge si el margen es 10% o mayor.
2. Mantené consistencia de colores/umbrales con la Tarea 058 si ambas fueron implementadas, para
   que la columna de margen y el badge no se contradigan visualmente.

RESTRICCIONES ESTRICTAS:
- No modifiques `/api/products` ni el schema de Prisma.
- Cálculo puramente client-side.
- Ejecutá `npm run build` al final.
```

**Archivos afectados:** `src/app/ui/products-inventory.tsx`

---

## PROTOCOLO DE REPORTE

Después de ejecutar cada tarea, el agente debe actualizar `TAREAS/REPORTE_DE_CAMBIOS.md`
con la siguiente estructura (agregar arriba de todo, del más reciente al más antiguo):

```
## Tarea XXX — [Nombre] — [fecha hora]
**Estado:** ✅ Completada | ⚠️ Parcial | ❌ Error
**Archivos modificados:** lista de archivos
**Cambios realizados:** descripción breve
**Notas:** problemas encontrados, decisiones tomadas, pendientes si los hay
---
```

Reglas del ciclo:
1. Ejecutar únicamente la tarea pedida — no adelantar otras tareas del lote.
2. Commitear los cambios con mensaje descriptivo (`feat: ...` / `fix: ...`) antes de reportar.
3. Diego hace `git push` manualmente desde su PC — el agente no pushea, salvo que la orden
   indique explícitamente "sube todo a github".
4. No borrar tareas de este archivo ni marcarlas como completadas acá — el estado se
   actualiza en Notion y en `REPORTE_DE_CAMBIOS.md` únicamente.
5. Las tareas 051, 053 y 054 requieren migración de Prisma (`npx prisma migrate dev`) — correr
   la migración es parte de "completar" esas tareas, no un paso opcional.
